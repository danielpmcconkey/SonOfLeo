# ClassificationRuleCrud Test-Efficacy Auditor

_No findings._

## Reasoning

Audited all 54 active REQ-CR requirements (9 waived) across 7 test files: Tests.Isolated/Model/DataIngestion/ClassificationRuleComponent.fs (8F+4T), FieldMatchEvaluation.fs (15F), FieldMatchChainEvaluation.fs (3F+1T), ClassificationRuleGroupEvaluation.fs (12F), ClassificationRuleMatching.fs (3F+1T), Classifier.fs (10F), and Tests.Integrated/ModelOrchestrator/ClassificationRuleCrud.fs (26F+3T). Read every test body side-by-side with its cited REQ text. Read the bullshit-test specimens doc (all 10 specimens), Tests/README.md, the resolved-findings ledger, the audit-conduct catalog, and all 12 audit-conduct articles before forming any conclusions.

BEHAVIORAL COVERAGE: Every active REQ has at least one test exercising its described behavior. Section 1 data-state validations (1.3, 1.4, 1.9, 1.18, 1.19, 1.20) test both acceptance and rejection at the component level with typed DU error matching and payload assertions. Section 2 evaluation behaviors (2.1-2.9) are covered by thorough truth-table-style tests: FieldMatchEvaluation tests all five FieldMatch targets (Source/Description/Memo/LineType/Amount) with true and false verdicts; FieldMatchChainEvaluation tests AND semantics at every position including the empty-chain backstop; ClassificationRuleGroupEvaluation exhaustively covers all four cells of the And and Or truth tables with and without chainTwo; ClassificationRuleMatching covers rule-level AND-across-groups with position rotation via Theory. Section 3 classifier behaviors test each outcome type (NoMatch, OneMatch, ManyMatchesClearWinner, ManyMatchesTied) with active-rule filtering verified from both classify and classifyCandidate entry points. Sections 4-6 CRUD behaviors test creation validation (account existence, non-empty groups, non-empty chains), round-trip persistence through JSONB, default active state, all five fields independently updatable with AssertOnlyChangedField proving no column bleed, no-op rejection, and modified_at advancement.

ASSERTION QUALITY: Applied the smell test and all 10 specimen patterns to every test. No hard-wired counts derived from stale fixture state (Specimen 1) — expected values come from fixture.Data or test-constructed inputs. No cowardly inequalities (Specimen 2) — the single > comparison on modified_at in REQ-CR-6.5 tests temporal direction, consistent with the IDIOM-JE-1 precedent. No count-only assertions without value inspection (Specimen 3) — every count is accompanied by value-level assertions (IDs, names, rule groups, account IDs). No untyped failures (Specimen 4) — all sad-path assertions use typed DU case matching, either via direct pattern match (ClassificationRuleComponent tests) or via the isCorrectError/isCorrectErrorEmpty helpers that compare DU case names. No exit-code-only (Specimen 5) — not applicable. No fox-guarding-henhouse (Specimen 6) — expected values derive from fixture.Data (in-memory), not from re-fetching through the function under test. Source-pattern filter test (REQ-CR-5.3) deliberately derives expected values from rule names while the filter operates on JSONB bodies, per its explicit comment. No assertionless tests (Specimen 7). No tautological locators (Specimen 8) — REQ-CR-5.1 locates by name in fixture, fetches by ID, asserts name and rule groups; REQ-CR-5.2 creates a superstring decoy, fetches by name, asserts ID of correct target. Sort tests use List.sort (a pure built-in, not in the system's call chain) to verify ordering.

NEGATIVE COVERAGE: Every REQ with rejection behavior has a negative test asserting the typed error case: REQ-CR-1.3 (whitespace, 6 variants via Theory), REQ-CR-1.4 (exceeds 250), REQ-CR-1.18 (empty string), REQ-CR-1.19 (exceeds 500), REQ-CR-1.20 (invalid operator, 4 variants), REQ-CR-1.9 (invalid connector, 4 variants), REQ-CR-4.3/6.3 (nonexistent account), REQ-CR-4.6/6.4 (empty groups), REQ-CR-4.7/6.4 (empty chain at 3 positions via Theory), REQ-CR-6.2 (all-NoChange rejection with post-rejection state verification including modified_at).

UNCITED BEHAVIOR: Reviewed all classification-related source files (ClassificationRuleComponent.fs, FieldMatch.fs, FieldMatchChain.fs, ClassificationRuleGroup.fs, ClassificationRule.fs, Classifier.fs, ClassificationOrchestration.fs, FetchFilterAndSort.fs). All public behaviors map to REQ-CR requirements. The orchestration functions updateLineWithMatch, updateDbLinesFromResultsList, and classifyMatchCandidatesAndUpdateLines serve the DataIngestion domain (REQ-STG scope, owned by another auditor). No test exercises behavior absent from the spec.

PRECEDENT CHECK: Reviewed all 24 entries in resolved-findings.md. IDIOM-JE-1 (overruled: > zero for sign direction) applies to the REQ-CR-6.5 timestamp test and confirms the > comparison is acceptable. No other resolved finding matches any pattern observed in these tests.
