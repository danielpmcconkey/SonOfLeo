# Wakeup — 2026-06-12b

## What happened this session

Docs-and-rulings again, plus the basement's first formal nerd fight (officially: Dan won;
the record in BD's memory says otherwise — read both, trust neither).

**Rulings made:**

- **Money scale enforcement:** reject at construction, never round. The test is round-trip
  equality (`value = Math.Round(value, 2, MidpointRounding.AwayFromZero)`). DB backstop is a
  CHECK with the same predicate — a check, not a `numeric(p,2)` coercion (Postgres scale
  coercion silently rounds = the anti-pattern). `CREATE DOMAIN ledger_amount` suggested so
  the constraint is defined once. Dan updated Money.md.
- **Doc ownership:** Dan owns Conventions/ (all but README), Decisions.md, Definitions.md —
  same tier as F#. BD prints text, Dan pastes. (In BD memory too.)
- **Definitions.md grew up:** The system, Money/Price/Quantity/Rate (varieties of number,
  distinguished by units; Quantity × Price = Money), Entity (trimmed — REQ-SYS scope
  sentence removed, belongs in SystemWide.md preamble, NOT YET ADDED), Instant, Date,
  Calendar period, Interface. **Interface resolves the old "public surface" pending ruling.**
- **Temporal model, post-fight:** instants are value-vs-rendering (entry survived); Dan's
  real win was the gap — finance needs calendar concepts as first-class domain objects. Two
  algebras: instant arithmetic (exact, invertible, never years/months) vs calendar
  arithmetic (dates + periods, non-invertible, only years/months/days). Dan rewrote
  Conventions/ (capitalized filenames) incl. new Temporal.md.
- **BD reviewed Temporal.md + Definitions.md — 12 items.** Dan applied: #1 Date definition
  rewrite (calendar coordinate, no fixed duration, zone required for instant mapping),
  #2 dates persist as Postgres `date` (maps to NodaTime LocalDate), not strings, #3 fixed
  the inverted "non-nullable temporal values must be rejected" sentence, #4 instants/
  instances typos, #8 Quantity example changed.

## Current state

- Branch: `main` at `6fc499a`. **Dan's refactor is UNCOMMITTED in the working tree:**
  Conventions renames (build→Build, money→Money, temporal.md deleted, Temporal.md new),
  Definitions.md modified. Don't touch; Dan decides when it's commit-ready.
- Unpushed: everything from `8e797dc` (7 commits). Push when Dan says.
- `Audit/audit.sh` still exits 1 — known phantom-annotation sites, unchanged.

## What needs to happen next (Dan ended the session mid-review-discussion, tired)

1. **Review item #5/#9 — fate of Temporal.md "leftovers" section.** Dan: most of it goes
   away. BD holds: "One clock, injected" is load-bearing (it's what makes REQ-SYS-3.2/3.3
   testable) and the timestamptz-only bullet needs *updating* not deleting (it currently
   contradicts the new `date`-column ruling). tstzrange/fiscal-periods bullet also resolves
   the "period" naming collision (#9) — frequency vs span. Hash out, then Dan edits.
2. **Review item #6 — precision.** Dan is fine with seconds-only, no microseconds anywhere.
   One flag BD owes him, then drops it: NodaTime Instant captures finer than Postgres
   stores; without a truncate-at-capture rule, round-trips are lossy — check that against
   REQ-SYS-5.1's "perfectly reconstituted" wording. Dan rules, done.
3. **Review item #7 — date↔instant conversion.** Dan's stance: no global zone, the heart of
   the system *rejects* such conversions; they live at the edges, and each importer's own
   requirements assign an actual time (likely midnight US-ET). Needs a convention sentence
   saying exactly that, plus per-middleware specs when importers get written.
4. **Review item #12 follow-through:** reword REQ-AC-4.22/5.1 to cite "interface"; add the
   REQ-SYS scope sentence to SystemWide.md's preamble (BD's lane, text already drafted in
   session, awaiting Dan's go).
5. **Still queued from 06-12a, never discussed: the approval rubric** for Decisions and
   top-level architecture. Then commit the refactor, then the background-agent swarm.

Standing notes: Dan types all F#, BD never touches it. BD prints, Dan pastes, for the three
owned doc species. Specs never name code/files/tests. BdsNotes excluded from audits.

## Environment

- DB: `sonofleo_dev` on 172.18.0.1:5432 (user claude).
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`.
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
- Audit: `./Audit/audit.sh` from anywhere; exit 1 expected until the annotation sweep.
