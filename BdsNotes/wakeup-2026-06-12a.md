# Wakeup — 2026-06-12a

## What happened this session

Another docs-and-rulings session; the only code change was Dan's (comment-only). The 11b
pending decisions all got resolved, and then the session went deep on the cross-cutting
invariants: time and money.

**Rulings made (all logged in Specs/Decisions.md, conventions in Specs/Conventions/):**

- The three PENDING waivers: REQ-AC-2.17 reason rewritten (validating "active begin" is the
  caller's responsibility, not absent behavior) and approved; REQ-AC-4.22 approved as
  written; REQ-SYS-4.1 dissolved entirely — deletion policy is per-entity, not system-wide.
  REQ-SYS-4.1 withdrawn (SystemWide now has its own Withdrawn table), REQ-AC-5.1
  resurrected as active with an approved waiver.
- **Definitions is a fourth document species** (`Specs/Definitions.md`, above the domains).
  Entity (two litmus questions — runtime user writes? regenerable from spec? — validated
  against leobloom_dev's real 22 tables), instant, and system clock: approved. "Public
  surface" entry: PENDING, on the back burner (alternative: reword REQ-AC-4.22/5.1 to
  "public means of" and drop the entry).
- **Temporal model:** all temporal values are instants; no date-only values anywhere;
  persistence discards original offsets; imported calendar dates anchor to US-ET; NodaTime
  is now a requirement (Instant everywhere, one injected IClock, truncate to micros at
  capture); Postgres timestamptz only, no DEFAULT now(), connections pin Timezone=UTC,
  Npgsql.NodaTime, tstzrange for future period ranges; leap seconds formally ignored.
- **Money:** exact decimal end to end (no float/double/real/pg-money); ledger amounts are
  USD scale 2; balance invariant is exact with NO tolerance (intra-system arithmetic);
  reconciliation tolerance is domain data — per-account-class thresholds, accepted
  discrepancies posted as explicit adjustment entries; rounding is half-up (BD's call under
  Dan's delegation, .NET defaults to banker's so always pass the mode); allocation must sum
  exactly; USD-only cash basis (foreign transactions enter as the USD the FI settled).
- **SolutionConventions.md split into `Specs/Conventions/`** with a card-catalog README —
  agents read the index, then only the file their task touches.
- Dan busted the PersonalFinance integer-arithmetic memory: that repo is decimal throughout,
  no ×1000 anywhere; its `< 1m` "call it a wash" tolerance is the anti-pattern SonOfLeo's
  balance invariant exists to forbid.

**Working rules established (also in BD's memory, project_sonofleo.md):**

- F# code is Dan's alone. BD and agents NEVER touch it. Table stakes.
- BD writes Specs/ freely; comment-only annotation edits in .fs files allowed; SQL changes
  allowed only with announcement + Dan's approval.
- **Dan reads Specs just-in-time** — at the point a concrete change confronts him with the
  text, never before. Consequence: every agent recommendation must QUOTE the spec text that
  justifies it; that citation is Dan's reading hook. Spec commits don't wait on his read.
- Commit ceremony deferred until the app is handed back to agentic dev; for now BD commits
  prose to main as it lands.

**Commits this session:** `aeba5c5` (deletion policy + waiver ratifications), `9ad220d`
(Definitions species), `fb936b5` (Conventions split + temporal + money + Decisions log),
`bffeddf` (Dan's comment-only removal of misapplied REQ-AC-1.25/26/27 annotations from
Account field declarations).

## Current state

- Branch: `main` at `bffeddf`, working tree clean.
- **Unpushed:** everything from `8e797dc` onward (6 commits). Push when Dan says.
- Builds clean. `Audit/audit.sh` still exits 1: the known phantom-annotation sites
  (Account.fs 96/97 + others, AccountComponent.fs, CreateAccountTable.sql) and the bogus
  `REQ-AC-001` test stub. Lines 16/17 of Account.fs are resolved (Dan deleted those).

## What needs to happen next

1. **Today's discussions (Dan queued these explicitly):**
   - **Approval rubric for Decisions and top-level architecture.** Dan's framing: a
     Decision or architecture choice outranks a minor requirement ("journal notes ≤ 500
     chars") and needs a heavier approval gate than the current append-and-go. Design the
     rubric with him.
   - **"Enforced at construction"** for money scale (marker sits in
     Specs/Conventions/money.md) — how scale/validation enforcement actually works,
     mechanism TBD.
   - Ruling 2 off the back burner if he's ready: "public surface" definition vs rewording
     REQ-AC-4.22/5.1.
2. **Then the background-agent swarm** (Dan will trigger): agents review specs, code,
   migrations line-by-line and RECOMMEND. Hard rules for the swarm prompt: agents never
   touch F#; every recommendation quotes the spec text justifying it; BD executes only
   prose + approved SQL. Commit the rubric/construction rulings first so the swarm has a
   stable baseline.
3. **Code-session backlog (Dan types, BD tutors):** annotation sweep for the remaining
   phantom sites (mapping table is in the 2026-06-11 session transcript and derivable from
   the Withdrawn tables); add REQ-SYS-3.1 annotations to Account.fs:16-17 (agreed); fix or
   delete the REQ-AC-001 test stub (BD leans delete); NodaTime + IClock refactor; migration
   to drop DEFAULT now() (BD may write SQL with approval); UtcNow/Now sweep; REQ-AC-1.5
   case-sensitivity gap check; reconstitute id≠parentId gap; then the test mountain
   (102/105 untested, every test case needs Dan's approval).

Standing notes: Dan types, BD explains — no F# from BD, ever. Specs never name code/files/
tests. BdsNotes excluded from all audits and find/replaces.

## Environment

- DB: `sonofleo_dev` on 172.18.0.1:5432 (user claude). leobloom_dev used read-only for
  schema archaeology.
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`.
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
- Audit: `./Audit/audit.sh` from anywhere; exit 1 expected until the annotation sweep.
