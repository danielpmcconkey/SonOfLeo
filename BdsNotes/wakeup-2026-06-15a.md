# Wakeup — 2026-06-15a

## What happened this session

Continued from wakeup-2026-06-14b. This was a long session spanning both days.

**Environment isolation validated:**
- Confirmed test DB and dev DB are properly isolated by running the REQ-AC-1.4
  test and CLI binary, checking rows in both databases.

**DAL requirements audit:**
- Withdrew 12 stale REQ-DAL-1.x requirements and REQ-DAL-3.2.2.
- Dan wrote 6 new requirements (REQ-DAL-1.14 through 1.19) for the new
  ConnectionStringEnvVar pipeline. BD annotated enforcement points in DAL.fs.

**Test project cleanup:**
- Removed Tests.Model and Tests.Ledger (empty shells). Deleted from solution and filesystem.

**Test implementation (Dan typing, BD reviewing):**
- Integrated tests implemented and passing:
  - REQ-AC-1.4: unique account code
  - REQ-AC-1.5: case-sensitive account code
  - REQ-AC-2.14 / REQ-SYS-5.1: create + fetch by ID round-trip (all fields)
  - REQ-AC-3.4: fetch by code
  - REQ-AC-3.5: fetch by parent ID (parent + 3 children)
  - REQ-AC-3.6: fetch by account type
  - REQ-AC-2.6: parent ID must reference existing account (negative)
  - REQ-AC-2.7: parent must be active (positive and negative)
  - REQ-AC-2.19: child AccountType must match parent (negative)
  - REQ-AC-4.1: deactivate sets active end
  - REQ-AC-4.2: rejects end earlier than begin (2 tests: earlier and equal)
  - REQ-AC-4.3: rejects deactivation with active children

**Code changes:**
- Implemented REQ-AC-2.19 (`confirmAccountTypesMatch`) in Account.fs.
- Moved `isActive` logic to `AccountActivityPeriod` module; `Account.isActive` delegates.
- Refactored cleanup helpers: `cleanUpAccountId` and `cleanUpAccountList` accept
  `Guid option`. Added `cleanUpParentIdAndChildren` for FK-safe ordered cleanup.
- Disabled parallel test execution in Tests.Integrated via `xunit.runner.json`.
- Fixed microsecond precision bug (tests now use `Clock.now()` not `Instant.FromDateTimeOffset`).

**F# learning moments:**
- `function` keyword as shorthand for `fun x -> match x with`
- `List.choose id` to flatten `option list`
- Result CE silently swallows errors if return value isn't inspected
- `String('A', 100)` for repeated char strings
- Monads explained: wrap a value + chain operations. Result CE is monadic.

## Current state

- Branch: `main` at `301ccd3`. Clean working tree.
- Hobson's wakeup (`HobsonsNotes/wakeup-2026-06-14a.md`) still untracked.
- Test count: 78 total (57 isolated + 21 integrated). 24 passing, 54 stubs remaining.
- Known issue: REQ-AC-4.3 test uses code `"AC-2.19-C1"` for child — copy-paste
  artifact, should be `"AC-4.3-C1"`. Cosmetic only.

## What needs to happen next

1. **Continue implementing test stubs.** Remaining integrated stubs:
   - REQ-AC-4.5: rejects already deactivated account
   - REQ-AC-4.8: updateAccountName (valid + invalid)
   - REQ-AC-4.9: updateExternalReference (valid + clear to None)
   - REQ-SYS-3.3: update sets modifiedAt from AuditEnvelope
   - REQ-AC-4.19: update to deactivated account is permitted
2. **Isolated test stubs:** 47 remaining across AccountComponent, Account, and Money.
   These should go fast — no DB, no cleanup, no result CE railroad.
3. **Items from prior sessions still open:**
   - Audit workflow skill (in `Skills/SonOfLeoRequirementsAudit/`)
   - DEC-1: Convention "must" vs requirement "must"
   - MR-4: Debug-mode production-DB prohibition recommendation
   - Approval rubric for Decisions.md
   - DB ownership: future tables need `REASSIGN OWNED BY` after migrations

Read `BdsNotes/wakeup-2026-06-15a.md` before starting work.

## Environment

- DB: `sonofleo_dev` / `sonofleo_test` / `sonofleo_prod` on 172.18.0.1:5432.
  Each env uses its own role. Connection strings in env vars named by
  `ConnectionStringEnvVar` in appsettings.
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`
- Test: `dotnet test --artifacts-path /tmp/sonofleo-build`
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
