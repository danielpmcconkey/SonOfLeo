# Wakeup — 2026-08-02b

## Read this one, and nothing older

Supersedes `wakeup-2026-08-02a.md`.

## What happened this session

Three pieces of work, all merged to main.

### 07b error trace gap filling — batch 2 (branch `fill-test-gaps-batch-2`)

Added 33 route-level multi-fail tests covering the remaining actionable "lower"
rows from `BdsNotes/07b-error-trace-dan-augmentation.md`. Updated the 07b table
to reflect all coverage. After Dan's annotations, the only uncommented row left
is `JournalEntryLineAccountDoesntExist` (stale-cache backstop, annotated as such).

Key commits: `639e32b`, `e6e8bba`, `e2016ce`.

### Small cleanup items

- Renamed 5 tests from REQ-AC-1.19.1 → REQ-AC-1.19 (`464c1fc`).
- Removed 3 stale `// todo: ask claude to provide the correct REQ #` comments,
  fixed collapsed lines from the removal (`254fe37`, `3e0966e`, `972cd88`).

### P2 — Behavioral coverage debt (branch `p2-behavioral-coverage-debt`)

New fixture data: accounts F-4500 (Temporal Revenue) and F-5700 (Temporal Expense)
with three JEs spread across today-3/today-1/today at distinctive amounts
($137.42/$88.17/$61.50) and descriptions ("Temporal entry alpha/beta/gamma").

Tests written:
- **REQ-AC-4.6** — renamed from REQ-AC-4.1 (deactivation-JE-date test).
- **REQ-AC-1.40** — parent must exist (orchestration level, Form 3).
- **REQ-AC-3.3** — fetchById returns matching account (model level, Form 2).
- **REQ-JE-3.6.1** — net balance positive in normal-balance orientation (Form 3,
  isolated accounts + auto-rollback, no fixture dependency).
- **REQ-JE-3.6.2** — as-of balance excludes entries after cutoff (Form 2).
- **REQ-JE-3.6.2** — as-of before all entries returns zero balances (Form 2).
- **REQ-JE-3.9** — activity filter by amount (Form 2, derived from fixture data).
- **REQ-JE-3.9** — activity filter by description (Form 2, derived from fixture data).

Two production bugs found and fixed by Dan:
- As-of balance: `asOf` JOIN condition on `je` didn't exclude `jel` amounts (`830b5c8`).
- Description filter: DU interpolation produced type name instead of inner string (`c54fe0c`).

Dan also added REQ-AC-3.3.1 to the unenforceable list (`6001d66`).

**Test standard update:** Added advisory to `Tests/README.md` — when testing a
filter, derive the filter target from fixture data, not hardcoded values.

## Current state

- Branch `main` @ **7ed7aef**, pushed. Clean working tree.
- 423 tests (254 integrated + 169 isolated), all passing.
- Traceability audit: all P2 target REQ IDs appear in green tests.

## What needs to happen next

Check `HobsonsNotes/roadmap-2026-08-02.md` for the full roadmap. P2 is done.
Remaining phases for BD:
- **P3** — DAL Proving Ground (Hobson preps the standard, BD writes tests).
- Activity filter permutation tests beyond amount/description (source, accountType,
  etc.) are not part of P2 but may come up in future work.

## Environment

- **The test database is shared.** BD's container and Dan's host both point at
  `Host=172.18.0.1 / sonofleo_test`, and `TestDataFixture` opens with
  `TRUNCATE ... CASCADE`. Announce before running; stop when Dan is verifying.
- `dotnet build --artifacts-path /tmp/sonofleo-build` — never the bare form.
- `fsharp-guard` blocks `Edit`/`Write` on `.fs` under `Src/`. `Tests/`, `Skills/`,
  `CompoundedLearnings/`, `BdsNotes/` are open. The hook does not cover `Bash`; the rule
  holds anyway.
- `git-guard` blocks `checkout`, `restore`, `clean`, `stash`, `reset --hard`,
  `switch --force`. Use `git switch -c`.
- Push needs `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`.
- Pre-commit hook runs `Checks/run-all.sh --quick`: 7 pass, `check-apperror-coverage`
  skipped as slow.

## Before starting work

Read `HobsonsNotes/roadmap-2026-08-02.md` for the big picture. Then `Specs/README.md`,
`Tests/README.md`, and the README of whichever test directory the work lands in. Then the
relevant `CompoundedLearnings/catalogs/` — only follow an article when its "read when"
trigger actually fires.

**Take no action until Dan says go.**
