# Wakeup — 2026-08-18b

## Read this one, and nothing older

Supersedes `wakeup-2026-08-18a.md`. Dan also has `HobsonsNotes/wakeup-2026-08-18a.md`
from the same day; that's Hobson's, not yours.

## What happened

Started with 1 untested requirement and 5 `Assert.Fail "not implemented"` stubs. Ended
with 0 of each. 190 isolated + 374 integrated passing, 9 checks green.

### REQ-STG-1.3 — the last untested, unwaived requirement

Three tests. Two in `StageEntryIngestion.fs` for the grouping half (6 rows / 2 groups → 2
entries with 4 and 2 lines; a group_id reused in a *second file* joins nothing, which is
the "within a single file" clause). One in `StageEntryPosting.fs` for the posting half — a
four-record group posts as one JE carrying four lines, read back via `fetchByReference`.

Watch the comments you write: an early draft cited `(REQ-STG-1.4)` in prose and the
traceability audit counted it as a test annotation, turning a waived REQ into a stale
waiver. The audit greps for the string, it doesn't parse F#. Don't put a REQ ID in a test
comment unless you mean it.

### The five route stubs

All implemented in `Tests/Tests.Integrated/InterfaceBridge/IngestionRoutes.fs`, form 4,
each owning its cleanup. Shared scaffolding at the top of the class: `ingestThroughRoute`,
`headerIdsToCleanUp`, `postThroughRoute`, `refetchStageEntry`, `latestStatusOf`,
`twoValidGroups`.

The REQ-STG-8.1 one is the real shadow-post check the last wakeup asked for. It observes
from *outside* the route — after the route returns, no JE exists for either fi_reference
and the entries are still `Classified`. It also pins the F-5300 debit delta at 60.10
*inside* the returned before/after pair, so a shadow post that silently posts nothing
fails. (First draft used `Assert.NotStrictEqual` on the two row lists — that's reference
equality, always true for two F# lists, a guaranteed silent pass. Caught in self-review.)

`Cleanup.fs` gained `cleanUpStageEntryHeaderId` / `...IdList` (audit → lines → header, FK
order) and `cleanUpIngestionSourceId`.

### Dan fixed Src mid-session

`updateStageEntry` used to call `StageEntryHeader.updateDb` unconditionally, and that
errors when all six header fields are `NoChange` — so a line-only edit was impossible.
Logged it, Dan fixed it in `9bdbfda` (`isThereAHeaderUpdate` / `isThereALineUpdate`, plus
a new `IngestionUpdateStageEntryNoOp` error). Then: removed the workaround from the route
test, deleted the finding note, added a no-op test at the orchestrator, and stripped the
`statusUpdate = SetTo Reviewed` scaffolding from both `REQ-STG-6.1` tests in
`StageEntryUpdate.fs` so they exercise only what 6.1 claims.

## Known-weak — do not trust these

The traceability audit is green. Green there means a test *cites* the REQ, not that it
checks it. Four cite and don't check:

- **REQ-STG-9.3, 9.4 (happy path), 9.5** — `StageEntryPosting.fs`. All three are the same
  four lines: find the staged entry, build a `JournalEntrySource`, `do! postStageEntry`.
  No assertion. Break the header mapping, the account-code resolution, or the external
  reference and all three stay green. Fix is one shared refetch-and-assert through
  `fetchByReference`, ~40 lines.
- **REQ-STG-9.8** — asserts `post` returns `Error`. The requirement's claim is "all or
  none"; the *none* half is unverified. Needs a JE count before and after.
- **REQ-STG-8.4** — its only coverage is the orchestrator test the last wakeup already
  flagged, which observes from inside the rollback that is the feature. The route test
  added this session does check staging is untouched, but it's tagged 8.1 only, so 8.4
  isn't drawing on it. Either tag it or give 8.4 its own assertions.

Also still open from the last wakeup, untouched this session and not blocking:
`REQ-STG-1.4` is listed in **both** the Waived and Unenforceable tables in
`Specs/Behavioral/DataIngestion.md`. The audit reports it as "dual-classified" every run.
Spec bookkeeping, Dan's call.

## Current state

- Branch `data-ingestion` @ `7f00370` plus this wakeup commit. Tree clean, **not pushed**.
  Commits ahead of `9bdbfda`: `054cca0`, `55552eb`, `7f00370`.
- 190 isolated + 374 integrated passing. 0 failing, 0 stubs.
- `bash Checks/run-all.sh` → 9 passed, 0 failed.

## What needs to happen next

1. Push, if Dan wants it pushed. Nothing has left the container.
2. The four weak tests above, in that order — 9.3/9.4/9.5 first, they share one fix.
3. Dan asked "are you done with this slice?" and the honest answer was no, for exactly
   those four. He has not yet said to fix them.

## Environment

- **The test database is shared.** BD's container and Dan's host both point at
  `Host=172.18.0.1 / sonofleo_test`, and `TestDataFixture` opens with `TRUNCATE ... CASCADE`.
  Announce before running; stop when Dan is verifying. The `claude` psql user has no
  CONNECT privilege on `sonofleo_test` — you cannot inspect it directly, only through tests.
- `dotnet build --artifacts-path /tmp/sonofleo-build` — never the bare form.
- `fsharp-guard` blocks `Edit`/`Write` on `.fs` under `Src/`. `Tests/`, `Skills/`,
  `CompoundedLearnings/`, `BdsNotes/` are open. Src fixes go to Dan with the exact diff.
- `git-guard` blocks the working-tree-destroying git verbs (the restore family, the
  branch-switching family, `clean`, `stash`, hard reset). Two consequences: you cannot
  stash before a rebase — commit first — and the guard matches on the *command string*,
  so a bash heredoc that merely names those verbs in prose gets blocked too. Write such a
  file with the Write tool instead.
- Push needs `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`.
- Pre-commit hook runs `Checks/run-all.sh --quick`.
- **Rules of engagement, confirmed present:** the `SonOfLeo-TestWriter` and
  `SonOfLeo-CodeReviewer` skills, `Tests/README.md` plus the README in whichever test
  directory you're in, and the `CompoundedLearnings/catalogs/` entries whose triggers fire.
  There is no `BdsNotes/workflow.md` here — that's a LeoBloom thing.

## Before starting work

**Take no action until Dan says go.**
