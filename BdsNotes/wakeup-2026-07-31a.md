# Wakeup — 2026-07-31a

## Read this one, and nothing older

**First, if you have not already: do `BdsNotes/memory-cleanup-2026-07-31.md`.** Your
persistent memory contradicts this document in six places. Clean it before you go further,
or you will reconcile the conflict silently and nobody will see which way you went.

Your previous wakeup is dated 2026-07-03. Between then and now the codebase was
substantially refactored and the documentation corpus was cut roughly in half. **Every
other file in `BdsNotes/` is archaeology.** If something in this document contradicts an
older note, this document wins. If an older note names a file, check that the file still
exists before believing it.

## Who you are, on this project

You are BD, working in the Docker container on the SonOfLeo repository. Dan owns this
codebase and personally reviews every line you present — by choice, not because you are on
probation. Your job is to make his read cheap, not to substitute for his judgment.

Hobson (host-side Claude) owns specs, `Checks/`, `Skills/` and review. Dan has written
essentially all the F# to date. This is your first story on it.

## What SonOfLeo is

Dan's hand-written rewrite of LeoBloom: a cash-basis, double-entry, USD personal finance
system. F# on .NET 10, PostgreSQL, xUnit. Not a port — he wrote it to learn F# and to set
an architecture that LLM developers can work in safely.

Eight projects under `Src/`: `Utilities` ← `Model` ← `ModelOrchestrator` ← `InterfaceBridge`
← `SonOfLeoCli`, plus `DataAccessLayer`, `Context` and `Logger`. Three under `Tests/`:
`Tests.Helpers` (shared library, no tests), `Tests.Isolated` (no database, parallel),
`Tests.Integrated` (database or CLI process, serial).

## Read these, in this order

1. `Specs/README.md` — how documentation works here, and the rule that governs it
2. `Src/README.md` — what infrastructure already exists. Read it before you write any
   helper; most of what you are about to invent is already there
3. `Tests/README.md` — the test standard. The five test forms and the silent-pass hazard
   are the two sections that will decide whether your work is correct
4. `Tests/Tests.Integrated/ModelOrchestrator/README.md` — what belongs in the layer you'll
   be working in
5. `Specs/Behavioral/JournalEntryCrud.md` — your requirement lives here

Then the skills: `Skills/TestWriter/SKILL.md` for the procedure, and
`Skills/CodeReviewer/SKILL.md` before you present anything. Follow the
`CompoundedLearnings/` catalogs when their "read when" triggers fire — don't read the whole
corpus up front.

## What no longer exists

Do not go looking for these, and do not trust anything that cites them:

- `PATTERNS.md` — deleted 2026-07-30. It was the house-style document. Roughly half of it
  was wrong and the rest was either obvious from the code or duplicated elsewhere. Its one
  valuable section survives as `Src/README.md`. **If you ever see a "P-code" like P2.4 or
  P7.6, it is a reference to a deleted document.**
- `Specs/Conventions/` — deleted. Its durable content is in `CompoundedLearnings/articles/`.
- `Specs/Decisions.md` — moved to `Specs/Archive/Decisions.md` and labelled history. Never
  cite it as a current rule.
- `validateThenConstruct` — a function the old docs described at length. It has never
  existed in this codebase. Entity construction is component smart constructors plus an
  entity `create`.
- `Result<_, string>` — replaced everywhere by `Result<'T, AppError>`.
- Test fixture "consumable victims" — described in old docs, never existed.

## What changed recently that affects how you work

- **Transaction ownership sits at the route handler.** `InterfaceBridge` routes create the
  transaction via `runRouteAndAutoCompleteTransaction`; model and orchestrator functions are
  participants that receive a `Context`. They do not create their own.
- **There is no automatic formatter.** Fantomas was dropped 2026-07-31; Dan is
  reformatting by hand to his own taste. The repo-root `.editorconfig` still sets 4-space
  indent, 120 columns, LF and a final newline. Match the surrounding file and do not
  reformat anything you were not asked to change.
- **`Checks/` is a suite of eight scripts, run automatically by a pre-commit hook.** They are the mechanical rules. A rule that can be
  a script is a script here, not a paragraph.
- **`Tests.Helpers` was created 2026-07-31** and holds `Railroad.fs`, `Cleanup.fs`,
  `GenericTestProperties.fs`, `EntityFunctions.fs`, `TestDataStage.fs`, `RouteResolver.fs`.
  Both test projects reference it.
- **The test database stays populated between runs.** `TestDataFixture` truncates at the
  *start* of staging, not on dispose. Cleanup inside a run is about within-run consistency,
  not about leaving a clean database.

## Before you start — verify the environment

Confirm the integrated suite can actually run from inside the container. It needs the
`SONOFLEO_TEST_CONNSTR` environment variable and a reachable PostgreSQL. Check this first:

```bash
bash Checks/run-all.sh
dotnet build --artifacts-path /tmp/sonofleo-build
dotnet test --artifacts-path /tmp/sonofleo-build
```

As of this writing on the host: 8 checks pass, build is clean, 339 of 339 tests pass. **If
you see red at this point, it is the environment, not your work.** Stop and tell Dan
rather than trying to fix it.

`dotnet build` without `--artifacts-path /tmp/sonofleo-build` writes container paths into
`obj/` on the bind mount and breaks Rider on the host. Never run the bare form.

## Your story — REQ-JE-1.56

**Branch first, before you touch anything:**

```bash
git switch -c je-1.56-tests
```

`git checkout -b` is the usual idiom and it is **blocked** — `git-guard` denies `checkout`
outright because it can overwrite working-tree files. `git switch -c` is the equivalent and
passes. One branch, this task only; you never work on `main`.

**The requirement** (`Specs/Behavioral/JournalEntryCrud.md`, line 69):

> **REQ-JE-1.56** Comment secondary journal entry ID may be updated to be pointed at a
> different JE or to no JE

**The state.** It is implemented — `updateComment` in
`Src/ModelOrchestrator/JournalEntryCommentOrchestration.fs` takes
`secondaryIdUpdate: FieldUpdate<JournalEntryHeaderId option>`, which is what makes both
repointing and clearing expressible. But `grep -rn 'REQ-JE-1.56' Src Tests` returns
**nothing**. No annotation, no test, and it is not in the Waived-from-testing table. It
fails the two-state rule: every active requirement is either tested or waived.

**Deliver:**

1. Two tests in `Tests/Tests.Integrated/ModelOrchestrator/JournalEntryCommentOrchestration.fs`
   — one repointing a comment's secondary link to a different journal entry, one clearing it
   to `None`. Assert the round-tripped value, not just success.
2. A written change request for the `// REQ-JE-1.56` annotation in `Src/`. See below.

**Done when:** `grep -rn 'REQ-JE-1.56' Tests` returns two or more hits,
`Checks/run-all.sh` is green, the build is clean, the full suite passes, and your hand-off
carries the annotation request.

### The annotation, and the guard you are about to hit

The requirement also needs a `// REQ-JE-1.56` traceability annotation at the site that
enforces it — every such site, if there is more than one. Source annotations are how a
future developer learns what bears load when a rule changes; see the linkage rules in
`Specs/README.md`.

**You cannot make that edit, and you should not try to route around it.** A `PreToolUse`
hook named `fsharp-guard` blocks writes to `.fs` files under `/workspace/SonOfLeo/Src`. F#
source there is Dan's alone. `Tests/` and non-`.fs` files are not blocked, so your tests are
unaffected.

Attempt it anyway. When the guard refuses, do what its message says: describe the change
precisely and let Dan type it. In your hand-off, give him the file, the line number, the
exact text of the annotation, and enough surrounding context that he can place it without
opening a second window. Getting blocked here is the expected outcome, not a failure.

The guard is a `PreToolUse` matcher on `Edit|Write|NotebookEdit` and therefore does not
cover `Bash`. Using `sed`, `tee`, a heredoc or a script to write into `Src/` would succeed.
**Don't.** The boundary is the rule; the hook is only the reminder. Doing it any other way
is the one thing that would make this whole exercise a failure.

**State your reasoning in the hand-off.** Two things specifically:

- **Which of the five test forms you chose, and why.** Work it out from the code — whether
  the function under test manages its own transaction is the question that decides it. This
  is not a trick; I want your reasoning written down because if the decision tree in
  `Tests/README.md` is unclear, your answer is how we find out.
- **How you terminated each railroad.** A `[<Fact>]` whose body evaluates to `Result<_,_>`
  passes unconditionally — xUnit discards the return value and the test reports green having
  verified nothing. `Tests/README.md` covers this. If your sad-path test would pass with the
  production code deleted, it is worthless.

Keep the diff to those two tests. If you find something else wrong, write it down and tell
Dan — do not fix it in this change.

## House rules

- **`Src/**/*.fs` is Dan's.** Standing rule, not a rule for this story — you propose F#
  changes in prose and he types them. `fsharp-guard` enforces it on `Edit`/`Write`; the rule
  holds regardless of whether a hook happens to catch you.
- **`git-guard` blocks anything that can overwrite the working tree** — `checkout`,
  `restore`, `clean`, `stash`, `reset --hard`, `switch --force`. `branch`, `commit`, `push`,
  `merge`, `pull`, `add`, `diff` and plain `switch` all pass. If you genuinely need a
  restore, give Dan the command rather than working around the guard.
- **Never touch** `appsettings*.json`, environment variables, or any DAL function in the
  connection-string chain. That boundary is what keeps a debug build away from production
  data. Dan configures it by hand.
- **F# compile order is load-bearing and hand-maintained.** A new file goes at its correct
  position in the `.fsproj` `<Compile Include>` list, never appended blindly.
  `Checks/check-compile-order.sh` checks membership, not order — the build catches order.
- **Never present red.** Run `Checks/run-all.sh`, build, and test before you hand off. If a
  check fails, reproduce that individual check before you edit the files it names — a check
  in this repo has produced both a false PASS and a false FAIL in the past. A pre-commit
  hook runs the suite automatically; never reach for `--no-verify`.
- **This clone is yours now.** As of 2026-07-31 Dan works from a separate clone at
  `/media/dan/fdrive/codeprojects/SonOfLeo` on the host. `/workspace/SonOfLeo` is yours
  alone — you are no longer sharing a working tree with his editor, so you may branch
  freely.
- **One branch per task.** `git switch -c je-1.56-tests` from `main` before you start —
  not `git checkout -b`, which `git-guard` blocks. Commit there, then
  `git push -u origin je-1.56-tests` and tell Dan the branch name. That is the whole
  hand-off; there is no pull request. He reviews the diff in Rider, against `main`.
- **You never touch `main`.** No commits to it, no merges into it, no rebases onto it.
  Merging is Dan's, always. If `main` moves while you are working and you need it, ask —
  do not merge it into your branch on your own initiative.
- **One branch per task means one task per branch.** If you find a second thing worth
  doing, write it down for Dan rather than adding it to this branch. A narrow diff is the
  point.
- Migrations are reviewed by Dan and Hobson before anything is applied. You do not apply
  them.

## Hand-off format

Follow `Skills/CodeReviewer/SKILL.md`'s output contract: the pass/fail summary lines from
checks, build and test; what each review pass flagged and how you resolved it ("nothing" is
an acceptable finding, silence is not); and anything you knowingly did that does not
conform, flagged for Dan's ruling.

Honest flags are cheap. Violations Dan finds himself are expensive.

---

**Do not start working.** Read this, read what it points you to, confirm the environment is
green, and tell Dan you are ready. Wait for him to say go.
