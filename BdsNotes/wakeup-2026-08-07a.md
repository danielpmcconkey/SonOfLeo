# Wakeup — 2026-08-07a

## Read this one, and nothing older

Supersedes `wakeup-2026-08-02b.md`.

## What happened across sessions 2026-08-02b through 2026-08-05

### REQ-JE-3.9.3 sort ordering tests (branch `req-je-3.9.3-sort-tests`, merged)

Wrote tests for all six `FetchSort` directions on `fetchFiltered`. Started with
six separate tests, Dan pushed back on false-positive risk — rewrote as three
mutual-reversal tests (fetch asc and desc, assert they're reverses with distinct
values present). Proves sort is causal, not coincidental.

### Audit gap tests (branch `audit-test-gaps-je49-je410`, merged)

Closed three audit findings from `Audit/2026-08-03a/10-truthfulness-tests.md`:
- **TT-JE410-CLOSED** — appending a reference is permitted when fiscal period is closed.
- **TT-JE49-STATE** — updating FI/value is permitted on voided entries and closed-period entries.
- Added fixture external references on the voided JE and closed-period JE (`voidedJeExtRefId`, `jeInClosedPeriodExtRefId`).

### SadPath helpers and global enforcement (branches `sadpath-pattern-enforcement` + `sadpath-route-test-conversion`, both merged)

Dan wrote `Tests/Tests.Helpers/SadPath.fs` with three functions:
- `isCorrectErrorString` — takes error case name as string (for Theory/InlineData tests).
- `isCorrectError` — takes DU case constructor (for Fact tests with payload cases).
- `isCorrectErrorEmpty` — takes bare DU case value (for no-payload cases).

BD converted all 37 `Result.isError` assertions and all 39 manual three-arm match
patterns across 17 files. Zero weak error assertions remain in the test suite.

Fixed a bug in `isCorrectError` where `Unchecked.defaultof<'A>` produced null tuples
that NRE'd on compound payloads. Replaced with safe default construction using
`Activator.CreateInstance` for value types and `FSharpValue.MakeTuple` for tuples.

### Dan's work (not BD)

- P3 (DAL Proving Ground) and P4 both completed by Dan/Hobson.
- Audit `2026-08-03a` run with full panel reports.
- `AccountBalanceReturn` and `JournalEntryLineReturn` now include `accountName`.
- `AccountActivityFilter.journalEntryId` changed from raw `Guid` to `JournalEntryHeaderId`.
- Trial balance report: new `TrialBalance` orchestrator, `Reports` CLI project,
  HTML report writer with visualization assets. Merged from `trial-balance-report`.
- Staging domain charter written (`HobsonsNotes/staging-domain-charter.md`).

## Current state

- Branch `main` @ **00b578d**, pushed. Clean working tree.
- 457 tests (288 integrated + 169 isolated), all passing.
- Branches `sadpath-route-test-conversion` and `req-je-3.9.3-sort-tests` can be deleted.

## What needs to happen next

Check with Dan. The audit action items and staging domain charter may have next
steps for BD. Dan has been driving feature work (trial balance) in parallel.

## Environment

- **The test database is shared.** BD's container and Dan's host both point at
  `Host=172.18.0.1 / sonofleo_test`, and `TestDataFixture` opens with
  `TRUNCATE ... CASCADE`. Announce before running; stop when Dan is verifying.
- `dotnet build --artifacts-path /tmp/sonofleo-build` — never the bare form.
- `fsharp-guard` blocks `Edit`/`Write` on `.fs` under `Src/`. `Tests/`, `Skills/`,
  `CompoundedLearnings/`, `BdsNotes/` are open. The hook does not cover `Bash`; the rule
  holds anyway.
- `git-guard` blocks `checkout`, `restore`, `clean`, `stash`, `reset --hard`,
  `switch --force`. Use `git switch -c`.
- Push needs `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`.
- Pre-commit hook runs `Checks/run-all.sh --quick`: 8 pass (including `check-traceability`),
  `check-apperror-coverage` skipped as slow.

## Before starting work

Read `BdsNotes/workflow.md` before doing anything else. Then check with Dan for direction.

**Take no action until Dan says go.**
