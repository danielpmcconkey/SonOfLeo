# Wakeup — 2026-08-21a

Supersedes `wakeup-2026-08-19a.md`. The two structural changes that wakeup announced still
hold — you own `Tests/` and are its only reviewer, and `README.md` at the repo root is the
first thing to read. Nothing in the way we work changed this session.

## What happened this session

`data-ingestion` merged to `main` before the session started. The work was remediating the
2026-08-19a requirements audit, in three rounds with Hobson turning specs around between them.

**Round 1 — ten test-efficacy findings** (`3841857`). Items 6, 10, 14, 15, 17, 19, 20, 23, 24,
36. Six tests that asserted nothing or asserted the wrong thing, thirteen Money sad paths on
untyped `IsError`, a `>= 9` count, a missing happy path, and a no-op test living two layers
above where it belonged. Resolutions are one line each in `Audit/2026-08-19a/action-items.md`.

**Round 2 — the last three** (`35916f6`). Items 11, 18, 6/23's fixture half.

**Round 3 — one re-cite** (`c1a0f8e`) after Hobson numbered the transition table.

### The two that were more than mechanical

**#11 — the transition table.** The old `REQ-STG-4.2` test counted the elements of
`validTransitions` and never looked at one, so five copies of the wrong transition would have
passed it. Replaced with all 72 `(from, to)` pairs — nine sources (eight statuses plus `None`)
against eight targets, 21 permitted and 51 denied — with the verdicts transcribed from the
spec's transition table, **not** from `validTransitions`. Taking them from the implementation
would have asserted that the code does what the code does. Spec and Src agreed on all 72.
Filed the old shape as Specimen 10.

**#6/#23 — the fixture was carrying the weight, not the assertion.** The trial balance sort
test asserted a flat alphabetical sort, which `REQ-RPT-1.6` explicitly calls wrong. Rewriting
it to derive depth-first order from the fixture hierarchy was the easy half. The hard half was
that *every* `F-` code in the fixture happened to sort into depth-first order anyway, so the
corrected test still could not tell a correct implementation from a flat one. `F-5305 "Sort
Discriminator"` now exists in `TestDataStage.fs` — parentless, coded to sit inside the `F-5000`
subtree alphabetically — and that separates the two orderings. Do not reparent or renumber it;
there is a comment above it saying so.

### Spec changes Hobson made in response

- **`REQ-JE-5.7`** — comment no-op rejection. Item 18's disposition said to re-cite that test to
  `REQ-SYS-6.1`, which is *waived* and had zero test citations. Flagging that rather than doing
  it is what produced 5.7.
- **`REQ-STG-4.6`** — the status transition table as a numbered requirement. It had been
  unnumbered prose under section 4, so the 72-pair test was citing `REQ-STG-4.2`, whose text
  covers only the Posted-is-terminal row. `4.2` keeps that one test; `4.6` has the other three.

### Mutation evidence

Forty-five assertions in round 1 across eleven rounds, plus three in round 2. Every one
perturbed, run, failure message read, perturbation reverted. Two worth remembering:

- Flipping `Reviewed → Posted` to denied and `Posted → Reviewed` to permitted failed exactly
  those two Theory cases and nothing else.
- Setting the trial balance test's expected order back to a flat sort **now fails it**. Before
  `F-5305` that same mutation passed. That is the whole argument for the fixture account.

## Known-weak — do not trust these

- **`REQ-STG-4.6`'s table has no initial-ingestion row.** It says "the following are the only
  permitted status transitions. Any transition not listed must be rejected," and the table
  starts at `Ingested → …`. But eight of the 72 pairs have `None` as their source and
  `None → Ingested` asserts **permitted**, because that is what the code does and what
  `REQ-STG-2.20` implies. On a strict reading of 4.6 the test asserts the opposite of the spec.
  The code is not wrong and neither is the test — the table is missing its first row. Flagged to
  Hobson; a `(none) → Ingested (initial ingestion)` line closes it. **Nothing is blocked on it.**
- **Item 17's test is one layer above where its function lives.**
  `JournalEntryLine.fetchByAccountId` is a `Src/Model` function, and the test sits in
  `Tests/Tests.Integrated/ModelOrchestrator/JournalEntryLineOrchestration.fs` because that is
  where Dan's disposition put it. `Tests/Tests.Integrated/Model/Ledger/` has no
  `JournalEntryLine.fs`, which is the pure home. Dan has not ruled. Do not move it unilaterally.
- **`REQ-STG-1.3` uses the instance form** and **`REQ-STG-1.4`/`1.16` history** — both ruled
  more than once. Leave them.
- **Mutation evidence still lives only in transcripts.** Everything committed on 2026-08-19 and
  2026-08-20 was mutation-tested; nothing older was, and you cannot tell which is which from the
  code. Dan has ruled on this: a repo-wide sweep is **a fool's errand**, and a recorded
  mutation-proof artifact is worse than nothing because it certifies a moment and then lies as
  the code moves. What survives is the per-assertion habit — perturb it once before you commit
  it. Do not reopen this.

## Current state

- Branch `main` @ `5fb67c9` plus this wakeup, tree clean, in sync with origin. No feature branch.
- **254 isolated + 376 integrated passing.** 0 failing, 0 stubs. The isolated count jumped from
  190 because the 72-pair Theory is 72 test cases.
- `bash Checks/run-all.sh` → 9 passed, 0 failed.
- Invariant 1 clean. **Invariant 2 is dirty and that is correct**: 46 `REQ-CR-*` requirements
  have no test and no waiver, because Hobson landed `Specs/Behavioral/ClassificationRuleCrud.md`
  minutes before this file was written. A spec lands before its tests exist — that is the whole
  reason step 12 is manual and `check-traceability` is out of the hook. Do not treat it as a
  regression, and do not "fix" it by waiving things.
- Eleven stale local branches, eight of them tracking remotes marked `gone`. Dan knows; he said
  git maintenance happens after the merge, and the merge has happened. `git-guard` blocks you
  from cleaning them up yourself.

## What needs to happen next

Nothing is assigned. Dan decides. But the shape of it is no longer a guess.

Between the last commit of this session and this file being written, Hobson pushed ten commits:
`Specs/Behavioral/ClassificationRuleCrud.md` (closing audit #31 and #33), a `fetchFiltered` for
stage entries and its UI routes (#25), a classification rule update route with account-code
validation, a fix for empty match rules matching greedily, and the removal of
`IngestionClassificationRuleToggleOpenNoOp` as a deliberate decision. The suite is green on top
of all of it; nothing of his broke anything of yours.

**That unblocks everything that was waiting on you**, and hands you a slice rather than an
errand:

| # | What | State |
|---|---|---|
| 34 | Re-cite the STG-5.3 test to the new ClassificationRule spec | **yours, unblocked** |
| 35 | Audit classification test coverage against the new spec | **yours, unblocked** |
| 32 | Tests for four classification rule CLI routes | **yours, unblocked** |
| 5 | CON-NGUI-1 — specs around the UI layer | Hobson |

And underneath those three items sits the real work: **46 `REQ-CR-*` requirements with no test
and no waiver.** That is a slice, and it starts at step 5 of the loop — read
`ClassificationRuleCrud.md` and draft names **from the spec alone**, before reading any of the
classification Src that Hobson just changed. Do not let the fact that the implementation already
exists tempt you into reading it first; that is precisely the ordering the README calls
load-bearing, and it is what found four wrong specs in the August slice.

Expect Hobson to hand over a business-level description of the slice (step 4). If he does not,
ask for one rather than reading the code.

## Environment

- **Dan and Hobson work in a different tree from you.** Everything moves through `origin`.
- **You cannot switch branches.** `git-guard` blocks `checkout`, and it matches on the command
  string, so `git stash list` gets refused too. If you need to be on a different branch, ask Dan
  to run `! git -C /workspace/SonOfLeo checkout <branch>` — the `!` prefix runs it in this
  container. `merge --ff-only` is allowed, so you can fast-forward yourself afterwards.
- **Never restore a file with `mv` from a backup.** `mv` preserves mtime, MSBuild sees a source
  older than the DLL, skips the rebuild, and you get a green-looking run of stale code. It cost
  a confusing five minutes this session. Write the file, or `touch` it after.
- **The test database is shared.** `Host=172.18.0.1 / sonofleo_test`; `TestDataFixture` opens
  with `TRUNCATE … CASCADE`. Announce before running; stop when Dan is verifying. The `claude`
  psql user has no CONNECT privilege — tests are your only window into it.
- `dotnet build --artifacts-path /tmp/sonofleo-build` — never the bare form.
- **The pre-commit hook no longer runs the traceability check.** Hobson took it out in `7988849`;
  `run-all.sh --quick` skips it now. Run
  `bash Skills/SonOfLeoRequirementsAudit/traceability-audit.sh .` directly when you want it.
  Dan does not need reminding that the old hook PASS was meaningless — he knows, he said so.
- `fsharp-guard` blocks `Edit`/`Write` on `.fs` under `Src/`. `Tests/`, `Skills/`,
  `CompoundedLearnings/`, `BdsNotes/` are open. Src fixes go to Dan with the exact diff.
  `Audit/` is Dan's, but he authorised status edits to `action-items.md` this session.
- Push needs `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`.
- **Rules of engagement, all confirmed present:** `README.md` at the repo root first, then
  `Tests/README.md` plus the README in whichever test directory you are in, the
  `SonOfLeo-TestWriter`, `SonOfLeo-TestNameReview` and `SonOfLeo-CodeReviewer` skills,
  `Skills/TestWriter/references/bullshit-test-specimens.md`, and the
  `CompoundedLearnings/catalogs/` entries whose triggers fire. There is no `BdsNotes/workflow.md`
  here — that is a LeoBloom thing.

## Working with Hobson

`BdsNotes/note-to-hobson-2026-08-20a.md` is the convention that worked: write him a file, tell
Dan the file exists, and stop making Dan the relay. He reads it and acts.

One calibration note in there worth keeping in mind in both directions: his hand-off listed six
things as news that were already in this clone. `git log --oneline <their-last-commit>..origin/main`
scopes a hand-off exactly, and it is as much your job as his.

## Before starting work

**Take no action until Dan says go.**
