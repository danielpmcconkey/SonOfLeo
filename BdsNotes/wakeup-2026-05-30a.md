# SonOfLeo Wakeup — 2026-05-30a

## Premise reminder (don't skip — the why matters more than the where)

SonOfLeo = Dan's hand-typed F# rewrite of LeoBloom, BD tutors, Dan codes. The
project has **two interlocked goals**:

- **Thread A** — test whether a *human-audited, regeneration-grade* BDD spec
  can stay clean enough that any future Claude session could rewrite the app
  from scratch using only the spec. (LeoBloom-elder proved BDDs accrete
  implementation-shape too — the rot just relocates from code to spec.)
- **Thread B** — Dan develops the F# reading muscle to audit agent-written
  F# elsewhere (LeoBloom-elder itself).

Charter, build order, and teaching protocol live in `BdsNotes/00`–`03`. Read
them if anything below is unclear; this wakeup assumes they're loaded.

## What happened this session

- Stood up the F# skeleton: solution + `src/SonOfLeo` console + `tests/SonOfLeo.Tests`
  (xUnit, F#, .NET 10). `dotnet test` green.
- Decided **xUnit, not TickSpec** — autopsied LeoBloom git history (commits
  `91102cb` add → `e80c560` delete, "refactor(tests): delete TickSpec
  infrastructure", Project 030 "Unfuck Test Harness"). Discipline doc +
  ambiguity linter both existed, both got bulldozed. **Do not propose TickSpec
  again** without a much sharper case than "it would be elegant."
- Hollow-test pattern established: vetted gherkin in comments, `[<Fact(Skip
  = "pending")>]` so they show as **skipped**, not falsely green. (Dan missed
  the `[<Fact>]` attribute first pass because his harness renders attributes
  dark-gray-on-black — memory `code-block-low-contrast.md` saved.)
- `Specs/` lives at **repo root**, deliberately outside `src/` and `tests/`,
  to keep it implementation-independent (thread A). Drop the `Structural/`
  category entirely — that was LeoBloom-elder's implementation-leak rot.
- Pass 1 vs Pass 2 discipline locked in:
  - **Pass 1 (vet spec pure):** strip implementation, reword to behavior. Output
    is regeneration-grade.
  - **Pass 2 (map, build, test):** decide enforcement site (test / type /
    constraint) and write F#.
  - **Never blend.** Blending = thrash + spec contamination.
  - **A behavior is NEVER deleted just because the type absorbs it.** (BD's
    earlier "cut FT-AC-011" was wrong, corrected. Behavior stays in spec;
    only enforcement relocates — gets an enforcement note.)
- Dan copied `AccountCrud.feature` into `Specs/Behavioral/` and we started
  vetting scenario-by-scenario.
- **Vetted AC-001:** killed `Background: ledger schema exists for account
  management` (it was test plumbing masquerading as a Given, not behavior).
  Rewrote CLI-free and table-name-free. Kept `is_active = true`. BD's clean
  version is in the transcript — Dan hasn't applied it to the `.feature` yet
  AFAIK; check the file before moving on.
- Walked the DDL for `ledger.account` and audited which columns merit BDDs.
  Verdict: most are already covered (or ride the fidelity round-trip). Real
  gaps: **max lengths** (code 10 / name 100 / subtype 25 / external_ref 50),
  **non-empty / required semantics** (`""` vs `NOT NULL`), **timestamp
  behaviors** (`created_at` immutable, `modified_at` bumps on update).
  Also flagged: schema treats `account_type` as an extensible lookup table;
  Dan is modeling it as a closed DU. Conscious-nod-worthy, not a bug.

## Current state

- **Repo:** `/workspace/SonOfLeo` — no commits yet, ever. Clean slate.
- **Untracked:** `.idea/`, `BdsNotes/`, `Specs/`, `SonOfLeo.slnx`, `src/`,
  `tests/`, plus `bin/`/`obj/`. `dotnet new gitignore` was suggested earlier
  but not run; do that before the first commit.
- **Build:** green. `dotnet test` reports 1 skipped (`FT-AC-001`).
- **Spec progress:** `Specs/Behavioral/AccountCrud.feature` present. AC-001
  vetting *discussed*; verify whether Dan applied the rewrite to the file or
  left it as the original LeoBloom copy. AC-002 through AC-013 unvetted.

## Open thread Dan parked on

**Which DDL-derived gaps does Dan actually want to spec?**

- (a) Max-length rejection (code/name at minimum)
- (b) Non-empty / required (reject `""` for code/name)
- (c) Timestamp semantics (`created_at` immutable; `modified_at` bumps on update)
- (d) Conscious nod on closed-DU-vs-extensible-lookup for `account_type`

The decision rule we agreed on: *"Would a from-scratch session, reading only
the BDDs, reproduce this behavior?"* If no, and it matters, spec it.

Resume by asking Dan which of (a)–(d) get scenarios, then continue vetting
AC-002 onward.

## What needs to happen next

1. Confirm whether AC-001's vetted rewrite landed in `Specs/Behavioral/AccountCrud.feature`
   or if it's still the LeoBloom-original wording.
2. Resolve the four DDL-gap questions above.
3. Continue Pass-1 vetting: **AC-002** next. (Reminder of BD's read: AC-002
   stays as a *behavior* in the spec — "create with an unknown account type is
   rejected" — with an enforcement note that the type system absorbs the
   create-service form and the real test lives at the parse edge.)
4. Eventually: `dotnet new gitignore`, add `.idea/`, first commit. Not urgent
   — Dan hasn't asked, and we have nothing yet that's worth a commit story.

## Standing protocol reminders

- Dan types every line of F#. BD writes notes/specs only. (Specs are not F#
  source — fair game for BD to draft if Dan asks.)
- `📖` flag for high-confidence claims worth Dan reading independently.
- Don't blend Pass 1 and Pass 2 vetting. Ever.
- One slice at a time, not one scenario at a time (scenarios in a slice share
  type design; one-at-a-time causes redesign churn).

## Environment

- .NET SDK 10.0.107 installed (also 8.0.126). Target = `net10.0`.
- IDE: JetBrains Rider. Dan is fine letting Rider abstract `.fsproj` compile
  order visually; will revisit when SonOfLeo has multiple `.fs` files or
  multiple `.fsproj`s.
- LeoBloom-elder at `/workspace/LeoBloom` is the reference codebase (BDDs
  source, F# implementation to audit-against — but agent-written, *not*
  gospel). Dev DB stays shared with LeoBloom-elder (per charter).

## Resurrection prompt

```
read BdsNotes/wakeup-2026-05-30a.md in the SonOfLeo repo and pick up where we
left off. We were mid-Pass-1 vetting of AccountCrud — AC-001 just got rewritten,
parked on which DDL-gap scenarios to add before moving to AC-002.
```
