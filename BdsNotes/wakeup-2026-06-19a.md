# Wakeup — 2026-06-19a

## What happened this session

Closed out both the Account and Money slices.

**Account slice (complete):**
- Wrote all 9 CLI route happy-path tests (Create, FetchById, FetchByCode,
  FetchByParentId, FetchByAccountType, FetchAll, Deactivate, UpdateName,
  UpdateExternalReference). Found and fixed multiple copy-paste bugs in test
  cleanup IDs (wrong account assigned to cleanup slot, orphaning records).
- Added domain-level tests: fetchAll (REQ-AC-3.7), fetchAll active-only
  (REQ-AC-3.9), AccountType DB ID mappings (REQ-AC-1.11-1.15).
- Struck REQ-AC-3.8 (passed-in reference time), replaced with REQ-AC-3.9
  (system run time).
- Waived REQ-AC-1.9, 1.16, 1.17 (type-system-enforced normal balance),
  REQ-AC-1.39, 2.16 (impossible self/circular parent via code path).
- Cross-referenced against LeoBloom's AccountCrud.feature gherkins. Only gap
  is FT-AC-009 (journal entry interaction), blocked on JournalEntry. SonOfLeo
  coverage is significantly broader than LeoBloom's 13 scenarios.
- Only remaining Account requirements are REQ-AC-4.4 and 4.6 (non-zero
  balance and journal entries after active end), both blocked on JournalEntry.

**Money micro-slice (complete):**
- Money type: private record with `amount: decimal`, smart constructor via
  `fromDecimal` with 2dp precision, min/max validation.
- Operations: `fromDecimalList`, `splitByN` (with remainder-to-first-share),
  `add`, `subtract`, `sumList`, `amount` (accessor/boundary function).
- 22 isolated tests covering happy paths, boundary validation, rounding mode
  (midway away from zero), remainder allocation, overflow/underflow.
- Decided against negative-amount splitByN tests — documented in Decisions.md
  with rationale (sign-symmetric arithmetic, no sign-dependent branching).
- Waived REQ-MON-1.1, 2.1, 2.1.1, 2.7, 2.7.1 as untestable (convention,
  absence-of-something, design guidance).
- No CLI routes, no integrated tests — Money is a pure domain primitive with
  no front door or persistence.

**Idiomatic F# refactoring pass:**
- Removed C#-style parens from function calls across the codebase.
- Replaced `let! _ =` with `do!` for unit Result unwrapping.
- Added `Zero()` to ResultCE so `if ... then do! Error` works without
  explicit `else Ok ()`.
- Simplified `fetchAll` (removed unnecessary result CE, `Some ""` -> `None`).
- Simplified `buildParamsList` (point-free style).
- Fixed bug in `confirmAccountIsValidAndActive` where `{id}` in error
  messages interpolated the accessor function instead of the account's ID.

## Current state

- Branch: `main` at `a941f08`. Clean working tree (wakeup file untracked).
- All tests passing (integrated + isolated suites, minus intentional Money
  stubs that were completed this session).
- Pushed to origin.

## What needs to happen next

1. **Journal slice.** This is the next major domain. Journal entries are the
   other half of double-entry bookkeeping. They unblock REQ-AC-4.4 and 4.6.
   Dan mentioned fiscal periods may be a dependency — check LeoBloom's
   FT-AC-009 gherkin (it references "a crud-test open fiscal period").
2. **Decide on fiscal period dependency** before starting Journal. Does
   SonOfLeo need fiscal periods to post journal entries, or was that a
   LeoBloom-specific design?

Read `BdsNotes/workflow.md` before starting work.

## Environment

- DB: `sonofleo_dev` / `sonofleo_test` / `sonofleo_prod` on 172.18.0.1:5432.
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`
- Test: `dotnet test --artifacts-path /tmp/sonofleo-build`
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
