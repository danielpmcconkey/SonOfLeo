# Wakeup — 2026-06-11b

## What happened this session

This was a docs-strategy session, almost no code. Dan laid out the five purposes of the
SonOfLeo documentation system (his words, roughly): (1) train Dan into a functioning F# team
lead before handing the project back to agentic development; (2) give him codebase knowledge
to call bullshit on the agent team; (3) "why" breadcrumbs on every non-obvious requirement
because memory fades; (4) a star chart — requirement IDs + code annotations as a cheap
navigation index for future agents at 200k-LOC scale; (5) drive testing where every
requirement is tested or explicitly waived, with Dan approving every test case. Pat (the
adversarial logic auditor from AtcStrategy POC4) returns eventually as pipeline auditor.

**Committed on main this session:**

- `b001a1c` — Cross-cutting requirements extracted to `Specs/Behavioral/SystemWide.md`
  (REQ-SYS-*); 17 AccountCrud requirements withdrawn with supersession pointers, no
  renumbering; REQ-AC-4.10–4.16 consolidated into new REQ-AC-4.22; SolutionConventions.md
  seeded with the create-vs-fromString naming rule (create = type wraps input,
  fromString = input names a DU case; don't unify).
- `59224b5` — Dan's trim fix in AccountType/AccountSubtype.fromString (REQ-AC-2.4, 2.10).
- `2908607` — Hobson's CLI requirements analysis (HobsonsNotes/).
- `2aa646a` — **Prefix rename FT- → REQ- repo-wide** (Dan did the replace himself; 359
  occurrences). BdsNotes deliberately untouched as archaeological record — old wakeups
  still say FT- on purpose.
- `8e797dc` — The documentation form factor, **PENDING DAN'S APPROVAL**: Specs/README.md
  (charter), Specs/Decisions.md (4 seed entries), Waived-from-testing tables in both
  Behavioral specs, expanded SolutionConventions.md, and Audit/audit.sh.

**Design decisions this session** (now in Specs/Decisions.md): deactivateAccount graduates
to the orchestration layer when its journal checks (REQ-AC-4.4/4.6) are implemented —
resolves the Account↔Journal circular-reference question; single-domain CRUD stays in
entity modules. The "// public orchestrators" section in Account.fs is acknowledged as
misnamed; refactor deferred until the journaling slice shows the bigger picture.

## Current state

- Branch: `main` at `8e797dc`. Commits b001a1c..2aa646a are pushed; `8e797dc` and this
  wakeup are local only.
- Builds clean, 0 warnings (`dotnet build --artifacts-path /tmp/sonofleo-build` — always).
- `Audit/audit.sh` runs and correctly **exits 1**: 22 annotation sites in Src/ and
  DbMigrations/ point at withdrawn requirement IDs (full list with file:line in the script
  output), plus `Tests/Tests.Ledger/AccountCrud.fs:6` uses bogus ID `REQ-AC-001`.

## What needs to happen next

1. **Dan ratifies or edits the pending items:** the three waiver reasons marked PENDING
   (REQ-AC-2.17, REQ-AC-4.22 in AccountCrud.md; REQ-SYS-4.1 in SystemWide.md), plus the
   README/Decisions wording generally.
2. **Annotation cleanup (code session):** fix the 22 phantom sites — re-point withdrawn IDs
   to their REQ-SYS successors per the Withdrawn tables; fix or delete the REQ-AC-001 test
   stub. Run audit.sh until invariant 1 is clean. This is Dan-types-BD-tutors work.
3. Then the previous wakeup's list still stands: tests (the mountain — 102/105 requirements
   untested; test cases need Dan's approval per the new process), CLI vertical slice,
   reconstitute id≠parentId gap, UtcNow/Now inconsistency.
4. Audit invariant 3 flagged: REQ-AC-1.5 (code case-sensitivity) has no code annotation
   anywhere — verify it's actually enforced or it's a real gap.

Standing notes: **Dan types, BD explains** — don't write code unless he asks for paste-able.
Specs never name code/files/tests (see Specs/README.md). BdsNotes is excluded from all
audits and find/replaces.

## Environment

- DB: `sonofleo_dev` on 172.18.0.1:5432 (user claude).
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`.
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
- Audit: `./Audit/audit.sh` from repo root (or anywhere; it cd's itself).
