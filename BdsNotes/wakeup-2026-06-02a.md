# SonOfLeo Wakeup — 2026-06-02a

> First read `BdsNotes/02-teaching-protocol.md` (rules of engagement), then
> `00-charter.md`. Take no action until Dan says go.

## What happened this session

- **UUID decision executed.** New `sonofledger` schema created in
  `leobloom_dev` DB with `account.id` as UUID PK (and `parent_id` as UUID
  FK). Migration script: `DbMigrations/2026-06-01-07-48-CreateAccountTable.sql`.
  Ran manually in dev.
- **DB collation verified:** `en_US.UTF-8` — case-sensitive by default.
  No explicit collation override needed for account code uniqueness.
- **Directory rename:** `src/` → `Src/`, `tests/` → `Tests/` (F# casing
  convention).
- **Requirement annotations (@FT-AC tags)** added to both `Ledger.fs` and
  the migration SQL, tracing code/schema to spec IDs. Convention: annotate
  enforcement points, not just field definitions.
- **AccountCrud.feature fully numbered** (1.1.1–1.1.41). These are
  valid/invalid data state requirements, not CRUD behavior specs (those
  still need to be written as section 1.2+).
- **Dan and BD debated** whether doubled subtype constraints (both
  directions) are redundant. Dan's position: they're different constraints
  with different enforcement points. BD conceded — both are load-bearing.
- **AccountSubtype.validFor started but WIP.** Has three bugs:
  - Return type `Result<AccountType>` is invalid — should be plain
    `AccountType` (no failure case)
  - `OperatingExpense | OperatingExpense` — duplicate, second should be
    `OtherExpense`
  - Only covers subtype→type direction (1.1.28/30/33/35), not
    type→valid-subtypes direction (1.1.29/31/32/34/36). Needs a
    complementary `isValidCombination` function, and Equity (no subtypes)
    isn't handled.

## Current state

- **Branch:** `main` @ `2c9193c`
- **Build:** Likely broken — `validFor` has the wrong return type.
- **sonofledger schema** exists in `leobloom_dev` with `account_type`
  (seeded, 5 rows) and `account` (empty, UUID PK).

## What needs to happen next

1. Fix `validFor` — return type, duplicate case, rename to something
   clearer if needed.
2. Write the validation function that covers both directions of the
   subtype-per-type matrix (1.1.28–1.1.36), including Equity's "no
   subtypes" rule.
3. Decide where the validation gets called — Account smart constructor
   (Option A, Dan's lean) or Ledger service (Option B).
4. Implement 1.1.39 (self-parent check) — pure GUID comparison, Model
   layer.
5. 1.1.38 (inactive parent check) is Ledger-layer — deferred until
   service work begins.
6. Write the actual CRUD behavior specs (section 1.2+).
7. Wire `Tests.Model -> Model` and write tripwire tests.

## Environment

- .NET SDK 10.0.107, target `net10.0`. IDE: JetBrains Rider.
- Dev DB: `leobloom_dev`, schema `sonofledger` (UUID PKs). Collation
  `en_US.UTF-8` (case-sensitive).
- Reference codebase: `/workspace/LeoBloom` — agent-written, not gospel.

## Resurrection prompt

```
read BdsNotes/wakeup-2026-06-02a.md in the SonOfLeo repo. AccountSubtype
.validFor is WIP with three known bugs. Subtype-per-type validation matrix
is the current work. I type the F#, you tutor.
```
