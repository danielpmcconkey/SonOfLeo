# SonOfLeo Wakeup — 2026-06-03a

> First read `BdsNotes/02-teaching-protocol.md` (rules of engagement), then
> `00-charter.md`. Take no action until Dan says go.

## What happened this session

- **Fixed all three `validFor` bugs** from previous session (return type,
  duplicate case arm, missing direction). Dan fixed the first two himself
  before this session started.
- **Added `validWith`** (type→subtypes direction) and
  **`validTypeSubtypeCombination`** (the actual validator using `validWith`
  + `List.contains`). Dan and BD discussed whether both `validFor` and
  `validWith` need to fire at the same enforcement point — answer is no,
  because the mapping is one-to-one. Both functions exist for different
  callers, not for double-checking.
- **Account record made private.** `private { ... }` pattern — external
  code can hold an `Account` but can't construct or field-access directly.
- **`Account.create` smart constructor** — takes validated types, runs
  cross-field validation (type/subtype combo), generates `id`
  (`Guid.NewGuid()`), `createdAt`/`modifiedAt` (`DateTimeOffset.UtcNow`),
  defaults `isActive` to `true` when `None`.
- **`Account.createFromPrimitives` factory** — takes raw strings/ints,
  validates each field via its `create`/`fromString` function, uses
  `result { }` CE to chain validations, delegates to `Account.create`.
- **Utilities class library created** with `ResultCE.fs` — a
  `ResultBuilder` computation expression (Bind/Return/ReturnFrom) so
  `createFromPrimitives` uses `let!` instead of nested `Result.bind`.
- **`AccountType.fromString` added** — parse boundary for string→DU.
- **Renamed** `toDbString`/`fromDbString` to `toString`/`fromString` on
  `AccountSubtype`.
- **Fixed stale project reference** in `Tests.Ledger.fsproj` (`src` → `Src`).
- **Curried function signatures** — `create` and `createFromPrimitives`
  switched from tupled to curried style for readability and partial
  application potential.

## Concepts Dan is working through

- **`Result<'T, 'TError>`** — understood as "gimme the thing or tell me
  why not." Bind/map as "if still on happy path, keep going."
- **Computation expressions** — Dan can use `let!` and `return!` correctly
  but doesn't yet grok the compiler desugaring. Said he expects this to
  click through repetition. Don't re-explain the machinery; let usage
  build the intuition.
- **Curried vs tupled** — understands the syntax difference, saw partial
  application explained but hasn't used it in anger yet.
- **`Result.map Some` for option wrapping** — used it correctly for
  optional field validation. Understands the three-outcome pattern
  (None→Ok None, Some+valid→Ok Some, Some+invalid→Error).

## Current state

- **Branch:** `main` @ `19f6d7e`
- **Build:** Clean, zero warnings.
- **sonofledger schema** in `leobloom_dev` — `account_type` (seeded, 5
  rows), `account` (empty, UUID PK).

## What needs to happen next

1. Write tripwire tests (`Tests.Model` → `Model` reference, basic
   create happy/sad paths).
2. Implement 1.1.39 (self-parent check) — only relevant for updates,
   not creates (create generates its own GUID).
3. 1.1.38 (inactive parent check) — Ledger-layer, deferred until
   service work begins.
4. Write actual CRUD behavior specs (section 1.2+).
5. Eventually: accessor functions on `Account` module so external code
   can read fields from the private record.

## Research flagged (📖)

- `List.contains` — FSharp.Core
- `Result.bind` / `Result.map` / Railway Oriented Programming — Wlaschin
- Computation expressions — Wlaschin's fsharpforfunandprofit.com series
- FsToolkit.ErrorHandling — library alternative to hand-rolled ResultCE
- Private record constructors — Wlaschin's *Domain Modeling Made
  Functional*, Chapter 3

## Environment

- .NET SDK 10.0.107, target `net10.0`. IDE: JetBrains Rider.
- Dev DB: `leobloom_dev`, schema `sonofledger` (UUID PKs). Collation
  `en_US.UTF-8` (case-sensitive).
- Reference codebase: `/workspace/LeoBloom` — agent-written, not gospel.

## Resurrection prompt

```
read BdsNotes/wakeup-2026-06-03a.md in the SonOfLeo repo. Dan is
learning F# by hand-writing a LeoBloom rewrite. BD tutors, Dan types.
Next up: tripwire tests or CRUD behavior specs.
```
