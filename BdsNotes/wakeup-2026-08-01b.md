# Wakeup — 2026-08-01b

## Read this one, and nothing older

Supersedes `wakeup-2026-08-01a.md`. Read `BdsNotes/05-ledger-hardening-strategy.md` for
the campaign context if needed — but the immediate task is clear without it.

## What happened this session

Full error trace crawl and gap remediation. Three parallel agents traced all 21 route
handlers and produced `BdsNotes/07-error-trace-by-route.md`. Dan augmented it into
`BdsNotes/07b-error-trace-dan-augmentation.md` with comment/gap-in-code/gap-in-testing
columns. Then:

1. Dan created 10 new AppError cases to close code gaps (wrong error codes, backstop
   leaks, missing validations). Branch `fixing-incomplete-error-codes`, merged to main.
2. BD fixed 8 tests whose assertions expected the old less-specific errors. Branch
   `fix-tests-for-new-error-codes`, merged to main.
3. BD audited every row against the test suite and filled in the gap-in-testing column.
4. Dan simplified the FetchActivity theory test — replaced the elif chain with
   `FSharpValue.GetUnionFields` reflection (currently using `GetType().ToString()` but
   may switch to the reflection approach next session).

## Current state

- `main` @ **1af2387**, pushed. No open branches. Clean working tree.
- 172 tests, all passing.
- `07b-error-trace-dan-augmentation.md` is the master sheet. Three gap-in-testing values:
  - blank = route-level tested (37 rows)
  - "lower" = tested at component/model/orch only (85 rows)
  - "yes" = no test at any level (15 rows)

## What needs to happen next

**Fill in test gaps.** The 07b sheet is the work list. Priorities:

1. **"yes" rows first** — 15 cases with zero tests anywhere. Some are "nearly impossible"
   DAL backstops (Dan may waive). The real ones:
   - `AccountParentAndChildAreSame` (Create)
   - `AccountDeactivationWithJournalEntriesDatedAfterDeactivationDate` (Deactivate)
   - `AccountDeactivationFailedJournalEntryValidation` (Deactivate)
   - `FiscalPeriodNoPeriodMatchingKey` (FetchByKey, Close, Reopen — new code path)
   - `FiscalPeriodNoPeriodMatchingId` (FetchByKey)
   - `JournalEntryFetchByDateRangeBeginAfterEnd` (brand new validation)
   - `JournalEntryVoidingCannotFetchFiscalPeriod` (Void)
   - `JournalEntryCommentSecondaryJeHeaderIdNotFound` (Void, AddComment)
   - `JournalEntryReferenceUpdateNoOp` (UpdateExternalReference)

2. **"lower" rows** — 85 cases tested at a lower level but not through the route. Dan
   will decide how many of these need route-level tests vs being adequately covered by
   existing component/orch tests.

**Wait for Dan's direction on scope and approach before writing any tests.**

## Environment

- **The test database is shared.** BD's container and Dan's host both point at
  `Host=172.18.0.1 / sonofleo_test`, and `TestDataFixture` opens with
  `TRUNCATE ... CASCADE`. Announce before running; stop when Dan is verifying.
- `dotnet build --artifacts-path /tmp/sonofleo-build` — never the bare form.
- `fsharp-guard` blocks `Edit`/`Write` on `.fs` under `Src/`. `Tests/`, `Skills/`,
  `CompoundedLearnings/`, `BdsNotes/` are open. The hook does not cover `Bash`; the rule
  holds anyway.
- `git-guard` blocks `checkout`, `restore`, `clean`, `stash`, `reset --hard`,
  `switch --force`. Use `git switch -c`.
- Push needs `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"`.
- Pre-commit hook runs `Checks/run-all.sh --quick`: 7 pass, `check-apperror-coverage`
  skipped as slow.

## Before starting work

Read `07b-error-trace-dan-augmentation.md` — it's the work list. Then `Specs/README.md`,
`Tests/README.md`, and the README of whichever test directory the work lands in. Then the
relevant `CompoundedLearnings/catalogs/` — only follow an article when its "read when"
trigger actually fires.

**Take no action until Dan says go.**
