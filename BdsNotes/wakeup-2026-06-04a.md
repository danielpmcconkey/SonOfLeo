# SonOfLeo Wakeup — 2026-06-04a

> First read `BdsNotes/02-teaching-protocol.md` (rules of engagement), then
> `00-charter.md`. Take no action until Dan says go.

## What happened this session

- **Continued from 2026-06-03a.** Dan picked up where he left off — no
  new bugs, build was clean.
- **Restructured Model project.** `Ledger.fs` god-module → `Ledger/Account.fs`
  under `namespace Model.Ledger`. Added solution folders for future
  sub-domains: `Ops/`, `Portfolio/`, `Reporting/`. These are known
  sub-domains from LeoBloom, not speculative — Dan pushed back on YAGNI
  call and was right.
- **Renumbered all spec tags.** Dropped redundant `1.` prefix:
  `@FT-AC-1.1.x` → `@FT-AC-1.x` across Account.fs, AccountCrud.feature,
  and migration SQL.
- **Wrote section 2 Create behavior specs** (2.1–2.14). Covers:
  string trimming (2.1), UTF-8 (2.2), case fidelity (2.3), account type
  string validation (2.4), isActive default (2.5), parent ID existence
  check (2.6), parent isActive match (2.7), duplicate ID rejection (2.8),
  duplicate code rejection (2.9), subtype string validation (2.10),
  timestamp generation (2.11–2.12), UUID generation (2.13), and happy
  path with return value (2.14).
- **Design discussion:** Dan asked whether every section 1 data state
  rule needs a section 2 counterpart. Answer: no — section 1 defines
  legality, section 2 only adds behaviors that are genuinely new in the
  context of creation (trimming, generation, persistence). Restating
  constraints with "upon creation" prepended is redundant noise.

## Concepts Dan is solidifying

- **Result/bind/map** — clicked enough to use fluently. Still building
  intuition on computation expression desugaring; don't re-explain the
  machinery, let repetition do the work.
- **Curried vs tupled** — understands syntax, hasn't used partial
  application in anger yet.
- **F# project structure** — understands file ordering within a project,
  namespace hierarchy, companion modules. Comfortable with Rider for
  project/folder creation but needs BD for fsproj edits (Rider hides
  them).

## Current state

- **Branch:** `main` @ `788a1c8`
- **Build:** Clean, zero warnings.
- **sonofledger schema** in `leobloom_dev` — `account_type` (seeded, 5
  rows), `account` (empty, UUID PK).
- **AccountCrud.feature:** Section 1 (data states, 1.1–1.41) and
  Section 2 (create behaviors, 2.1–2.14) complete. Sections 3+ (Read,
  Update, Deactivate) not yet written.

## What needs to happen next

1. Write section 3+ behavior specs (Read, Update, Deactivate).
2. Implement trimming in `AccountCode.create`, `AccountName.create`,
   etc. (2.1 — not yet in code).
3. Wire up service layer for DB-dependent validations (2.6, 2.7, 2.8,
   2.9, 2.14).
4. Tripwire tests for Account model layer.
5. Accessor functions on `Account` module (record is private, external
   code can't read fields yet).

## Research flagged (📖) — cumulative

- `List.contains` — FSharp.Core
- `Result.bind` / `Result.map` / Railway Oriented Programming — Wlaschin
- Computation expressions — Wlaschin's fsharpforfunandprofit.com series
- FsToolkit.ErrorHandling — library alternative to hand-rolled ResultCE
- Private record constructors — Wlaschin's *Domain Modeling Made
  Functional*, Chapter 3
- F# pipe operator and function composition — fsharpforfunandprofit.com

## Environment

- .NET SDK 10.0.107, target `net10.0`. IDE: JetBrains Rider.
- Dev DB: `leobloom_dev`, schema `sonofledger` (UUID PKs). Collation
  `en_US.UTF-8` (case-sensitive).
- Reference codebase: `/workspace/LeoBloom` — agent-written, not gospel.

## Resurrection prompt

```
read BdsNotes/wakeup-2026-06-04a.md in the SonOfLeo repo. Dan is
learning F# by hand-writing a LeoBloom rewrite. BD tutors, Dan types.
Next up: R/U/D behavior specs or implementing trimming + service layer.
```
