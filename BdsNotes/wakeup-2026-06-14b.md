# Wakeup -- 2026-06-14b

## What happened this session

**Validated environment isolation:**
- Ran the REQ-AC-1.4 uniqueness integration test with cleanup disabled to confirm
  rows land in the test DB. Ran the CLI debug binary to confirm it writes to dev DB.
  Dan confirmed: test row in test DB, CLI row in dev DB. Environment isolation (built
  by Dan and Hobson in a prior session) is working correctly.
- Re-enabled cleanup, re-ran test, Dan confirmed test DB was empty afterward.

**DAL requirements audit:**
- Walked through all 13 REQ-DAL-1.x requirements one at a time with Dan.
- Withdrew REQ-DAL-1.1, 1.2, 1.4 through 1.13 (all referenced eliminated
  LEOBLOOM_ENV / LEOBLOOM_DB_PASSWORD pipeline).
- Kept REQ-DAL-1.3 (config file inaccessible -> error) -- still valid, behavior unchanged.
- Dan wrote 6 new requirements (REQ-DAL-1.14 through 1.19) covering the new
  ConnectionStringEnvVar pipeline. Annotated enforcement points in DAL.fs.
- Fixed stale error messages in DAL.fs that still referenced `ConnectionStrings:SonOfLeo`.
- Withdrew REQ-DAL-3.2.2 (config files no longer hold connection strings).
- Sections 2 and 3 otherwise unchanged and valid.

**Test project cleanup:**
- Removed Tests.Model and Tests.Ledger projects (empty shells after prior migration).
- Deleted from solution file and filesystem.

**Test implementation (Dan typing, BD reviewing):**
- Migrated all unit tests to Tests.Isolated, integration tests to Tests.Integrated.
- Dan implemented new integration tests:
  - REQ-AC-1.5: account code case sensitivity (two accounts with same code, different case)
  - REQ-AC-2.14 / REQ-SYS-5.1: create account, fetch by ID, assert all fields match
  - REQ-AC-3.4: fetch by code returns correct account
  - REQ-AC-3.5: fetch by parent ID returns all children (parent + 3 children setup)
- Fixed microsecond precision bug: tests were using `Instant.FromDateTimeOffset(DateTimeOffset.Now)`
  instead of `Clock.now()`, causing round-trip assertions to fail on sub-second differences.
- Dan implemented many new isolated tests for AccountComponent, Account, and Money types.

**Cleanup helper refactoring:**
- Changed `cleanUpAccountId` to accept `Guid option` (handles None internally).
- Changed `cleanUpAccountList` to accept `Guid option list`.
- Added `cleanUpParentIdAndChildren` for FK-safe ordered cleanup (children first, then parent).
- All test finally blocks simplified as a result.

**F# learning moments:**
- `function` keyword as shorthand for `fun x -> match x with`
- `List.choose id` to flatten `option list` to `list`
- Result CE swallowing errors silently if the CE return value isn't inspected
  (xUnit sees no assertion failure, reports pass). Fixed by matching on CE result.

## Current state

- Branch: `main` at `8419b37`.
- Clean working tree (no uncommitted changes).
- Hobson's wakeup file (`HobsonsNotes/wakeup-2026-06-14a.md`) is untracked.
- All tests that are implemented pass. Many stubs remain (`Assert.Fail "not implemented"`).

## What needs to happen next

1. **Continue implementing test stubs.** Remaining stubs in Tests.Integrated:
   - REQ-AC-3.6: fetch by account type
   - REQ-AC-1.4 / REQ-AC-2.9: duplicate account code rejected (overlaps with existing 1.4 test?)
   - REQ-AC-2.6: parent ID must reference existing account
   - REQ-AC-2.7: parent must be active at AuditEnvelope instant
   - REQ-AC-2.19: child AccountType must match parent
   - Deactivation tests (4.1, 4.2, 4.3, 4.5)
   - Update tests (4.8, 4.9, SYS-3.3, 4.19)
2. **Items from prior sessions still open:**
   - Audit workflow skill (in `Skills/SonOfLeoRequirementsAudit/`)
   - DEC-1: Convention "must" vs requirement "must"
   - MR-4: Debug-mode production-DB prohibition recommendation
   - Approval rubric for Decisions.md
3. **DB ownership:** Future tables created by postgres (root) will need `REASSIGN OWNED BY`
   after migrations. Not urgent until a migration pipeline exists.

## Environment

- DB: `sonofleo_dev` (dev), `sonofleo_test` (test), `sonofleo_prod` (prod) on 172.18.0.1:5432.
  Each environment uses its own role. Connection strings are in environment variables
  named by `ConnectionStringEnvVar` in each environment's appsettings.
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`
- Test: `dotnet test --artifacts-path /tmp/sonofleo-build`
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
