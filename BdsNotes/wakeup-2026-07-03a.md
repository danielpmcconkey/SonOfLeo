# Wakeup 2026-07-03a

## What happened this session

Executed the full 07-01b punch list, then rode the momentum through the EntryDate bug,
REQ traceability, and a merge to main.

- **Punch list items 1–9 landed** (`ff30083`): `cleanUpJournalEntryId`/`List` helpers,
  transaction+rollback on all committing update/append/round-trip tests, consumable
  void victims + shared-ref fixture pair, fetchByReference tests rewritten against
  fixture data, creation tests clean up in `finally`, TestWriter skill + references
  amended (orchestrator-committing exception, consumable victims, reserved-empty
  +4-month period).
- **Discovered a punch-list miss:** CLI `JournalEntryRoutes.fs` committed mutations —
  its UpdateExternalReference test was destroying TestBank/TXN-001 (same disease as
  the 07-01a cascade, one layer up). Fixed with consumable CLI victims; later swept
  the whole file: PostNew/AddExtRef/AddComment clean up in `finally`, Void route
  consumes a fixture victim, UpdateComment uses a victim comment (`35d871e`).
- **Dan fixed the EntryDate bug:** `EntryDate.create` was re-running REQ-JE-2.7
  (period-is-open) on every read, making closed-period JEs unfetchable everywhere.
  Check moved to `JournalEntryHeader.constructNewAndSaveToDb`; voiding validates
  REQ-JE-4.5 inline on the railway. New REQ-JE-3.2 closed-period fetch test guards
  it; REQ-JE-5.5 comment test flipped to assert spec behavior. Item 11 closed.
- **Item 10:** pruned integrated empty/whitespace rejection tests duplicated by
  Tests.Isolated; one wiring rejection test kept per function (`5418028`).
- **REQ-JE-4.3 nonexistent-id voiding test** added — guards the railway (Dan's first
  fix draft had `Result.defaultWith failwith` mid-railroad; nothing caught it).
- **Section 1 traceability pass:** REQ-JE-1.21/1.22/1.40 tagged onto existing tests;
  ten type-system/schema waivers added to the spec table (Dan approved via commit).
- **REQ-JE-2.2/2.3/2.4 rewritten lean by Dan**; CLI PostNew tagged with 2.3. The
  case-sensitivity clause debate ended when Dan produced the migration receipts
  (deterministic en_US.UTF-8 collation, REQ-DAL-3.5) — and his real COA codes are
  all digits anyway.
- **Merged `core-je-model` → `main`** (fast-forward to `248545d`, pushed). Created
  `core-je-fetching` for the next slice.

## Current state

- Branch: `core-je-fetching` @ `248545d`, clean, pushed with tracking. `main` is at
  the same SHA. `core-je-model` still exists local+remote (Dan's call to delete).
- Suites: 130/130 integrated, 177/177 isolated, all green.
- **Two-state rule holds** for the whole JE spec except REQ-JE-3.6/3.7/3.8/3.9 —
  deliberately deferred: Dan is writing that Src (Hobson added 3.6–3.8 as new REQs
  and replaced withdrawn 3.4 with 3.9). Do NOT test, stub, or flag them until his
  code lands.

## What needs to happen next

1. Dan writes Src for REQ-JE-3.6 (account debit/credit totals + net balance),
   3.7 (fetch by date range), 3.8 (fetch by FI alone), 3.9 (enriched lines by
   account — CLI route exists at `Src/SonOfLeoCli/JournalEntryRoutes.fs:170` but
   `JournalEntryLine.fetchByAccountId` still carries the withdrawn REQ-JE-3.4 tag;
   Dan retags when he builds the slice).
2. BD tests the 3.6–3.9 slice after Dan's code lands (TestWriter two-phase: stub
   sheet first).

## Environment

- **Build/test recipe (container):** `MSBUILDDISABLENODEREUSE=1 dotnet build
  --artifacts-path /tmp/sonofleo-build`, then `dotnet test --no-build ...`. Without
  the env var, MSBuild daemon nodes linger ~15 min and the harness Bash call hangs
  on the process tree long after tests finish. Suite wall-clock is ~90s in the
  container vs ~32s in Dan's Rider — container overhead, not a bug.
- Test DB: `sonofleo_test`, connection via `SONOFLEO_TEST_CONNSTR` env var (own
  user; the `claude` PG user cannot connect). If the fixture dirties the DB after a
  crashed run, truncate the six ledger tables (same statement as the fixture's
  Dispose) using creds parsed from that env var.
- Hooks: git-guard allows plain `git switch`/`merge`/`push`; blocks checkout,
  restore, reset --hard, clean, stash. fsharp-guard blocks Src/*.fs edits.

## Standing notes

- Read `BdsNotes/00-charter.md` and `BdsNotes/02-teaching-protocol.md` before
  starting work if not already familiar this session.
- Src is Dan's. Tests are BD's. Invoke `SonOfLeo:TestWriter` before touching tests.
- **Answer, then stop.** "Check my work" means deliver a verdict, not run suites and
  edit tests. Got burned on this hard this session; memory written.
- Take no action until Dan says go.
