# Wakeup — 2026-06-16a

## What happened this session

Continued from wakeup-2026-06-15a. Massive test implementation session — Dan
typed everything, BD reviewed.

**Integrated tests (Tests.Integrated) — complete:**
- REQ-AC-4.5: deactivateAccount rejects already deactivated
- REQ-AC-4.8: updateAccountName (valid + invalid via REQ-SYS-2.1)
- REQ-AC-4.9: updateExternalReference (set value + clear to None)
- REQ-SYS-3.3: update sets modifiedAt from AuditEnvelope
- REQ-AC-4.19: update to deactivated account is permitted
- All 21 integrated tests passing.

**Isolated tests (Tests.Isolated/AccountComponent) — complete:**
- AccountType: 5 valid + 1 reject + 1 trim = 7 tests
- AccountSubtype: 9 valid + 1 reject + 1 trim = 11 tests
- Type-subtype matrix: 45 tests (5 types x 9 subtypes, spreadsheet-generated)
- Null subtype (REQ-AC-1.19.1): 5 tests (all types accept None)
- AccountExternalReference: empty, whitespace, over-50, exactly-50, trim = 5 tests
- AccountActivityPeriod: 4 boundary tests (earlier, equal, null end, valid)

**Isolated tests (Tests.Isolated/Account) — complete:**
- constructNew: UUID generation, timestamp from envelope, invalid code,
  invalid type-subtype combo, invalid activeEnd (earlier + equal), invalid subtype
- reconstitute: skipped with comment — no separate logic, delegates to domain types
- isActive: 6 boundary tests (before begin, at begin, between, at end, after end, no end)

**Code changes:**
- Flipped `AccountActivityPeriod.isActive` and `Account.isActive` to data-last
  parameter order for pipe-friendly usage.
- Fixed REQ IDs on spreadsheet-generated subtype tests (were all REQ-AC-1.28/1.29,
  corrected to per-type REQ IDs).
- Normalized whitespace across AccountComponent.fs test file.
- Added `createdAt`/`modifiedAt` accessors to Account module.
- Added REQ-AC-1.19.1 to AccountCrud.md.

**F# learning moments:**
- `Result.defaultWith failwith` for unwrapping Results in test setup
- Piping: `|> Result.isError |> Assert.True` instead of match expressions
- `Option.map` to unwrap value objects through Options
- Can't pipe into .NET static methods directly, but can chain F# functions
- Data-last parameter order enables piping; data-first blocks it
- `fun` keyword — anonymous lambdas don't need parens as the last pipe argument
- Floating expressions inside `result { }` CE cause FS0708 if they return non-unit

**CLI design conversation started:**
- Hobson's usage audit reviewed (HobsonsNotes/cli-requirements-from-leobloom-usage.md)
- LeoBloom used pure CLI args (`--code X --name Y --type Z`)
- Explored alternatives: args, interactive prompts, file-based, stdin JSON
- stdin JSON is the leading candidate — structured data in, structured data out,
  no shell escaping, round-trip symmetry with `--json` output
- Concerns noted: discoverability, error reporting (both manageable)
- **Decision not yet made.** Dan wants to design the high-level CLI direction
  before any implementation calories are spent.

## Current state

- Branch: `main` at `8133cdf`. Clean working tree (wakeup file untracked).
- Test count: all passing except Money stubs (intentionally deferred to journal slice).
- Account CRUD slice: model, DAL, and tests complete. CLI is the remaining piece.

## What needs to happen next

1. **Design the CLI input strategy.** stdin JSON vs args vs hybrid. High-level
   direction first — Dan wants this conversation before any implementation.
   Key question: how does structured data get into an F# CLI executable?
2. **Items from prior sessions still open:**
   - Audit workflow skill (in `Skills/SonOfLeoRequirementsAudit/`)
   - DEC-1: Convention "must" vs requirement "must"
   - MR-4: Debug-mode production-DB prohibition recommendation
   - Approval rubric for Decisions.md
   - DB ownership: future tables need `REASSIGN OWNED BY` after migrations

Read `BdsNotes/wakeup-2026-06-16a.md` before starting work.

## Environment

- DB: `sonofleo_dev` / `sonofleo_test` / `sonofleo_prod` on 172.18.0.1:5432.
  Each env uses its own role. Connection strings in env vars named by
  `ConnectionStringEnvVar` in appsettings.
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`
- Test: `dotnet test --artifacts-path /tmp/sonofleo-build`
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
