# Wakeup — 2026-06-14a

## What happened this session

**Test implementation (Dan typing, BD reviewing):**
- Dan implemented 6 AccountCode tests in a new file `Tests/Tests.Model/Ledger/AccountComponent.fs`.
  All 6 pass. Covers: empty/whitespace rejection, length boundary (exactly 10, over 10),
  whitespace trimming, post-trim length check.
- Dan wrote a uniqueness test (`REQ-AC-1.4`) in `Tests/Tests.Model/Ledger/Account.fs` using
  `Account.constructNewAndSaveToDb`. Uses `try/finally` with mutable ID capture for DB cleanup.
  Test compiles but fails at runtime — missing `appsettings.Development.json` in Tests.Model
  output directory (the DB-hitting test needs a connection string config).
- Cleanup helper in `Tests/Tests.Model/Ledger/_Cleanup.fs` — `CleanUpAccountId` deletes
  by GUID, returns `Result<unit, string>`, `failwith` on error in the `finally` block.

**Test project restructure:**
- Dan and BD agreed that the Tests.Model / Tests.Ledger split was organized by business domain,
  but the *actual* dividing line is execution characteristics: does the test need infrastructure
  or not?
- Created two new projects: **Tests.Isolated** (no external dependencies — DB, network, filesystem)
  and **Tests.Integrated** (requires live database + appsettings). READMEs in each state the rule.
- Old projects (Tests.Model, Tests.Ledger) left in place. Dan will migrate test files manually.
- Tests.Integrated has its own `appsettings.Development.json` with DB connection string.
  Both projects added to `SonOfLeo.slnx`. Solution builds clean.

**Environment isolation FMEA:**
- Hobson wrote an environment isolation plan (`HobsonsNotes/environment-isolation-plan-2026-06-14.md`).
  Build config as environment selector, env var names in appsettings (not secrets), pg_hba lockdown.
- BD reviewed and wrote a 9-finding FMEA (`BdsNotes/environment-isolation-fmea-2026-06-14.md`).
  Key risks: tests silently hitting dev DB when `.runsettings` is forgotten (HIGH/HIGH),
  `environment.d` might not work on Cinnamon/Mint, password duplication across `.bashrc` and
  `environment.d`, `claude` user bypassing pg_hba lockdown.
- Structural concern: build-config-as-selector works for CLI (Debug=Dev, Release=Prod) but
  breaks for tests (Debug builds needing a third "Test" environment via side-channel override).

**After BD went to sleep:** Dan and Hobson were about to implement the environment isolation
plan (with or without addressing the FMEA findings). Expect significant changes to DAL.fs,
appsettings files, test project structure, and possibly compose.yml and pg_hba.conf.

## Current state

- Branch: `main` at `37de274`. Significant uncommitted changes (see below).
- Uncommitted modified: DbMigrations (4 files), SonOfLeo.slnx, Specs/Behavioral/AccountCrud.md,
  Src/Model/Ledger/Account.fs, Tests.Model (fsproj + old stubs + new Ledger/ files).
- Uncommitted new: Tests.Isolated/, Tests.Integrated/, BdsNotes (wakeups + FMEA),
  HobsonsNotes/environment-isolation-plan.
- **WARNING:** Dan and Hobson were about to make sweeping changes after this session.
  The state you find may look very different from the above. Read the git log. Don't assume.

## What needs to happen next

1. **Assess what Dan and Hobson changed.** Read git log, check branch state, see if the
   environment isolation plan was implemented (fully or partially). Read the FMEA to
   understand what risks BD flagged.
2. **Test migration.** Dan was going to move test files into Tests.Isolated and Tests.Integrated.
   Check if that happened, whether the old projects are still around, and if the solution builds.
3. **Remaining test stubs.** 65 stubs were scaffolded last session. Dan filled in 7 this session
   (6 AccountCode + 1 uniqueness). ~58 remain across AccountComponent, Account, and Money tests.
4. **Items from prior session still open:**
   - Audit workflow → Skill (in `Skills/SonOfLeoRequirementsAudit/`)
   - DEC-1: Convention "must" vs requirement "must"
   - MR-4: Debug-mode production-DB prohibition recommendation
   - Approval rubric for Decisions.md

Read `BdsNotes/wakeup-2026-06-14a.md` before starting work.

## Environment

- DB: `sonofleo_dev` on 172.18.0.1:5432 (user claude — but this may have changed if Hobson's
  plan was implemented. Check for new roles: leobloom_dev, leobloom_test, leobloom_hobson).
- Build: `dotnet build --artifacts-path /tmp/sonofleo-build`.
- Test: `dotnet test --artifacts-path /tmp/sonofleo-build`.
- Push: `export GIT_SSH_COMMAND="ssh -i /home/sandbox/.claude/.ssh/id_ed25519"` first.
