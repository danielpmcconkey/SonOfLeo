# Wakeup — 2026-08-19a

## Who you are

Hobson, Dan's host-side Claude Code, the butler. CLAUDE.md at
`~/penthouse-pete/CLAUDE.md`, memory at
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`.

## Lanes

| Actor | Owns | Never touches |
|---|---|---|
| **Dan** | `Src/` F#. Assigns all work not covered below. | — |
| **BD** | `Tests/` — own branches, own clone | `Src/`, `Specs/` |
| **Hobson** | `Specs/` (REQs) | `Src/`, `Tests/`; never `dotnet test`; never BD's clone |

Everything else is Dan's to assign per task. See `feedback_sonofleo_lanes.md`.

## Where we are

**`data-ingestion` merged to `main`.** Traceability audit passes clean —
every active REQ is tested, waived, or unenforceable. No phantoms. The
ingestion pipeline (ingest → dedup → classify → manual review → shadow
post → batch post) is code-complete and tested.

~7,100 lines added across 113 files. About half the size of the
journaling domain.

## What happened since wakeup-2026-08-18a

### Spec rewrites (Hobson)

- **REQ-STG-4.4** rewritten. Postable = status only (`Classified` or
  `Reviewed`). Dropped the "all lines coded" filter — silent exclusion
  hides upstream bugs. If invariants break, posting fails loudly at
  account_code resolution (REQ-STG-9.4).

- **REQ-STG-9.3** rewritten. JE `source` is a fixed provenance label
  ("Data ingestion import") describing how the entry was created, not
  which FI it came from. The FI identity lives on the external reference
  (REQ-STG-9.5). Decision from 2026-08-14, code matched since `6abd131`,
  spec now matches too.

- **REQ-STG-9.4** rewritten. Null account_code at posting = broken
  upstream invariant. Invalid non-null codes can't happen (FK constraint).
  Dropped the phantom "does not resolve" failure mode.

- **REQ-STG-1.1, 1.2** waived (definitional, not testable).

- **REQ-STG-1.4** waived (required string, visible by inspection).
  **REQ-STG-1.16** added for the file-uniqueness clause (unenforceable —
  file-scoped, no persistent state). Originally split as 1.4a/1.4b but
  letter suffixes broke the traceability script's regex, so renumbered.

- **Design note — naming** added to top of DataIngestion.md. Spec
  identifiers name domain concepts; they don't prescribe code naming.

### Audit workflow (Hobson)

- Replaced the single `truthfulness:tests` auditor with a **per-spec
  test-efficacy fan-out**. Each agent gets the full behavioral spec, the
  bullshit-test specimens doc, and Tests/README, and checks four criteria:
  1. Behavioral coverage (smell test: would garbage pass?)
  2. Assertion quality (six specimen patterns)
  3. Negative coverage (typed error assertions for rejection REQs)
  4. Uncited behavior (code/test behavior with no REQ)

### Code changes (Dan)

- `isThereAHeaderUpdate` / `isThereALineUpdate` in
  `StageEntryOrchestration.fs` were returning inverted booleans (double
  negation accidentally worked). Rewritten to `<>` / `||` so names match
  return values.

- `updateStageEntry` now skips `StageEntryHeader.updateDb` when every
  header field is NoChange. Fixes the bug BD found where a line-only
  update errored on the header no-op.

### BD's work

- Collapsed route validation tests into Theory with InlineData rows.
- Fixed two `/tmp` infra bugs (appsettings.json copy, ReportRoutes
  scratch dir).
- Covered REQ-STG-1.3, 4.4, 9.3 with real tests.
- Added test-name grader skill (`Skills/TestNameReview/`).
- Attempted to rename spec identifiers to F# field names — reverted by
  Hobson (contradicts the naming design note).

### Infrastructure

- BD's Claude Code updated from 2.1.170 to 2.1.234.
- `AskUserQuestion` denied globally in both host and container
  settings.json. No more dialog modals.
- `install-hooks.sh` comment fixed — host and container are separate
  clones, not shared.
- Dockerfile pinned version changed from `@2.1.170` to `@latest`.

## What's next

1. **Bespoke parsers.** Outside SonOfLeo repo. Convert LeoBloomOps
   Python importers to produce the base staging JSONL format. Not
   started.

2. **Saturday cutover.** Can Hobson run the full Saturday routine
   through SonOfLeo without touching LeoBloom's CLI for any ledger or
   obligation operation?

3. **Next domain slice.** Per the migration roadmap
   (`project_sonofleo_roadmap.md`): obligations, invoicing, reporting —
   whatever Dan picks.

## Key resources

| What | Where |
|---|---|
| **STG spec** | `Specs/Behavioral/DataIngestion.md` |
| **Audit workflow** | `Skills/SonOfLeoRequirementsAudit/requirements-audit.workflow.js` |
| **Bullshit-test specimens** | `Skills/TestWriter/references/bullshit-test-specimens.md` |
| **Migration roadmap** | Memory: `project_sonofleo_roadmap.md` |
| **Prior wakeup** | `HobsonsNotes/wakeup-2026-08-18a.md` |

## Standing corrections

- **Hobson owns REQs only.** Everything else is Dan's to assign.
- **Never run `dotnet test`.** Dan's/BD's lane.
- **Never work in BD's clone.** Yours is `codeprojects`; his is `ai-sandbox`.
- **Never edit Src/ files.** Dan writes all F# code. Review only.
- **Dispositions outrank re-derivation.** If your fresh reading contradicts
  a disposition, the disposition wins — raise the conflict with Dan.
- **Dan is PO — make the case once, then defer.**
- **Account codes, not IDs in staging.** Code-to-ID resolution at post time.
- **fi_reference is always required.** Parsers derive a deterministic
  reference for sources without native IDs.
- **Classification authority hierarchy:** parser > classifier > operator.
- **Manual review = operator authority.** Status is caller-set, not
  auto-inferred.
- **Same-file duplicate fi_reference is a parser defect.** Caught at
  ingestion validation, not dedup.
- **JE source is provenance, not FI identity.** Fixed label like "Data
  ingestion import". The FI name goes on the external reference.
- **Pull before editing, pull before committing.** Dan and BD push
  concurrently. Use `--rebase`.

## Outstanding items

- **COA renumbering** — deferred to LeoBloom session.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
