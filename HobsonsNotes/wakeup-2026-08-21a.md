# Wakeup — 2026-08-21a

## Who you are

Hobson, Dan's host-side Claude Code, the butler. CLAUDE.md at
`~/penthouse-pete/CLAUDE.md`, memory at
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`.

## Lanes

| Actor | Owns | Never touches |
|---|---|---|
| **Dan** | `Src/` F#. Assigns all work not covered below. | — |
| **BD** | `Tests/` — own branches, own clone | `Src/`, `Specs/` |
| **Hobson** | `Specs/` (REQs) | `Src/`, `Tests/`; never `dotnet test`; never BD's clone |

Everything else is Dan's to assign per task. See `feedback_sonofleo_lanes.md`.

## Where we are

**2026-08-19a audit is essentially complete.** 40 action items
dispositioned — 33 done, 7 overruled. Five items remain open:

| # | What | Owner | Status |
|---|------|-------|--------|
| 5 | CON-NGUI-1: discuss UI spec structure | Hobson | needs Dan |
| 12 | STG-MISLABEL-1: rename misattributed test | Dan/BD | accepted, blocked on #33 |
| 25 | CUST-FETCH-1: fetchFiltered route on StageEntry | Dan | in progress |
| 31/33 | ClassificationRule spec | Hobson | pending |
| 32/34/35 | Classification rule tests + coverage audit | Dan/BD | blocked on #31/33 |

Dan is actively building the StageEntry fetchFiltered (#25). He has the
query working, the `StageEntryFetchFilter` record type, dynamic where
clauses, and the orchestrator function. DevDataStage now has dev-only
fixture data (8 staged entries, one per status) for query development.

## What happened since wakeup-2026-08-19a

### Audit (Hobson + BD)

- Ran the full 29-auditor audit in 6 batches. Fixed a syntax error in
  the workflow script (`noFindingsReasoning` brace placement).
- Dan reviewed all 40 findings across two rounds.
- Hobson spec fixes: status casing (#2), JE-4.2 post-posting alterations
  (#4), Postable definition rewrite (#13), NGUI-1.3.1 split (#22),
  Definitions.md non-entity notation (#38), REQ-STG-4.6 transition table
  ID, REQ-JE-5.7 comment no-op.
- Hobson audit skill fixes: GAAP scope expanded (#39), statement-delta
  prompt tightened (#40), 4 entries added to resolved-findings.md.
- BD closed 13 test items in two rounds. Notably: 72-pair status
  transition Theory (#11), fixture account F-5305 for sort
  discrimination (#6/#23), moved external ref no-op test to orchestrator
  (#36). See `BdsNotes/note-to-hobson-2026-08-20a.md`.
- Traceability check marked SLOW in pre-commit hook — specs and tests
  land in separate commits, so the invariant transiently fails. Still
  runs on full `Checks/run-all.sh`.

### Infrastructure (Dan)

- Created `Src/Utilities/ConfigManager.fs` — centralized appsettings
  reader with typed cache. Replaced the DAL's inline
  `ConfigurationBuilder` usage. Timezone moved from hardcoded
  `"America/New_York"` to `appsettings.json` `LocalizedTimeZone` key.
- F# idiom fixes: `List.forall` in classification doesMatch functions
  (#27/#28).
- DevDataStage refactored: calls `TestDataFixture()` for shared data,
  then `DevFixtures.stageDevData` for dev-only staged entries (8 entries,
  one per status).

### New REQs

- **REQ-STG-4.6** — the status transition table, previously unnumbered
  prose. BD's 72-pair test now cites this.
- **REQ-JE-5.7** — comment-update no-op rejection. Fills the gap BD
  identified where every other no-op had a per-entity REQ except
  comment update.
- **REQ-NGUI-1.3.2** — stack-trace inclusion on system exceptions.
  Split from 1.3.1, waived as untestable.

## What's next

1. **Dan finishes #25** — fetchFiltered route on StageEntry. Query and
   orchestrator exist; needs the InterfaceBridge route and CLI wiring.

2. **Hobson writes the ClassificationRule spec (#31/#33)** — this
   unblocks #12, #32, #34, #35. Read the code in
   `Src/Model/DataIngestion/Classification/` and the existing fixture
   data in `Tests/Tests.Helpers/TestDataStage.fs` (lines 660-738) to
   understand what the rules engine actually does.

3. **#5 — UI spec discussion** — CON-NGUI-1 needs deeper reasoning
   about how the NGUI spec should be structured. Deferred until Dan
   wants to talk about it.

4. **Bespoke parsers** — outside SonOfLeo repo. Convert LeoBloomOps
   Python importers to produce the base staging JSONL format. Not
   started.

5. **Saturday cutover** — can Hobson run the full Saturday routine
   through SonOfLeo without touching LeoBloom's CLI for any ledger or
   obligation operation?

## Key resources

| What | Where |
|---|---|
| **Audit action items** | `Audit/2026-08-19a/action-items.md` |
| **Audit reports** | `Audit/2026-08-19a/10-*.md` |
| **Resolved findings** | `Skills/SonOfLeoRequirementsAudit/resolved-findings.md` |
| **BD's note** | `BdsNotes/note-to-hobson-2026-08-20a.md` |
| **STG spec** | `Specs/Behavioral/DataIngestion.md` |
| **Classification code** | `Src/Model/DataIngestion/Classification/` |
| **DevDataStage fixtures** | `DevDataStage/DevFixtures.fs` |
| **Migration roadmap** | Memory: `project_sonofleo_roadmap.md` |
| **Prior wakeup** | `HobsonsNotes/wakeup-2026-08-19a.md` |

## Standing corrections

- **Hobson owns REQs only.** Everything else is Dan's to assign.
- **Never run `dotnet test`.** Dan's/BD's lane.
- **Never work in BD's clone.** Yours is `codeprojects`; his is `ai-sandbox`.
- **Never edit Src/ files.** Dan writes all F# code. Review only.
- **Dispositions outrank re-derivation.** If your fresh reading contradicts
  a disposition, the disposition wins — raise the conflict with Dan.
- **Dan is PO — make the case once, then defer.**
- **Account codes, not IDs in staging.** Code-to-ID resolution at post time.
- **fi_reference is always required.** Parsers derive a deterministic
  reference for sources without native IDs.
- **Classification authority hierarchy:** parser > classifier > operator.
- **Manual review = operator authority.** Status is caller-set, not
  auto-inferred.
- **Same-file duplicate fi_reference is a parser defect.** Caught at
  ingestion validation, not dedup.
- **JE source is provenance, not FI identity.** Fixed label like "Data
  ingestion import". The FI name goes on the external reference.
- **Pull before editing, pull before committing.** Dan and BD push
  concurrently. Use `--rebase`.
- **Check `git log` against BD's last commit before writing hand-off
  summaries.** Don't repeat things already in his clone. (Calibration
  note from BD's 2026-08-20a message.)
- **Traceability hook is SLOW.** Skipped in pre-commit. Specs and tests
  land in separate commits from different actors. Run full
  `Checks/run-all.sh` manually when you need invariant 2.

## Outstanding items

- **COA renumbering** — deferred to LeoBloom session.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
