# SonOfLeo Roadmap — 2026-08-02

Product roadmap for the post-refactor cleanup, running through the audit re-run (FT-9).
Written by Hobson for a future session with zero conversational context. Read this
alongside its source of authority:

> **`Audit/2026-07-06a/action-items-reclassification-2026-08-02.md`**
> — the reclassification of every 2026-07-06a audit action item against the July refactor,
> including Dan's dispositions of all open questions (2026-08-02). Item numbers below
> (#17b, #61, #113a…) refer to that document and to `action-items.md` beside it.
> **The dispositions outrank anything you re-derive.** If your fresh reading of the repo
> contradicts a disposition, the disposition wins — raise the conflict with Dan, don't
> silently re-litigate.

## Lanes (hard boundaries — do not cross)

| Actor | Owns | Never touches |
|---|---|---|
| **Dan** | All `Src/` F#, by hand, through Rider. Controls `main`; runs the test suites himself. Assigns all work not covered below. | — |
| **BD** (sandbox Claude, `/media/dan/fdrive/ai-sandbox/workspace/SonOfLeo/`) | `Tests/` — on his own branches in his own clone | `Src/`, `Specs/` (he proposes in prose) |
| **Hobson** (host Claude, `/media/dan/fdrive/codeprojects/SonOfLeo/`) | `Specs/` (REQs) | `Src/`, `Tests/` code; never runs `dotnet test`; never works in BD's clone |

Everything outside these three lanes (`Checks/`, `Skills/`, `CompoundedLearnings/`,
scripts, prose, review) is Dan's to assign per task — not Hobson's by default.

Dan is PO. Make the case once, then defer. Design/judgment questions land in P7 sessions,
not in unilateral edits.

## Sequencing

```
P1 ──────► P4 ──► P8 (FT-9 re-run)
  ╲          ▲      ▲
   ► P2 ─────┤      │
   ► P3 ─────┘      │
   ► P6 (parallel)  │
P5 ─────────────────┘   (any time before P8)
P7 interleaves at Dan's pleasure
```

P1 goes first: it defines the three-state vocabulary P2/P3 waivers use, and P4's gate
cannot exist until the spec ledger is coherent. P2, P3, P6 then run in parallel — three
different lanes, no file collisions. P4 flips on only when the Invariant 2 backlog is
actually clear. P8 is the capstone and the proof.

---

## P1 — Spec Ledger Integrity  *(Hobson executes; Dan approves each waiver/wording)* — M

One clean pass over `Specs/` so the requirement ledger tells the truth. All items are
spec-file or tooling edits; no code.

1. **Three-state migration** (#29/#41 disposition):
   - `Specs/README.md`: replace the two-state rule with tested / waived / **unenforceable**.
     Definitions per the disposition: *waived* = enforced (by code, type, schema, or
     review) but deliberately untested; *unenforceable* = nothing enforces it, it binds
     humans not code.
   - Add an `## Unenforceable` table (ID, why, Dan's approval date) to each of the 7
     `Specs/Behavioral/*.md`. Empty tables are fine — they signal "not forgotten."
   - Move REQ-MON-1.1 (USD-only) from Money.md's waived table to unenforceable — the
     canonical specimen.
   - Sweep every existing waived row with the test *"does anything actually enforce
     this?"* No → move it. The negative-existence family (AC-4.22, AC-5.1, JE-4.1,
     JE-6.1/6.2, MON-2.1, MON-2.7…) **stays waived** — code review enforces those.
   - Also fix the "Two-state rule" boilerplate sentence heading each waived table.
2. **Amend REQ-JE-4.9** (#17b disposition — July ruling reversed): add *"The FI and value
   may be updated regardless of whether the entry is voided or its fiscal period is
   closed (mirrors REQ-JE-4.10 and REQ-JE-5.5)."* Principle of record: **void seals the
   money, never the metadata.**
3. **REQ-JE-2.14 waiver row** (#74 disposition): enforced structurally — no creation
   input contract exposes `voidedAt` (`JournalContracts.fs`: it appears only on
   `JournalEntryHeaderReturn`). Note in the reason that Model-layer *reconstitution*
   legitimately carries `voidedAt`; reconstitution is not creation.
4. **Consolidate REQ-AC-1.19 / 1.19.1** (#38) — verbatim duplicates at
   `AccountCrud.md:25-26`. IDs are permanent: withdraw one with a reason, don't delete.
5. **Fix REQ-JE-3.4's note** (#60) — it claims "we have functioning tests"; no test cites
   it. The capability is covered under JE-3.9. Either add a waiver row saying so, or
   correct the note and let P2/P3 decide if a citing test is wanted.
6. **Amend REQ-DAL-2.3** (#69 — overruled as originally proposed): the "user input"
   scoping is deliberate, not a gap — non-parameterized values (e.g. `limit: int option`)
   are type-safe by construction. Update the requirement's language to explain *when*
   interpolation is acceptable: when the value's type makes injection structurally
   impossible.
7. **Two `resolved-findings.md` entries** (both in
   `Skills/SonOfLeoRequirementsAudit/resolved-findings.md`):
   - *Deferred:* "FP closing is a posting lock only; GAAP closing entries are a planned,
     unscheduled enhancement. Do not flag closing as incomplete. Revisit when: Dan
     schedules the closing-entries slice." (#96a)
   - *Overruled:* "No repo-level agent entry point (CLAUDE.md or equivalent) — by
     design; agents enter via wakeups/prompts. Vetoed repeatedly. Do not re-flag." (#112a)
8. **`traceability-audit.sh` learns the third table**: the section-boundary awk (~line
   29) and `section_ids` calls hardcode "Withdrawn" / "Waived from testing" — add
   "Unenforceable"; Invariant 2 becomes `active − (tested ∪ waived ∪ unenforceable)`;
   add the consistency check *unenforceable-but-tested is a contradiction*.
9. **Housekeeping in `action-items.md`**: flip stale statuses (#116a/#117a/#118a and the
   §2 met items read CONFIRMED), delete the duplicate "Phase 4 — GAAP Panel" section
   (appears twice), add a note that #127a (Fantomas) was done then deliberately reversed
   on 2026-07-31.

**Exit:** specs internally consistent; every active REQ classifiable into exactly one of
three states on paper (the test backlog itself is P2/P3's job).

---

## P2 — Behavioral Coverage Debt  *(BD writes; Dan reviews)* — M

The §1 behavioral-coverage list. **Explicitly NOT part of the 07b error-trace sheet** —
Dan ruled (question 8) that 07b is a bounded input-contract-validation exercise and these
are behavioral gaps. Separate work list, own branch.

- REQ-AC-1.40 (parent must exist) and REQ-AC-3.3 (fetch-by-ID) — no test cites either.
- REQ-JE-3.6.2 — as-of balance. Only `asOf = None` is exercised today
  (`AccountRoutes.fs` FetchBalances happy path). Needs a `Some` date proving temporal
  exclusion.
- REQ-JE-3.6.1 — signed balance in normal-balance orientation (#101a).
- Activity filter permutations (#100a): amount and description filters have zero positive
  tests; `Tests.Integrated/ModelOrchestrator/AccountActivity.fs` has only 3 tests. Per
  `Tests/README.md`: every filter parameter tested independently.
- Rename the test at `AccountRoutes.fs` ("Deactivate rejects when JEs dated after
  deactivation date", currently cited REQ-AC-4.1) to cite **REQ-AC-4.6** — the behavior
  is tested, the linkage is wrong (#2).
- Resolve the `// todo: ask claude to provide the correct REQ #` on the FetchBalances
  happy path (`AccountRoutes.fs:~311`) — Hobson determines the right ID, BD renames.

**Exit:** the named REQ IDs all appear in green, content-bearing tests.

---

## P3 — DAL Proving Ground  *(BD writes; Hobson preps the standard; Dan approves waivers)* — L

Dan's #61 ruling verbatim: no wholesale waiver, no carve-out — **DAL requirements get
direct tests**. Hand-craft bad SQL and bad inputs, send them at the DAL functions,
assert the exact typed `AppError` case.

- **Hobson first:** add the DAL rung *below* `Tests.Integrated.Model` in
  `Tests/README.md`'s layer hierarchy, plus a per-directory README for
  `Tests/Tests.Integrated/DataAccessLayer/`. Failure vectors proven here come OFF the
  upper layers' books (test-once-at-the-lowest-layer rule).
- **BD then:** the suite. Vector families: malformed SQL → DAL error case; row-count
  expectation violations → `DalResultantRowsDidntMatchExpectation`; the
  `DalErrorDuring*Unboxing` family; env-var cases (`DalConnectionStringEnvVarNotFound`,
  `DalConnectionStringEnvVarContainsConnectionString`); transaction-bracket
  rollback-on-throw. `TestingError` (fenced out of Src by `check-testingerror.sh`) exists
  for exactly this harness work.
- Walk all ~30 active REQ-DAL IDs. Anything genuinely unprovokable gets an individual
  waiver or unenforceable row — one at a time, Dan approves.

**Exit:** `traceability-audit.sh` Invariant 2 reports zero uncovered REQ-DAL IDs; most
`Dal*` gaps in `check-apperror-coverage.sh` close as a side effect (#125a).

---

## P4 — The Commit Gate  *(Hobson)* — S — **strictly after P1–P3**

#113a disposition. Do not start until the Invariant 2 backlog is actually clear — the
repo fails it wholesale today, and a gate that's red on day one teaches everyone to
bypass it.

1. Promote Invariant 2 in `traceability-audit.sh` from report to failure (today only
   Invariant 1 phantoms exit non-zero).
2. Wire it into `Checks/run-all.sh` — it's grep-speed, fine for `--quick` and the
   pre-commit hook.
3. Write the placeholder policy **once**, in `Specs/README.md`'s requirement-anatomy
   section: a new REQ ships in the same commit as a citing test; a placeholder
   `Assert.Fail "Not yet implemented"` satisfies the gate. Two deliberate properties:
   the gate vets *existence only* (content is review's and the audit's job — Dan named
   this limitation and accepted it), and a placeholder is *loud* — red in every run
   until the real test lands, which is the feature.

**Exit:** committing a new REQ without a citing test fails the pre-commit hook.

---

## P5 — Audit Machine v2  *(Hobson)* — M — any time before P8

Rebuild `Skills/SonOfLeoRequirementsAudit/requirements-audit.workflow.js` per the FT
rulings (all from Dan's post-audit review, still undone):

- **FT-1:** one auditor at a time, findings discussed with Dan before the next fires. No
  parallel fan-out (`parallel()` at ~line 540 today), no aggregate summary.
- **FT-3:** remove severity rankings from the finding schema (~line 45) and reports —
  severity is Dan's call; low labels made him skip things he shouldn't.
- **FT-4 wiring gap:** the eleven prompt-fix items already exist as articles in
  `CompoundedLearnings/catalogs/audit-conduct.md`, but the workflow never tells auditors
  to read the catalog — it wires in `resolved-findings.md` only. Add the instruction to
  every auditor prompt.
- **FT-2:** verify each phase's output is written to the run folder as it completes, not
  after the fact.
- **FT-8:** per-run finding IDs and a clean disposition record per run (the schema's
  `priorRuling` field and `resolved-findings.md` are the start; finish the shape).
- **#107a residue:** confirm the two unattested backstops (test appsettings point only at
  dev/test; release-config gate), then record all four backstops with dates in
  `CompoundedLearnings/articles/architecture/debug-release-access.md`. Dan attested the
  other two on 2026-08-02: no prod password in the container, network block on prod.
  Deliberately NOT suppressed in resolved-findings — auditors *should* re-verify
  isolation posture against this written baseline.

**Exit:** the skill is ready to run FT-9 the way Dan wants to experience it.

---

## P6 — Src Odds and Ends  *(Dan, at leisure)* — S

Independent small items, all in Dan's lane; listed so they aren't lost:

- **#119a** — `JournalEntryOrchestration.fetchHeadersFromFilter`: pass
  `AnyQuantityIsAcceptable` to the DAL, dedup, then enforce the caller's `expectedRows`
  against the deduped count. Deletes the warning comment at ~line 312-316 ("dedup happens
  *after* DAL checks the exactly-one condition").
- **#122a** — `JournalEntryComment.fs:~159` todo: decide whether
  `fetchByJournalEntryHeaderIdList` should match the secondary entry ID too (and whether
  `composeFromFetchedLists` follows).
- **#123a** — the confirmX rename sweep: 8 sites on `check-confirm-naming.sh`'s
  allowlist. When done, **Hobson empties the allowlist** (the script header says so).
- **#124a** — LookupCache module doc comment: one CLI invocation = one cache lifetime,
  no invalidation by design, loud `failwith` on init load is deliberate; a future
  long-lived host drops the cache rather than building invalidation. This is the paper
  trail for Dan's 2026-08-02 #91 blessing.

---

## P7 — Design Sessions  *(Dan + Hobson conversations; outputs spawn spec → Src → test work)* — ongoing

Each is a discussion first. Hobson brings the LeoBloom operational context; Dan decides;
Hobson specs; then the normal lanes take over.

- **Rate × Money** (#18): how projections (loan balance in N months) will work against
  REQ-MON-2.7's multiplication prohibition. The Rate definition wording finalizes after.
- **Counter-account** (#102a): does account activity need the counter-account, or does
  JE fetch-by-date-range already cover the workflow? Revisit with fresh LeoBloom eyes.
- **Reclass verb** (#103a): scope then spec. LeoBloom evidence: reclass = void + repost,
  weekly use; `ledger reclass` was a top wished-for command
  (`HobsonsNotes/cli-requirements-from-leobloom-usage.md`).
- **Next-month period auto-create** (#115): mini-feature. REQ first (Hobson), then Src
  (Dan), then tests (BD).

**Parked, do not raise:** period close / GAAP closing entries (#96a/#105/#106) — planned
enhancement, design session happens when Dan schedules that slice. The resolved-findings
entry from P1 keeps auditors quiet about it.

---

## P8 — FT-9: The Audit Re-run — capstone

After P1–P5 minimum (P6/P7 need not block). Fresh statement of position from Dan (the
workflow requires it verbatim), run under the rebuilt P5 machine. This is the audit that
proves the refactor: the two-state rule made true, the DAL covered, the error taxonomy
tested, the auditors carrying the conduct catalog and the prior rulings.

---

*Committed alongside the reclassification doc. When any project completes, update the
reclassification doc's tables rather than this file — this file is the map, that one is
the ledger.*
