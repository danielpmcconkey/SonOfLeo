# Wakeup — 2026-08-03a

## Who you are

You are **Hobson** — Dan's host-side Claude Code, the butler. Personality and standing
orders: `~/penthouse-pete/CLAUDE.md`. Cross-project memory:
`~/.claude/projects/-home-dan-penthouse-pete/memory/` — `project_sonofleo.md` is current.

## Lanes

| Actor | Owns | Never touches |
|---|---|---|
| **Dan** | `Src/` F#. Assigns all work not covered below. | — |
| **BD** | `Tests/` — own branches, own clone | `Src/`, `Specs/` |
| **Hobson** | `Specs/` (REQs) | `Src/`, `Tests/`; never `dotnet test`; never BD's clone |

Everything else (`Checks/`, `Skills/`, `CompoundedLearnings/`, scripts, prose, review) is
Dan's to assign per task. See `feedback_sonofleo_lanes.md`.

## Where we are

`main` at `b607ff2`, pushed. 454 tests pass. Invariant 1: clean. Invariant 2: clean.

**The roadmap is nearly complete.** P1–P7 are done. Only P8 (FT-9 audit re-run) remains.

## What happened this session

Massive session — P4, P5, P6, and P7 all completed.

### P4 — The Commit Gate

- Invariant 2 promoted from report to `exit 1` in `traceability-audit.sh`
- `Checks/check-traceability.sh` created — wired into `run-all.sh`, runs in `--quick`
- Placeholder-test policy written in `Specs/README.md`
- Dan tested it live (modified AccountCrud.md, got denied, reverted)

### P5 — Audit Machine v2

- Workflow script rewritten per all FT rulings:
  - **FT-1:** Sequential auditors (no parallel fan-out), no synthesis agent
  - **FT-2:** Progressive file writes in batches of 5
  - **FT-3:** Severity removed from finding schema
  - **FT-4:** Audit-conduct catalog wired into every auditor prompt
  - **FT-8:** Per-run finding IDs, `99-disposition.md` template per run
- Stale references fixed: `Specs/Conventions/` gone, `Specs/Decisions.md` gone,
  annotation system retired, Src/ area partitioning updated
- **#107a:** Four debug/release backstops recorded in
  `CompoundedLearnings/articles/architecture/debug-release-access.md` with attestation dates
- **Run folder moved:** `Skills/.../Runs/2026-07-06a/` → `Audit/2026-07-06a/`
- Skill README updated

### P6 — Src Odds and Ends

All four items resolved (Dan did the code, Hobson did the check script):
- **#119a:** Done last session
- **#122a:** Dan traced the call chain — `fetchByJournalEntryHeaderIdList` correctly
  checks only primary (secondary comments wouldn't attach to anything in the result set).
  Todo comment left as documentation of the decision.
- **#123a:** Dan renamed all 8 `validateX` → `confirmX`. Hobson emptied the allowlist.
- **#124a:** Dan wrote the module doc comment on LookupCache.

### P7 — Design Sessions

All four items resolved through conversation:
- **#18 (Rate x Money):** Closed. `Model.Money` doesn't need multiplication. Future
  projection type (double-backed) handles that. Rate def updated (dropped "or Quantity"),
  Money def note added distinguishing the concept from the type.
- **#102a (Counter-account):** Overruled. Hobson reviewed with fresh LeoBloom context —
  the assumed workflow (walking cash activity to check categorization) doesn't exist.
  Categorization review happens through staging tables, spending reports, and JE lookups.
- **#103a (Reclass verb):** Overruled. The weekly reclass volume is a LeoBloom importer
  problem. SonOfLeo's import domain redesign may eliminate it; void + create are sufficient
  primitives.
- **#115 (Period auto-create):** Overruled. Monthly friction is a 30-second procedural
  step. Hobson adds it to the Saturday routine. Auto-create-on-post is a hidden side effect.

### Reclassification ledger updated

All completed items marked done/overruled in
`Audit/2026-07-06a/action-items-reclassification-2026-08-02.md` — but this commit hasn't
been pushed yet. Do that first.

## What's next

**P8 — FT-9: The Audit Re-run.** The capstone. Prerequisites are met:
- P1–P5 complete (spec ledger clean, coverage debt paid, DAL proved, gate wired, audit
  machine rebuilt)
- P6 and P7 resolved

To run it:
1. Get Dan's fresh statement of position (required, verbatim)
2. Invoke the workflow:
   ```
   Workflow({
     scriptPath: "<repo>/Skills/SonOfLeoRequirementsAudit/requirements-audit.workflow.js",
     args: {
       repoRoot: "/media/dan/fdrive/codeprojects/SonOfLeo",
       runDir: "/media/dan/fdrive/codeprojects/SonOfLeo/Audit/<date>",
       danStatement: "<verbatim>"
     }
   })
   ```
3. Walk Dan through each auditor's report one at a time (obligation-review style)
4. Record rulings in `99-disposition.md`; promote reusable precedent to `resolved-findings.md`

## Key resources

| What | Where |
|---|---|
| **The roadmap** | `HobsonsNotes/roadmap-2026-08-02.md` |
| **The disposition ledger** | `Audit/2026-07-06a/action-items-reclassification-2026-08-02.md` |
| Doc system + REQ grammar | `Specs/README.md` |
| Test standard | `Tests/README.md` |
| Infrastructure inventory | `Src/README.md` |
| Checks suite | `Checks/` — `bash Checks/run-all.sh` (hook runs `--quick`) |
| Traceability audit | `Skills/SonOfLeoRequirementsAudit/traceability-audit.sh` |
| Audit workflow + README | `Skills/SonOfLeoRequirementsAudit/` |

## Standing corrections

- **Hobson owns REQs only.** Everything else is Dan's to assign. Do not assume ownership
  of Checks/, Skills/, CompoundedLearnings/, scripts, or prose.
- **Be dramatically less verbose.** Tables and short paragraphs, ~250 words unless it's a
  deliverable document.
- **Never work in BD's clone.** Yours is `codeprojects`; his is `ai-sandbox`.
- **Deferral is not abandonment.** Ask which of the two before classifying.
- **Dan is PO — make the case once, then defer.**
- **Dispositions outrank re-derivation.** If your fresh reading of the repo contradicts a
  disposition in the reclassification doc, the disposition wins — raise the conflict with
  Dan, don't silently re-litigate.

## Outstanding items

- **Commit + push** the reclassification ledger updates from this session (P6/P7 items).
- **Remaining open items in the reclassification doc** (§1/§3) — these are not on the
  roadmap but are still tracked: #120a (unwrap test helper), #121a (isolated test-file
  mismatches), #125a (AppError coverage). All are BD/Dan's lane.
- **REQ-SYS-6.1 stale waiver** — traceability audit flags it each run (waived but tests
  exist). Cosmetic; the waiver row should be removed.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan. Then wait for instruction.
