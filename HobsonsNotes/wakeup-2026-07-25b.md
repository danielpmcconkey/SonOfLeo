# Wakeup — 2026-07-25b

## Who you are

You are **Hobson** — Dan's host-side Claude Code, the butler. Personality and
standing orders in `~/penthouse-pete/CLAUDE.md`; cross-project context in
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`. On SonOfLeo your
lane is **specs, infrastructure (Checks/, Skills/, PATTERNS.md), and review** —
Dan owns the F# through Rider, BD (Opus 4.6, Docker sandbox on this same clone)
builds under the guardrails. This session ran on Fable 5.

## What this project is

SonOfLeo: Dan's hand-rewrite of LeoBloom — cash-basis GAAP personal finance CLI,
F# on .NET 10, PostgreSQL, xUnit. Not a 1:1 port. Dan wrote it to learn F# and to
set an architectural foundation for LLM developers.

## Where we are

**Phase 3 (guardrail design/build) is COMPLETE and pushed** — the whole
enforcement layer is live on `core-je-fetching` (commits `090a609..71cf860`):

- **`PATTERNS.md` at repo root** — the canonical house-style doc (moved from
  HobsonsNotes). All guardrails cite it. Authority chain: Specs > PATTERNS.md >
  CompoundedLearnings.
- **`Checks/`** — nine grep-grade detectors + `run-all.sh` + `install-hooks.sh`.
  Pre-commit hook installed in `.git/hooks/` (shared clone → covers BD's container
  too). Doctrine: detect and refuse, NEVER rewrite. Extension process =
  CreateLearning Step 7 triage.
- **Fantomas adopted** — `.editorconfig` at root, one-time format commit
  (`66d2385`). Cost accepted eyes-open: Src +12.1%, Tests +21.5% lines. The two
  unconfigurable behaviors (CE brace eviction, one-arg-per-line) were tested
  against every config option incl. Stroustrup before Dan accepted.
  `check-format.sh` gates commits now.
- **Skills** — `Skills/CodeReviewer/` (BD's mandatory 4-pass self-review, new)
  and `Skills/TestWriter/` (reconciled to PATTERNS §7; P7.6 assertion canon;
  negative exemplars in `references/bullshit-test-specimens.md`, harvested from
  `bba1c17..5cb26c4`).
- **CompoundedLearnings** — triaged (record: `HobsonsNotes/cl-triage-2026-07-25.md`).
  3 stale articles deleted with Dan's approval, 3 updated, testing/process
  catalogs created, six domains kept.

**In flight, uncommitted: Dan is mid-refactor on #118a** — he extracted the DAL
into a new **`Src/DataAccessLayer/`** project (six files: DbConnections,
DbTransaction, QueryParameters, ExecuteReader, ExecuteNonQuery, ExecuteScalar);
`Utilities/DAL.fs` is deleted. Design: `DbTransaction` is itself the wrapper —
private record over `NpgTranAndConn option` — so participants take a
**non-option** `DbTransaction`; autocommit is the None-form. Full design
recorded in action item #118a. Only DataAccessLayer + Utilities build; Model on
up are not yet migrated. Layering becomes: Utilities ← DataAccessLayer ← Model ←
ModelOrchestrator ← InterfaceBridge ← CLI.

## What happened last session

1. Phase 3 built end-to-end (checks, hook, skills, specimens, learnings triage)
   after Dan dispositioned the architecture draft (`HobsonsNotes/phase3-architecture.md`).
2. Fantomas pilot on Account.fs → tuned config → Dan ruled adopt-all after
   seeing precise costs. Dan invented the inline `FieldUpdate` idiom for
   SET-clause builders (#126a, better than my named-bindings spec) — P2.5 amended.
3. #118a design conversation → Dan's wrapper-type idea chosen, recorded in the
   action item. Dan then started building it (the DataAccessLayer extraction).
4. I reviewed the extraction: architecture approved; found the
   rollback-mislabeled-as-commit bug (fixed in-session) and the missing
   autocommit constructor (open — see next). Updated check-npgsql/check-confirm-naming
   allowlists for the new paths (uncommitted, in tree).
5. **Corrections earned this session** — see Standing corrections: no test
   running, file:line on every finding, no unsolicited scope.

## What's next

1. **Dan finishes the #118a/DAL refactor.** Known open items in his lane:
   - **Autocommit constructor** — no way to build the None-form `DbTransaction`
     from outside the DAL; read paths can't compile until it exists.
   - `withTransaction` bracket itself (InterfaceBridge ownership) not yet written.
   - Migrating Model → CLI to the new non-option currency + all test call sites.
   - Open sub-decision: P7.5 rollback-form test story (test-facing
     always-rollback bracket vs exposed begin/rollback pair).
   - Deferred by Dan: the ~20 remaining `of unit` AppError cases.
2. **After the refactor lands and builds**: I amend PATTERNS.md P1.1 (six
   projects) and P2.3 (Npgsql home = DataAccessLayer project), and commit the
   Checks allowlist updates sitting in the tree.
3. **BD's first assignment** — doubles as the acceptance dry-run for the
   guardrails. Dan picks the task.
4. Backlog: #119a–#125a (125a flips check-apperror-coverage from report to
   gate; baseline 45/94), ~30 older audit items, FieldUpdate helper doc/annotations.

## Key resources

| What | Where |
|---|---|
| Canonical house style | `PATTERNS.md` (repo root) |
| Action items (118a design, 126a/127a DONE) | `Skills/SonOfLeoRequirementsAudit/Runs/2026-07-06a/action-items.md` |
| Guardrail architecture + Dan's dispositions | `HobsonsNotes/phase3-architecture.md` |
| Checks suite | `Checks/` (run: `bash Checks/run-all.sh [--quick]`) |
| BD's review protocol | `Skills/CodeReviewer/SKILL.md` |
| Test doctrine + specimens | `Skills/TestWriter/` |
| Learnings triage record | `HobsonsNotes/cl-triage-2026-07-25.md` |
| Fantomas pilot + P2.5 example | `HobsonsNotes/fantomas-pilot/` |
| The in-flight DAL refactor | `Src/DataAccessLayer/` (uncommitted) |

## Standing corrections

- **Dan owns the F#; you own specs/infrastructure/review.** Don't edit `Src/`
  or `Tests/` unless asked.
- **NEW — never run `dotnet test`.** Testing is Dan's and BD's lane; the
  integrated suite depends on DB state you don't manage. Review = conformance +
  `Checks/run-all.sh` + `dotnet build`. (In memory too.)
- **NEW — every review finding cites file:line.** Dan hunted for a typo I
  reported without location. No exceptions.
- **NEW — answer what was asked; park your own agenda.** Review requests get
  reviews, not to-do lists. Explaining a plan Dan didn't ask about reads as
  scope creep. Write it in HobsonsNotes instead.
- **Plain English in findings.** "Listed above for whenever you pass through"
  earned a "do you speak English." State the file, the line, the fix.
- **Dan is PO — he decides, you advise.** Make the case once, then defer.
  (Flagged twice = retired; e.g. the FieldUpdate helper doc comment.)
- **Every write traces to a stated purpose; lands in HobsonsNotes or the
  action-items file unless directed otherwise.** (Checks/, Skills/, PATTERNS.md
  became my lane by Dan's Phase 3 approval.)
- **Settle empirical questions with data, not memory** — the Fantomas/Stroustrup
  test won an argument my memory would have lost.
- **`[Dan]...[/Dan]` inline annotation** works well for document review cycles.
- **F# compile order is load-bearing.** Migration review is your job.
- Flagged for promotion to MEMORY.md (5+ sessions): "Dan owns the F#",
  "compile order", "skills live in repo / installed via symlink". Ask Dan.

## Outstanding items

- #118a completion (Dan, in flight) — see What's next.
- PATTERNS.md P1.1/P2.3 amendments — gated on the refactor landing (mine).
- Commit the Checks allowlist updates (in tree, uncommitted).
- BD onboarding + acceptance dry-run — gated on #118a per Dan's sequencing.
- #119a–#125a and ~30 older audit action items.
- ~20 `of unit` AppError cases — Dan deferred explicitly.
- Testing + process CompoundedLearnings domains have catalogs now but no articles.
- Test-file structure rethink (1 code : N test files) — still deferred.
- #113a traceability cross-reference — still unperformed.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
