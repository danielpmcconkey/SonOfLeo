# Wakeup — 2026-07-04a

## Who you are

You are **Hobson** — Dan's host-side Claude Code, the butler. Personality and
standing orders in `~/penthouse-pete/CLAUDE.md`; cross-project context in
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`. On SonOfLeo
your lane is **specs, infrastructure, and review** — Dan owns the F# and drives
it through Rider. You write to BD's workspace directly (this repo lives on BD's
side; you maintain `HobsonsNotes/` here).

## What this project is

SonOfLeo: Dan's hand-rewrite of LeoBloom — cash-basis GAAP personal finance,
F# on .NET 10, PostgreSQL, xUnit. NOT a 1:1 port. Account CRUD, Fiscal Period
CRUD, and Journal Entry CRUD are done end-to-end; the JE slice just merged to
main. **New read queries (3.6–3.9) are the active front.**

## Where we are

Branch `core-je-fetching` @ `248545d`, clean, pushed. Main is at the same SHA.
130 integrated + 177 isolated tests, all green. The full JE model — header,
lines, external references, comments, voiding, orchestration — is merged and
tested.

Dan asked Hobson for a **user-efficacy review** of the entire CLI surface against
the Saturday routine (the weekly LeoBloom batch that SonOfLeo will eventually
replace). That review produced:

- **Two bugs fixed** (both by Dan):
  1. `@source` → `@je_source` parameter mismatch in `JournalEntryHeader.insertNewToDb`
  2. `voidedAt` removed from `JournalEntryHeaderInput` (hardcoded to `None` at the
     boundary — posting a pre-voided entry was against spec)

- **Four new requirements added to JournalEntryCrud.md §3** (by Hobson):
  - **REQ-JE-3.6** — Account balance query (debit total, credit total, net) for
    non-voided lines. Needed for reconciliation every Saturday.
  - **REQ-JE-3.7** — Fetch entries by date range (inclusive Calendar Dates).
    Saturday operates weekly, not monthly; FetchByPeriod is too coarse.
  - **REQ-JE-3.8** — Fetch entries by FI only (no reference value required).
    "Show me everything from Synchrony" without knowing exact refs.
  - **REQ-JE-3.9** — Enriched lines-by-account: line fields + parent entry's
    `entry_date`, `description`, `source`, `voided_at`. Boundary-only return
    type — model unchanged. Replaces withdrawn REQ-JE-3.4 (bare lines without
    entry context are useless for account activity review).

- **REQ-JE-3.4 stricken**, original text preserved in the Withdrawn table.
- **REQ-JE-5.4 stricken** by Dan (too restrictive), replaced by REQ-JE-5.6
  (primary link fixed, secondary now mutable).

- **Acknowledged but not acted on:**
  - Trial balance is the next slice, not this one.
  - Fiscal period auto-creation is on Dan's backlog but undesigned; he'll
    pre-create the first year.
  - Hobson gave Dan contradictory advice during coding (#5 — told him bare
    lines-by-account was sufficient, then flagged the absence of entry context
    as a gap). Noted for future: don't sign off on a query shape without
    thinking about what the actual consumer needs in the return type.

## What happened last session

Hobson loaded both SonOfLeo and LeoBloom wakeups. Dan asked for a full
user-efficacy review: "you're the primary user-to-be — will this meet your
needs?" Hobson reported 3 bugs, 3 missing capabilities, and 3 missed
opportunities. Dan accepted 2 bugs (fixed them, Hobson verified), rejected 1
(REQ-JE-5.4 was stricken by design), accepted 3 of the 6 capability/opportunity
items as new REQs, deferred 2 to future slices, and called Hobson out on the
contradictory advice. Hobson then added the new REQs and performed the 3.4→3.9
surgery on the spec.

## What's next

Dan writes Src for the 3.6–3.9 read queries. BD tests them after Dan's code
lands. Hobson's work here is done until Dan wants spec review, migration review,
or another efficacy pass.

## Key resources

| What | Where |
|---|---|
| JE spec (current, with 3.6–3.9) | `Specs/Behavioral/JournalEntryCrud.md` |
| Account spec | `Specs/Behavioral/AccountCrud.md` |
| SystemWide spec | `Specs/Behavioral/SystemWide.md` |
| Decisions log | `Specs/Decisions.md` |
| JE CLI routes | `Src/SonOfLeoCli/JournalEntryRoutes.fs` |
| Interface contracts | `Src/Model/UI/InterfaceContractTypes.fs` |
| JE creation orchestrator | `Src/ModelOrchestrator/JournalEntryCreation.fs` |
| JE voiding orchestrator | `Src/ModelOrchestrator/JournalEntryVoiding.fs` |
| JE fetching orchestrator | `Src/ModelOrchestrator/JournalEntryFetching.fs` |
| Account deactivation | `Src/ModelOrchestrator/AccountDeactivation.fs` |
| BD's latest wakeup | `BdsNotes/wakeup-2026-07-03a.md` |
| Prior Hobson wakeup | `HobsonsNotes/wakeup-2026-06-22a.md` |

## Standing corrections

- **Dan owns the F#, you own the specs.** Review and advise on code, don't edit
  `Src/` or `Tests/` unless asked. You edit `Specs/` directly.
- **Settle empirical questions with prod data, not memory.** You have the
  `leobloom_prod` skill. Use it before agreeing or arguing.
- **Dan is PO on SonOfLeo — he decides, you advise.** Make the case, then defer.
- **F# compile order is load-bearing** — a file only sees files above it in the
  `.fsproj`.
- **Account active dates are inclusive Calendar Dates.** `begin <= ref <= end`.
- **Migration review is your job** — check names, nullability, CHECKs, FK
  direction, drop order/re-runnability, and per-env ownership every time.
- **Don't sign off on a query shape without thinking about the consumer's return
  type.** The 3.4→3.9 lesson: "lines by account" sounded right in the abstract,
  but the return type was missing the two fields (entry_date, description) that
  make the query useful. Think about what the caller actually looks at.

## Outstanding items

- **REQ-JE-3.6/3.7/3.8/3.9 code not yet written.** Dan is doing this. Do NOT
  test, stub, or flag these until his code lands.
- **REQ-AC-4.6** (deactivation vs JE entry dates) — now implemented in
  `AccountDeactivation.fs`. Verify it's covered by tests when next reviewing
  the deactivation code.
- **Pin whether `external_reference` is a SYS-3.1 entity.** Migration gives it
  `created_at`+`modified_at`; REQ-JE-4.9/4.10 need it to be editable. Spec
  should say so explicitly.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
