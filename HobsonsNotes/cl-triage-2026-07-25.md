# CompoundedLearnings Triage — 2026-07-25

Staleness pass of all 34 articles + 4 catalogs against PATTERNS.md and the post-July-refactor
codebase. Per Dan's rule: nothing is hard-deleted until he approves the deletion list below.

## Deletion list — APPROVED BY DAN, EXECUTED 2026-07-25

| Article | Why it dies |
|---|---|
| `articles/architecture/two-layer-architecture.md` | Superseded by P1.1's five-layer architecture. Actively wrong on two counts: no InterfaceBridge, and "orchestration has no business logic of its own" contradicts P1.1 (ModelOrchestrator owns cross-entity business validation). |
| `articles/architecture/solution-structure.md` | Superseded by P1.1. Pre-refactor project list (no InterfaceBridge), describes `Ops/Portfolio/Reporting/UI` Model subdivisions and DevDataStage-in-Src (leaving per #116a). Its "where new code goes" table would misdirect BD today. |
| `articles/coding/validate-then-construct.md` | The `validateThenConstruct` function is retired — P3.1 (private record + companion `create` over validated components) and P4.1 (`reconstitute`) are the survivors. Also stale on `Result<T, string>` (#117a). Redundancy with PATTERNS.md is a defect: two sources of truth drift. |

On approval I delete the three files and their catalog rows (two in `architecture.md`,
one in `coding.md`).

## Updated (done — stale terminology, core still valuable)

- `architecture/type-taxonomy.md` — VTC → smart constructors (P3.1); contracts relocated to
  InterfaceBridge (P1.4). **Kept its unique value:** the contract-sharing doctrine (Return
  types may be shared on identical shape; Input types never shared across semantically
  different operations) — this exists nowhere in PATTERNS.md.
- `coding/validation-layers.md` — layer 2 reworded from VTC to smart constructors/create;
  cross-refs to P3.1–P3.4, P4.1.
- `architecture/orchestration-layer.md` — post-refactor note added (P1.1/P4.4 cross-refs);
  its "does it orchestrate?" test remains the best statement of the boundary anywhere.

## Kept unchanged (28)

- `architecture/` (3): container-build-discipline, debug-release-access,
  environment-separation — operational rules PATTERNS.md deliberately doesn't cover;
  debug-release-access is the basis for #115a.
- `coding/` (7): descriptive-naming, field-update-pattern, money-arithmetic-boundaries,
  money-type-enforcement, nodatime-discipline, temporal-arithmetic, temporal-persistence —
  all consistent with P2/P6; each carries depth (rounding mode, USD assumption,
  instant-vs-date algebras, DB-never-originates-time) that PATTERNS.md references but
  doesn't spell out. Plus validation-location (the F#-vs-SQL two-condition test — unique,
  load-bearing).
- `audit-conduct/` (12): all kept — auditor judgment rules from the 2026-07-06a audit,
  independent of code patterns. (One example in schema-guaranteed-values cites the old
  `constructFromRawForDbRead` name; it's a historical citation of an overruled finding —
  accurate as history, left alone.)
- `gaap-domain/` (5): all kept — domain knowledge serving both coding agents and
  finance work.

## Structural changes (done)

- **README authority chain**: now *Specs > PATTERNS.md > learnings*.
- **`catalogs/testing.md` created** — seeded with the bullshit-test-specimens reference.
- **`catalogs/process.md` created** — seeded with the guardrail-triage pointer and the
  standing migration-review / green-checks rules.
- **CreateLearning Step 7 added** — the guardrail triage (mechanical → Checks script;
  recurring judgment → article + skill checklist line; house style → PATTERNS.md
  amendment). This is the whole moving-forward extension process.
- `catalogs/coding.md` VTC row wording fixed (full row removal awaits the deletion approval).

## Domain structure verdict (Dan flagged the six as unvetted)

Recommendation: **keep the six domains** (audit-conduct, gaap-domain, architecture, coding,
testing, process). They partition cleanly by *reader*: coding agents read architecture/
coding/testing; auditors read audit-conduct; everyone reads gaap-domain; process spans.
For the finance-operations future (SonOfLeo running Dan's books), gaap-domain already
serves it, and a new domain (e.g. `bookkeeping-ops`) can be added the day a learning
doesn't fit — CreateLearning Step 2 already requires proposing new domains to Dan.
Renaming or merging now would churn 30+ article paths for no present reader. Boring but right.
