# Wakeup — 2026-09-16a

## Who you are

Hobson, Dan's host-side Claude Code, the butler. CLAUDE.md at
`~/penthouse-pete/CLAUDE.md`, memory at
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`.

## Lanes

| Actor | Owns | Never touches |
|---|---|---|
| **Dan** | `Src/` F#. Assigns all work not covered below. | — |
| **Simian** | `Src/` mechanical build-out under Dan's direction | `Specs/`, `Tests/` |
| **BD** | `Tests/` — own branches, own clone | `Src/`, `Specs/` |
| **Hobson** | `Specs/` (REQs). ArchReviewer skill. First-round audit adjudication (Layer 6). | `Src/`, `Tests/`; never `dotnet test`; never BD's clone |

Everything else is Dan's to assign per task. See `feedback_sonofleo_lanes.md`.

## Where we are

**CashFlow build is complete.** Zero `NotImplementedException` left in
`Src/`. The full CashFlow/Saturday plan from
`/home/dan/simian/plans/cashflow-saturday-routine.md` is built and
committed. 38 commits reviewed and approved by the ArchReviewer skill
(29 original + 2 audit-action/backstop fixes + 3 projection + 1
billsToChase fix + 2 sort fixes + 1 confirmComposite refactor).

**Agentic pipeline plan written** last session (2026-09-13). Dan pivoted
to full agentic development. Plan at
`HobsonsNotes/agentic-pipeline-plan-2026-09-13.md`.

## What happened this session (2026-09-13 through 2026-09-16)

### ArchReviewer skill created

Built a "What Would Dan Do" code review skill by mining Dan's review
history from Simian's session logs. Found the engagement cliff (Sep
10–11 — Dan stopped reviewing individual commits), extracted 15
architectural critique patterns from 17 sessions, then scanned the full
`Src/` at the last reviewed commit (`005bef5`) to document 17
architectural paradigms with code examples.

Deliverables:
- `Skills/ArchReviewer/SKILL.md` — 13 checks ranked by Dan's enforcement frequency
- `Skills/ArchReviewer/references/paradigms.md` — the canonical shape for every pattern
- Installed in Hobson's harness as `SonOfLeo:ArchReviewer`

### Full review of all unreviewed commits

Reviewed all 29 commits since Dan stopped manual review. Findings:
- 0 REJECTED, 10 CONDITIONAL, 19 APPROVED
- 2 actionable findings: stale `Ingest*` audit action names (fixed in
  `a7123f4`), impossible-case backstop in `isOverpaid` (fixed in
  `11f268e`)
- 1 false positive retracted: deeply nested tuples in construct
  functions are the established pattern, not a smell. Paradigms doc and
  SKILL.md updated.

### Projection feature designed and reviewed

Simian was blocked on `projectCashFlowNDaysForward` — needed Hobson's
answers since Hobson is the only consumer. Answered 8 design questions.
Simian caught an inversion in my answer about which leg is cash (I had
it backwards — corrected). Three commits reviewed and approved:
- `9a41ee9` — orchestrator body
- `79e7061` — route
- `16fbadd` — billsToChase per-agreement fix (Hobson updated REQ-CF-8.4)

### Spec update: REQ-CF-8.4

Changed from "Instances with no Invoice" to "Payment agreements on
unfulfilled Instances that have no Invoice." Fixes a real gap where a
mixed instance (one fixed invoice present, one variable invoice missing)
looked fully covered.

### Additional Simian commits reviewed

- `387d965` — confirmComposite refactor, delegates to InstanceOrchestration. Approved.
- `2307ac9` — ClassifyPaymentAgreements sort + shared converter sort for 5 more routes. Approved.
- `6105375` — transitionPaymentsToPosted sort moved to boundary. Approved.
- Several smaller cleanup commits (dead AppError case, stale todo, today() → context instant). All approved.

## What's next

1. **Saturday dry run.** The CashFlow domain is built. The first real
   Saturday through SonOfLeo is the cutover test. Can Hobson run the
   full routine without touching LeoBloom's CLI for any ledger or
   obligation operation?

2. **Agentic pipeline.** The plan is written
   (`HobsonsNotes/agentic-pipeline-plan-2026-09-13.md`). Open questions
   from the plan:
   - Who builds the Pipeline engine? (Python, not F#)
   - First slice to prove the pipeline?
   - Hobson's adjudication authority boundaries (Layer 6.3)?

3. **Remaining Simian items** (from his end-of-build report):
   - UpdatePaymentAgreementLink can strand a payment (validation gap)
   - RuleMatch.fetchByRunIdAndClaimantType has no caller (dead code)
   - EveryOtherWeek only checks weekday, not phase (pre-existing)
   - CashflowInvoiceCompositeUpdateNoOp has no raise site (dead AppError)

4. **Spec updates still queued** from the 09-11 wakeup — REQ-CF-7.7,
   REQ-CF-6.5, REQ-CF-11, REQ-STG-2.8, status transition table, state
   machine draft.

5. **SrcDeveloper skill needs updates** for the new architecture.

## Key resources

| What | Where |
|---|---|
| **ArchReviewer skill** | `Skills/ArchReviewer/SKILL.md` |
| **ArchReviewer paradigms** | `Skills/ArchReviewer/references/paradigms.md` |
| **Agentic pipeline plan** | `HobsonsNotes/agentic-pipeline-plan-2026-09-13.md` |
| **Ratified build plan (CashFlow)** | `/home/dan/simian/plans/cashflow-saturday-routine.md` |
| **CashFlow spec** | `Specs/Behavioral/CashFlow.md` |
| **DataIngestion spec** | `Specs/Behavioral/DataIngestion.md` |
| **SrcDeveloper skill** | `Skills/SonOfLeoSrcDeveloper/SKILL.md` |
| **Saturday routine draft** | `HobsonsNotes/saturday-routine-sonofleo-draft-2026-08-28.md` |
| **Migration roadmap** | Memory: `project_sonofleo_roadmap.md` |

## Standing corrections

- **Hobson owns REQs only.** Everything else is Dan's to assign.
- **Never run `dotnet test`.** Dan's/BD's lane.
- **Never work in BD's clone.** Yours is `codeprojects`; his is `ai-sandbox`.
- **Never edit Src/ files.** Dan writes all F# code. Review only.
- **Dispositions outrank re-derivation.** If your fresh reading
  contradicts a disposition, the disposition wins — raise the conflict.
- **Dan is PO — make the case once, then defer.** (Exception: Layer 6
  grants Hobson first-round audit adjudication authority.)
- **A domain records its own bookkeeping on its own table.**
- **Authority hierarchy:** Dan > Hobson > import script > classifier.
- **Account IDs in the model, codes at the boundary.**
- **The build plan supersedes the state machine draft on flow ordering.**
- **Pull before editing, pull before committing.** Use `--rebase`.
- **Infallible create, orchestrator validates.**
- **TransactionPointer is line-level on both sides.**
- **Stage entries carry back-linkage to ledger.**
- **No backstop creation — hard stop with rollback.**
- **This is a public repo.** No FI names, account numbers, or PII.
- **Spec language: operator concepts, not implementation details.**
- **Tuple lists in construct functions are not a smell.** Deliberate
  avoidance of a third named type between composite and contract.

## Outstanding items

- **CashFlow spec needs major update** — three-table model, derived
  lifecycle states, isFulfilled, PA linkage, diagnostic entity,
  line-level pointers, projection (REQ-CF-8.4 updated this session).
- **DataIngestion spec needs update** — back-linkage reversal, status
  transition table changes.
- **REQ-CF-7.7, 6.5, 11** — stale or void per the build notes.
- **State machine draft** — stale on flow ordering.
- **SrcDeveloper skill** — needs updates for the new architecture.
- **UpdatePaymentAgreementLink** — can strand a payment (no validation
  that existing payments are cleared before relinking).
- **RuleMatch.fetchByRunIdAndClaimantType** — dead code, no caller.
- **EveryOtherWeek phase validation** — pre-existing gap, checks weekday
  but not bi-weekly phase.
- **CashflowInvoiceCompositeUpdateNoOp** — dead AppError case, no raise site.
- **Saturday cutover test** — can Hobson run the full Saturday through
  SonOfLeo without touching LeoBloom's CLI?

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
