# Wakeup — 2026-08-08a

## Who you are

Hobson, Dan's host-side Claude Code, the butler. CLAUDE.md at
`~/penthouse-pete/CLAUDE.md`, memory at
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md` —
`project_sonofleo.md` is current.

## Lanes

| Actor | Owns | Never touches |
|---|---|---|
| **Dan** | `Src/` F#. Assigns all work not covered below. | — |
| **BD** | `Tests/` — own branches, own clone | `Src/`, `Specs/` |
| **Hobson** | `Specs/` (REQs) | `Src/`, `Tests/`; never `dotnet test`; never BD's clone |

Everything else is Dan's to assign per task. See `feedback_sonofleo_lanes.md`.

## Where we are

`main` at `d40f6a6`, pushed. **All 10 audit action items from 2026-08-03a
are done.** Trial balance specs written, tests implemented, test doctrine
updated. The project is ready for new feature work.

## What happened this session (2026-08-07)

### Audit cleanup (items 9–10)

- **#9:** Audit workflow updated — auditors with no findings must now
  populate `noFindingsReasoning` in their report. Schema, `formatReport`,
  pipeline writer, and HYGIENE instructions all updated.
- **#10:** New CompoundedLearnings article
  `articles/process/audit-batch-execution.md` documenting the FT-1
  revision (parallel batches, not sequential). Cataloged in `process.md`.

### Trial balance specs (new: `Specs/Behavioral/Reporting.md`)

Hobson wrote the full Reporting spec — domain `RPT`:
- **Section 1 (12 REQs):** Trial balance data — tree walk, roll-up,
  sort, as-of filtering, voided exclusion, normal balance direction,
  zero-activity accounts.
- **Section 2 (6 REQs):** Report output — OutputSpecifier DU, data-only
  vs report mode, date interpolation, file I/O errors, read-only context.
- **Section 3 (6 REQs):** HTML rendering — header/footer, depth CSS
  classes, sign CSS classes, print CSS. All waived (code review).
- **REQ-RPT-1.12 withdrawn** — Dan's call. The trial balance must always
  include all accounts; an optional filter at this layer is wrong because
  you can't verify offsets balance without the full COA.

### NGUI spec amendments

- **REQ-NGUI-1.6:** Account name required in all return payloads
  (CUST-NAME-1).
- **REQ-NGUI-3.10:** `--file` flag as alternative payload delivery.
- **Section 4 (5 REQs):** Reports CLI — separate executable, name-based
  routing, same exit code conventions.
- Stale waivers for NGUI-4.2 and NGUI-4.4 removed after Dan's CLI tests
  provided coverage.

### Traceability check scoped to main

`Checks/check-traceability.sh` now skips on feature branches — only
enforces on main. Solves the chicken-and-egg problem where specs and
tests land from different actors on different branches.

### Tests — full round trip with BD

Dan wrote Reports CLI E2E tests (`Tests/Tests.Integrated/Reports/Program.fs`),
then BD implemented the trial balance and report route test stubs. Dan
reviewed BD's work, wrote 16 points of feedback (`delete-me-bd-feedback.txt`
on Dan's desktop — ephemeral), showed BD the `return! match` pattern
(commit `ee09e30`), and BD revised. Key lessons:

- **Foxes/hen house:** BD was using `fetchByAccountIdList` to derive
  expected values for tests of `fetchTrialBalanceData`, which calls
  `fetchByAccountIdList`. Both sides share the same bug. Expected values
  must come from fixture journal entry lines.
- **Use fixture.Data, not re-fetches:** BD kept calling `Account.fetchAll`
  for data already in `fixture.Data.accounts`.
- **`return! match` over `Result.map`:** keep DU matching inside the
  result CE so errors stay in the railway.

### Test doctrine updates

All three lessons codified:
- **Specimen 6** added to `bullshit-test-specimens.md` — the foxes/hen
  house pattern with before/after.
- **Two new advisories** in `Tests/README.md` — call-chain rule broadened
  to all values, and "use fixture.Data not re-fetches."
- **`return! match` pattern** added to TestWriter skill Code Quality
  section.

### Branch cleanup

Deleted 1 local + 5 remote branches (all merged): `trial-balance-report`,
`audit-action-items-6-8`, `audit-test-gaps-je49-je410`,
`sadpath-pattern-enforcement`, `sadpath-route-test-conversion`.

### COA sort order issue discovered

Dan's prod COA has children whose codes sort before their parents (e.g.,
5280 is a child of 5300). The trial balance's flat `sortBy(_.accountCode)`
breaks hierarchical display — a child appears before its parent.

Two fixes needed:
1. **Code:** sort children by code inside `crawlAndCompile`, drop the
   final `sortBy` — Dan's to do in `Src/`.
2. **COA in prod:** renumber where codes don't reflect hierarchy — direct
   edit per the standing carve-out.

REQ-RPT-1.6 ("sorted by account code") needs revision once Dan decides
the sort order. Note appended to the LeoBloom wakeup
(`LeoBloom/HobsonsNotes/wakeup-2026-08-02a.md`) for the COA renumbering.

## What's next

1. **Dan fixes the trial balance sort order** in `Src/`. Then Hobson
   revises REQ-RPT-1.6 to specify tree-order traversal instead of flat
   sort by code.

2. **Staging domain REQs.** The charter is at
   `HobsonsNotes/staging-domain-charter.md`. Many open design questions
   flagged in the charter need design sessions before full REQs. Phase A
   (base staging format, staging table, ingestion CLI) is the starting
   point.

3. **COA renumbering in LeoBloom prod** — deferred to a LeoBloom session.
   Note in LeoBloom wakeup.

## Key resources

| What | Where |
|---|---|
| **Reporting spec** | `Specs/Behavioral/Reporting.md` |
| **NGUI spec (amended)** | `Specs/Behavioral/NonGraphicalInterface.md` |
| **Staging domain charter** | `HobsonsNotes/staging-domain-charter.md` |
| **Audit action items (all done)** | `Audit/2026-08-03a/action-items.md` |
| **Trial balance orchestrator** | `Src/ModelOrchestrator/TrialBalance.fs` |
| **Reports CLI** | `Src/Reports/Program.fs` |
| **Report contracts** | `Src/InterfaceBridge/InterfaceContracts/ReportsContracts.fs` |
| **TestWriter skill** | `Skills/TestWriter/SKILL.md` |
| **Bullshit test specimens** | `Skills/TestWriter/references/bullshit-test-specimens.md` |
| **Audit workflow (batched)** | `Skills/SonOfLeoRequirementsAudit/requirements-audit.workflow.js` |
| Doc system + REQ grammar | `Specs/README.md` |
| Checks suite | `Checks/` — `bash Checks/run-all.sh` |

## Standing corrections

- **Hobson owns REQs only.** Everything else is Dan's to assign.
- **Never run `dotnet test`.** Dan's/BD's lane.
- **Never work in BD's clone.** Yours is `codeprojects`; his is `ai-sandbox`.
- **Dispositions outrank re-derivation.** If your fresh reading contradicts
  a disposition, the disposition wins — raise the conflict with Dan.
- **Dan is PO — make the case once, then defer.**
- **No specific error case names in spec waiver rows.** Say "fails with a
  typed AppError," not the case name.
- **FT-1 revised:** auditors run in parallel batches of 5, not
  sequentially. Review is still one auditor at a time.
- **Traceability check runs on main only.** Feature branches skip it.

## Outstanding items

- **REQ-RPT-1.6 revision** — pending Dan's sort-order code fix.
- **COA renumbering** — deferred to LeoBloom session. Note in LeoBloom
  wakeup.
- **Staging domain design sessions** — open questions in the charter need
  resolving before specs can be written.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
