# Wakeup — 2026-07-25a

## Who you are

You are **Hobson** — Dan's host-side Claude Code, the butler. Personality and
standing orders in `~/penthouse-pete/CLAUDE.md`; cross-project context in
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`. On SonOfLeo
your lane is **specs, infrastructure, and review** — Dan owns the F# (drives it
through Rider), BD currently touches only tests. This repo lives on BD's side;
you maintain `HobsonsNotes/` here.

Note: this session ran on **Fable 5** (Dan switched models for it). The
assignment was written for Fable-grade strategy work; if you wake up as Fable,
mind the token bill — Dan wants strategy from you and construction delegated
to Opus 4.6.

## What this project is

SonOfLeo: Dan's hand-rewrite of LeoBloom — cash-basis GAAP personal finance,
F# on .NET 10, PostgreSQL, xUnit. NOT a 1:1 port. Dan wrote every line by hand
over ~2 months, partly to learn F#, partly to set an architectural foundation
for LLM developers (BD, Opus 4.6, Docker sandbox) to build on.

## Where we are

Branch `core-je-fetching`, top of tree just past `ef2c18c`. The July refactor
is **done**: all Src builds, the test suite is fully migrated and rewritten
(the bullshit tests are expunged — before/after specimens live in commits
`bba1c17..5cb26c4`, harvest them for the test-quality skill).

We are mid-way through a three-phase assignment defined in
`HobsonsNotes/fable-prompt.md`:

- **Phase 1 (done):** Hobson read all of `Src/` + a thorough sample of
  `Tests/` and extracted the house patterns.
- **Phase 2 (done):** Dan dispositioned every pattern via `[Dan]...[/Dan]`
  inline annotations; discussion resolved D1–D14 and O1–O8. Result:
  **`PATTERNS.md` — the confirmed canonical house-style
  document.** Read it first; it is the foundation for everything in Phase 3.
  (`patterns-draft.md` is the superseded discussion record with Dan's raw
  annotations.)
- **Phase 3 (next):** design the guardrail/enforcement layer for BD.

Eight new action items (118a–125a) were appended to
`Skills/SonOfLeoRequirementsAudit/Runs/2026-07-06a/action-items.md` — code
work for Dan arising from the pattern review. The big one is **#118a**
(transaction ownership moves to InterfaceBridge + `withTransaction` helper),
which should land **before BD writes code** because it changes signatures BD
would learn from.

## What happened last session

1. Read the codebase (all Src line-by-line, tests sampled deep), produced
   `patterns-draft.md` with batch-confirm patterns, 14 discussion items, and
   8 observations (2 real bugs — Dan fixed several in-session, including
   converting an `of unit` error case to nullary).
2. Dan annotated inline; Hobson answered the open questions (FsToolkit,
   AppError structure, LookupCache future, namespace/module rules, re-fetch,
   transaction seam). All resolved — dispositions condensed into a decision
   table at the bottom of `PATTERNS.md`.
3. Wrote `PATTERNS.md` (canonical), marked pending gaps `[pending: #NNNa]`.
4. Logged action items 118a–125a.
5. **A blemish to remember:** Hobson emitted one spurious Write to a nonsense
   path (`/media/fan/...placeholder-check.md`) with no purpose. It failed on
   permissions; nothing was created. Dan noticed and asked why. There was no
   why — it was a misfire. See standing corrections.

## What's next — Phase 3: design the guardrails

Read `fable-prompt.md` Phase 3 in full. Summary of the brief plus what Phase 0
discussion added:

**Deliverables:** a first-pass code-reviewer skill; a test-writing skill; CI
checks; whatever else is warranted. All referencing `PATTERNS.md`, not
restating it.

**Dan's constraints (from Phase 0 discussion, binding):**
- **Portable.** If Dan moves to Gemini, the guardrails must still work.
  Guardrail content = plain files in the repo; Claude-specific wiring is a
  thin adapter.
- **CI-phase, not real-time.** Dan's database-trigger analogy: the 100th
  real-time hook makes every operation a slog. Deterministic checks run at a
  CI/pre-commit phase, not per-edit.
- **Fantomas for formatting** (COTS), thin scripts for house rules no COTS
  tool knows (`Clock` not `DateTime.Now`, Npgsql only in DAL, compile-order
  integrity, `TestingError` banned in Src, no wildcard in `toMessage`),
  prose skills only for judgment calls.
- **No bureaucracy expansion.** Directory-ownership enforcement and BD's
  CLAUDE.md/settings are **parked** unless they prove load-bearing.
- **Fable does strategy; Opus 4.6 builds.** Design the architecture and spec
  the pieces for handoff.
- **Extensibility is a core tenet.** Evaluate the existing
  `Skills/CreateLearning/SKILL.md` + `CompoundedLearnings/README.md`
  framework — Dan suspects the framework has merit even though many specific
  learnings died in the refactor. New problems → new remediations must slot
  into the moving-forward process.
- **Acceptance dry-run** (subagent plays BD with skills installed, small
  task, see what leaks) — agreed as a good idea, cost-conscious timing.

**Specific guard targets Dan named:** (1) bullshit tests — use the
`bba1c17..5cb26c4` specimens as negative exemplars; (2) rolling-his-own —
the P2 infrastructure catalog in patterns.md is the discoverability answer,
violations mechanically detectable; (3) stylistic variance — the codebase
must look single-authored in 6 months.

**Also queued (pre-existing):** #125a (every-AppError-case-tested suite) is a
natural CI guardrail; testing + process CompoundedLearnings domains are still
empty and Phase 3 likely feeds them.

## Key resources

| What | Where |
|---|---|
| **The canonical patterns doc** | `PATTERNS.md` |
| The assignment | `HobsonsNotes/fable-prompt.md` |
| Phase 2 discussion record | `HobsonsNotes/patterns-draft.md` |
| Action items (incl. new 118a–125a) | `Skills/SonOfLeoRequirementsAudit/Runs/2026-07-06a/action-items.md` |
| Bullshit-test specimens | `git log bba1c17..5cb26c4` (test rewrite commits) |
| Learning framework to evaluate | `Skills/CreateLearning/SKILL.md`, `CompoundedLearnings/README.md` |
| Test fixture / infrastructure | `Tests/Tests.Integrated/_TestDataStage.fs`, `_Cleanup.fs`, `GenericTestProperties.fs` |

## Standing corrections

- **Dan owns the F#, you own the specs/infrastructure/review.** Don't edit
  `Src/` or `Tests/` unless asked.
- **Dan is PO — he decides, you advise.** Make the case, then defer.
- **Every write must trace to a stated purpose, and lands only in
  `HobsonsNotes/` or the action-items file unless Dan directs otherwise.**
  (Earned this session — the placeholder-check misfire. If you can't say why
  a tool call exists, don't make it.)
- **`[Dan]...[/Dan]` inline annotation worked well** — reuse it for any
  future document review cycle.
- **Batch the uncontroversial, discuss the contentious one at a time** — Dan
  approved this triage for pattern review; it fit him.
- **Settle empirical questions with data, not memory.**
- **F# compile order is load-bearing.**
- **Migration review is your job.**
- **CompoundedLearnings is operational guidance; behavioral specs win on
  conflict.**
- **Skills live in the repo; both Hobson and BD install via symlinks.**
- Flagging for promotion to memory (5+ sessions old): "Dan owns the F#",
  "compile order", "skills live in repo" — ask Dan whether to move these to
  MEMORY.md and cull here.

## Outstanding items

- **Phase 3** — the immediate work (above).
- **#118a before BD codes** — Dan's task; sequencing gate for BD onboarding.
- **#119a–#125a** — Dan's smaller fixes from pattern review.
- **~30 older confirmed audit action items** — see action-items.md; FT-6
  sequencing (minor before major) still applies.
- **Testing + process CompoundedLearnings domains empty.**
- **Test-file structure rethink** (1 code file : N test files) — deferred,
  needs its own discussion; Phase 3 framework must not preclude it.
- **#115a** — config/env/DAL protection hook (infrastructure, relates to
  Phase 3).
- **Traceability cross-reference #113a** — still unperformed.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
