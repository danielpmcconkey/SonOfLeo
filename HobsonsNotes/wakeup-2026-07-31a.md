# Wakeup — 2026-07-31a

## Who you are

You are **Hobson** — Dan's host-side Claude Code, the butler. Personality and standing
orders in `~/penthouse-pete/CLAUDE.md`; cross-project context in
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`. On SonOfLeo your lane is
**specs, infrastructure (`Checks/`, `Skills/`, `CompoundedLearnings/`) and review**. Dan
owns the F# through Rider. BD writes tests, on his own branch, in his own clone.

**Your working copy moved today.** It is now `/media/dan/fdrive/codeprojects/SonOfLeo`.
`repos.json` is updated. Do not work in `/media/dan/fdrive/ai-sandbox/workspace/SonOfLeo`
— that is BD's, and the reason is written up under Standing corrections.

## What this project is

SonOfLeo: Dan's hand-rewrite of LeoBloom — cash-basis GAAP personal finance CLI, F# on
.NET 10, PostgreSQL, xUnit. Not a 1:1 port. He wrote it to learn F# and to set an
architectural foundation for LLM developers.

## Where we are

`main` at `1958b55`, pushed, sole branch. 8 checks pass, `dotnet build` clean, Dan reports
339/339 tests. **The pre-commit hook is installed and firing** — first time since 2026-07-26.

Today was three jobs: execute the documentation disposition, extract a shared test project,
and stand BD up. All three landed.

**The documentation corpus is roughly half what it was.** `PATTERNS.md`, all of
`Specs/Conventions/`, and two TestWriter references are gone. `Specs/Decisions.md` is now
`Specs/Archive/Decisions.md`, labelled history. The live shape:

| Doc | Holds |
|---|---|
| `Specs/README.md` | The document system, the REQ grammar, and the rule the whole cleanup follows |
| `Specs/Definitions.md` | Terms with a SonOfLeo-specific meaning |
| `Src/README.md` | **New.** The infrastructure inventory salvaged from PATTERNS §2, plus the two conventions the code follows silently |
| `Tests/README.md` | **The single test standard.** Five test forms, the silent-pass hazard, fixture rules |
| `Skills/*/SKILL.md` | Procedure only. They point at the standards, they don't restate them |
| `CompoundedLearnings/` | Judgment |
| `Checks/*.sh` | Everything mechanical |

The governing rule, now written down in `Specs/README.md`: **prefer the executable form of
a rule; where it can't be executable, write it once, as close as possible to the thing it
governs.** Skills are procedure; READMEs next to the code are standard. Keep that split.

## What happened last session

Long session. Six things worth carrying.

**1. The disposition executed (`792fded`).** Dan read `doc-disposition-2026-07-29.md`, ruled
"assume I agree with everything I didn't comment on," and gave four annotations. Deletions
went as specced. Net −1318/+463 lines. **My disposition was wrong on one point:** it claimed
nothing in `Checks/` cited `PATTERNS.md`; nine scripts did, in headers and failure messages.
Those now name the rule instead of a P-code.

**2. The five test forms are settled and written down.** Dan's list had a numbering gap that
made it look like six; there are five, and the first one's real divider is "does it touch the
database," not "is it a conversion." Forms 3 and 4 are the disambiguation that matters — same
test shape, opposite cleanup, decided by whether *the function being tested* owns its
transaction. `Tests/README.md` has them as a decision tree.

**3. Dan created `Tests/Tests.Helpers`** (`c0a7c4b`) and hit exactly the constraint I'd
flagged: `[<CollectionDefinition("SharedTestData")>]` cannot live in a referenced library,
because xUnit reflects over the *executing test assembly* to find it. Every integrated test
failed until the two-line declaration went back into `Tests.Integrated`. That reason is now
recorded in `Tests/README.md` so nobody retries the move.

**4. Fantomas is gone** (`e8ce2e9`), Dan's call — he'll reformat by hand. `check-format.sh`
deleted, the five `fsharp_*` keys stripped from `.editorconfig`. The payoff: `check-format`'s
false FAIL was the sole reason the pre-commit hook was disabled, so the hook is back, in both
clones. **Do not offer to reinstate Fantomas.**

**5. Two silently-passing tests found and fixed** (`d28cbba`). `REQ-AC-1.4/2.9` and
`REQ-AC-2.6` in `Tests/Tests.Integrated/Model/Ledger/Account.fs` were bare
`runFuncAndAutoRollback` calls with no terminator, so the `[<Fact>]` body evaluated to a
`Result` and xUnit discarded it. They had never executed an assertion. Both invariants turned
out to hold. A scan of all 306 `[<Fact>]`/`[<Theory>]` members found no others — the script
is in this session's transcript and is worth rebuilding if you need it again.

**6. BD is stood up but has not started.** Dan moved to his own clone and gave BD the sandbox
one outright. BD's onboarding is three files in `BdsNotes/`: `memory-cleanup-2026-07-31.md`
(done — his memory was four to eight weeks stale and contradicted the repo in six places),
`memory-archive-2026-07-31.md` (what he stripped), and `wakeup-2026-07-31a.md` (his story).

## What's next

**BD's first story: REQ-JE-1.56.** Two tests for repointing and clearing a journal-entry
comment's secondary link, plus a written change request for the `// REQ-JE-1.56` annotation
he is blocked from making. He branches `je-1.56-tests`, pushes, and Dan reviews the diff in
Rider. No PR — Rider's Compare with Branch is better for F# than a web diff, and the
container has no `gh` anyway.

The story is a deliberate diagnostic: BD must derive the test form from the code and state
his reasoning. If the decision tree in `Tests/README.md` is unclear, his answer is how we
find out.

**One bug of mine to fix, and Dan has not yet said go:** `BdsNotes/wakeup-2026-07-31a.md`
opens with *"First, if you have not already: do the memory cleanup"*. That work is done, but
a fresh BD instance can't know it, so he starts by redoing it. Turn that opening into a
statement of fact rather than a task. Dan asked whether to push it while BD was mid-read;
that question is unanswered.

**Two decisions still Dan's:**

1. `Specs/` write permission for BD. His memory says "BD writes freely, encouraged," which
   predates you taking specs. `memory-cleanup-2026-07-31.md` step 4 tells him to ask. My
   recommendation, already given: he proposes, he doesn't write — one rule, **BD writes
   `Tests/` and `BdsNotes/`, everything else he proposes in prose.**
2. Whether to install `gh` plus a PAT so BD can open real PRs. Deferred, correctly, until
   after his first story. It is a bigger grant than a push key.

## Key resources

| What | Where |
|---|---|
| The test standard — read before reviewing any test | `Tests/README.md` |
| What infrastructure already exists | `Src/README.md` |
| The doc system and the write-once rule | `Specs/README.md` |
| BD's story and house rules | `BdsNotes/wakeup-2026-07-31a.md` |
| What BD's memory used to claim | `BdsNotes/memory-archive-2026-07-31.md` |
| The disposition, with Dan's `[Dan]` markup | `HobsonsNotes/doc-disposition-2026-07-29.md` |
| Audit backlog — 126 items, many already done and unmarked | `Skills/SonOfLeoRequirementsAudit/Runs/2026-07-06a/action-items.md` |
| Checks suite | `Checks/` — `bash Checks/run-all.sh`, and the hook runs `--quick` |
| BD's guards | `/media/dan/fdrive/ai-sandbox/workspace/.claude/hooks/{fsharp,git}-guard.py` |

## Standing corrections

**Read this first, because you failed it again.**

- **Be dramatically less verbose.** Third session running as a standing item. Tables and
  short paragraphs. Lead with the answer. If a reply runs past roughly 250 words, cut it.
  This does *not* apply to deliverable documents — a wakeup or a standard is supposed to be
  complete.

**New this session:**

- **Never work in BD's clone.** You committed to `pr-plumbing-test` without noticing he had
  checked it out under you, and `git push origin main` then succeeded while doing nothing.
  Two agents in one tree fails silently. Yours is `codeprojects`; his is `ai-sandbox`. Check
  `git branch --show-current` before you commit, always.
- **Don't invent threat models.** You argued `Tests.Isolated` shouldn't reach
  `DataAccessLayer` because it would spend a structural guarantee. There was no guarantee —
  it already references `Model`, which owns persistence. Dan caught it in one line. Before
  claiming a boundary exists, check that it exists.
- **Verify before asserting a tooling fact.** You said "no `gh` in the image" from a
  Dockerfile grep, then had to confirm it properly with `docker exec`. The conclusion held;
  the evidence didn't, until it did. `docker exec ai-sandbox-sandbox-run-*` works and is
  cheap.
- **Warnings before the step.** You wrote that rule into `Specs/README.md` and then put BD's
  branch instruction *after* his deliverable in the very next document. Dan caught it.
- **When Dan gives new argument, update the answer.** He talked you off cutting the
  annotation from BD's story — the guard *prescribes* the escape path, so a blocked edit
  rehearses a channel rather than teaching him to fight a wall. That was correct on both
  sides.

**Carried forward:**

- **Dan owns the F#; you own specs/infrastructure/review.** Don't edit `Src/` or `Tests/`
  unless asked.
- **Never run `dotnet test`** against this repo. Review = conformance + `run-all.sh` +
  `dotnet build`.
- **Every review finding cites file:line.** No exceptions.
- **Stop speaking in codes.** P-codes are dead — the document they indexed no longer exists.
  Name the idea; point at his own code.
- **Dan is PO — he decides, you advise.** Make the case once, then defer.
- **Dan has decades of banking and finance engineering.** When he states how something is
  done in industry, that's data.
- **He intends to be in every PR/diff, by choice.** Don't argue to remove him from the loop.
- **F# compile order is load-bearing.** Migration review is your job.
- `[Dan]...[/Dan]` inline annotation works well for document review cycles.

**Flagged for promotion to CLAUDE.md/memory** (raised four sessions running — raise it and
stop carrying it): "Dan owns the F#", "compile order is load-bearing", "never run dotnet
test", "skills live in repo / installed via symlink".

## Outstanding items

- The wakeup-opening fix for BD, above. Unpushed, unasked.
- `Specs/` write-permission ruling.
- `Checks/check-transaction-brackets.sh` — restrict bracket creation to routes. Designed,
  never written.
- `Context.Context.Context` — namespace, module and type all named `Context`. Rename one.
- **The audit backlog needs a marking pass.** #116a, #117a and #118a are CONFIRMED but were
  done weeks ago. The file also contains a verbatim duplicate of the "Phase 4 — GAAP Panel"
  section (lines 184–203 and 232–251).
- #120a — the `unwrap` helper. Still open, still nothing defined. Now **178** raw
  `Result.defaultWith` sites across `Tests/`, not the ~60 specced. `Tests.Helpers` is its
  home. Good second story for BD: mechanical, zero judgment.
- 36 trailing-match railroads remain in the route tests and `Program.fs`. They work. Older
  form.
- `#119a`–`#125a` and ~30 older audit items. `#113a` traceability cross-reference still
  unperformed.
- BD acceptance dry-run — no longer deferred, it is the current task.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
