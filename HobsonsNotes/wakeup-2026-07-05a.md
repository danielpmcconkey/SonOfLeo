# Wakeup — 2026-07-05a

## Who you are

You are **Hobson** — Dan's host-side Claude Code, the butler. Personality and
standing orders in `~/penthouse-pete/CLAUDE.md`; cross-project context in
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`. On SonOfLeo
your lane is **specs, infrastructure, and review** — Dan owns the F# (drives it
through Rider), BD currently touches only tests. This repo lives on BD's side;
you maintain `HobsonsNotes/` here.

## What this project is

SonOfLeo: Dan's hand-rewrite of LeoBloom — cash-basis GAAP personal finance,
F# on .NET 10, PostgreSQL, xUnit. NOT a 1:1 port. Vision (Dan, this session):
imports move INTO the codebase as a first-class staging domain with a
standardized classification rules engine and dedup in staging (not the ledger);
long-term the data feeds an ML-adjacent retirement engine (Monte Carlo
successor). The app will manage Dan's entire financial life.

## Where we are

Branch `core-je-fetching` @ `f0e3009`, pushed. Account CRUD, Fiscal Period
CRUD, and the full JE slice (header/lines/refs/comments/void/fetching, incl.
REQ-JE-3.6–3.9 read queries) are implemented and tested — **323 [<Fact>] tests,
all green per Dan's Rider run 2026-07-05 12:58**. Next dev fronts per Dan:
trial balance, then period close. Not started: reporting, import, recon.

**The main event this session: a full multi-lens audit is (or was) running.**

## What happened last session

1. Dan commissioned a five-lens audit of the whole app: customer (Hobson-as-
   comptroller, three horizons), GAAP (incl. trial-balance/close readiness),
   F#/DDD idiom (teaching-oriented), architecture (structure/god types/DB
   shape — NOT perf), AI-maintainability (BD handoff safety).
2. I **rebuilt the audit skill** at `Skills/SonOfLeoRequirementsAudit/` —
   see its README.md for the invocation contract. Design rule: the script
   never describes codebase state. A scout derives state each run; Dan's
   "where I think we are" statement is a REQUIRED arg; a statement-delta
   auditor diffs the two (both directions). Precedent ledger
   (`resolved-findings.md`) demoted to precedent-not-law: suppress exact
   matches only, re-raise anything ambiguous; a ledger-vet agent checks the
   ledger itself for staleness.
3. Launched the workflow (run id `wf_03ab669c-700`), ~21 agents, output to
   `Skills/SonOfLeoRequirementsAudit/Runs/2026-07-05a/`. Session limit loomed
   mid-run — completion not confirmed at time of writing.
4. Refinements while it ran: `runTests` arg now defaults to FALSE (Dan runs
   suites in Rider; audit cites Rider session logs at
   `~/.cache/JetBrains/Rider*/log/UnitTestLogs/Sessions/`, UTF-16, as recency
   evidence). Rider log "elements" counts = leaf tests + container nodes
   (345 = 323 + 22 on 2026-07-05) — leaf count is the truth.

Dan's statement of position (audit input, verbatim): back-end Account CRUD
done; back-end Journaling CRUD done; CLI exposes account/JE management plus a
few QoL retrieval functions; unit+integration tests solid; trial balance,
period close, reporting, import, reconciliation not started.

## What's next

1. **Check `Skills/SonOfLeoRequirementsAudit/Runs/2026-07-05a/`.**
   - If `99-synthesis.md` exists: read it, walk Dan through it item by item,
     Saturday-exception style. Rulings get appended to `resolved-findings.md`
     (date, scope, overruled/deferred + revisit trigger).
   - If absent/partial: the run died with the session. Re-launch per the
     skill's README (workflow resume is same-session only). Get a FRESH
     where-we-are statement from Dan — do not recycle the one above.
2. After the audit conversation: accepted actions get dispatched (spec edits
   me, code Dan, test tasks BD).
3. Dan's dev horizon: trial balance, then period close.

## Key resources

| What | Where |
|---|---|
| Audit skill (rebuilt this session) | `Skills/SonOfLeoRequirementsAudit/{README.md,requirements-audit.workflow.js}` |
| Audit run output | `Skills/SonOfLeoRequirementsAudit/Runs/2026-07-05a/` |
| Precedent ledger | `Skills/SonOfLeoRequirementsAudit/resolved-findings.md` |
| JE spec | `Specs/Behavioral/JournalEntryCrud.md` |
| Decisions log | `Specs/Decisions.md` |
| Prior wakeup (JE efficacy review context) | `HobsonsNotes/wakeup-2026-07-04a.md` |

## Standing corrections

- **Dan owns the F#, you own the specs.** Review and advise on code; don't
  edit `Src/` or `Tests/` unless asked. You edit `Specs/` directly.
- **Dan is PO — he decides, you advise.** Make the case, then defer.
- **Don't sign off on a query shape without thinking about the consumer's
  return type** (the REQ-JE-3.4→3.9 lesson).
- **Settle empirical questions with data, not memory** — prod DB via the
  `db:leobloom_prod` skill, the repo via grep. This session's example: the
  345-vs-323 test count was resolved by counting attributes, not by arguing.
- **F# compile order is load-bearing** — a file only sees files above it in
  the `.fsproj`.
- **Migration review is your job** — names, nullability, CHECKs, FK
  direction, drop order, per-env ownership, every time.
- **Audit runs need Dan's fresh statement of position** — the skill hard-fails
  without it, by design. Never recycle a stale one.

## Outstanding items

- **Audit synthesis → exception conversation → ledger rulings** (see What's
  next #1) — the big one.
- **Pin whether `external_reference` is a SYS-3.1 entity** (carried from
  07-04a; the audit may have flagged it — check before raising separately).
- **REQ-AC-4.6 test coverage** (deactivation vs JE dates) — verify when next
  reviewing deactivation; the test-truthfulness auditor may have covered it.
- **BdsNotes/wakeup-2026-07-03a.md and HobsonsNotes/wakeup-2026-07-04a.md are
  untracked** in git — BD or Dan may want them committed.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
