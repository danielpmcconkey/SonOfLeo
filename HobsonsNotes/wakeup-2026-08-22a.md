# Wakeup — 2026-08-22a

## Who you are

Hobson, Dan's host-side Claude Code, the butler. CLAUDE.md at
`~/penthouse-pete/CLAUDE.md`, memory at
`~/.claude/projects/-home-dan-penthouse-pete/memory/MEMORY.md`.

## Lanes

| Actor | Owns | Never touches |
|---|---|---|
| **Dan** | `Src/` F#. Assigns all work not covered below. | — |
| **BD** | `Tests/` — own branches, own clone | `Src/`, `Specs/` |
| **Hobson** | `Specs/` (REQs) | `Src/`, `Tests/`; never `dotnet test`; never BD's clone |

Everything else is Dan's to assign per task. See `feedback_sonofleo_lanes.md`.

## Where we are

**Data-ingestion slice is complete. 2026-08-21a audit is dispositioned.**

The classification rules engine was the last piece of the ingestion
slice. Dan built the update route (`updateClassificationRule` in
`ClassificationOrchestration.fs`), Hobson wrote the `ClassificationRuleCrud.md`
spec (domain code `CR`, 44 REQs), BD wrote tests in two batches (A:
evaluation semantics §1-3, B: CRUD + classifier §4-6).

The 2026-08-21a audit ran 31 auditors across 7 batches. Dan dispositioned
all 29 findings: 4 done (fixed during review), 8 overruled, 17 accepted.
Action items are in `Audit/2026-08-21a/action-items.md`.

Codebase at HEAD: ~10,300 lines Src, ~10,000 lines Tests, ~430 tests,
~1,480 lines of specs across 9 behavioral spec files.

## What happened since wakeup-2026-08-21a

### Classification rules (Dan + Hobson + BD)

- Dan built fetchFiltered for StageEntry (ea62745, eb508bb), closing
  audit #25.
- Dan fixed name collisions from new sort types (095f465).
- Dan added empty-list validation at orchestrator level (18ec104) and
  backstop false-returns in doesMatch.
- Dan built the full `updateClassificationRule` route (f91f08a) —
  replaced the old private per-field update functions and
  activate/deactivate with a single multi-field FieldUpdate pattern.
  Removed orphaned `IngestionClassificationRuleToggleOpenNoOp` error.
- Dan removed the trim from `StringSearchPattern.create` (0fabeb0f) —
  whitespace is meaningful in regex patterns.
- Dan refactored `fetchRulesFiltered` to use `readRowsFromDb` instead
  of hand-building its own query (IDIOM-CR-1 fix).
- Hobson wrote `Specs/Behavioral/ClassificationRuleCrud.md` — 7 sections,
  44 REQs covering data states, evaluation semantics, classifier
  behaviors, CRUD, and update. Registered `CR` domain in Specs/README.md.
  Updated forward references in DataIngestion.md.
- Hobson added REQ-CR-2.8 and 2.9 (empty-list evaluation backstops),
  split REQ-CR-4.4/4.8 (testable vs negative-existence), reworded
  REQ-CR-1.21 waiver (validated write path, not type system).
- BD wrote tests in two batches. Batch A: isolated evaluation tests
  for §1-3. Batch B: integrated CRUD tests for §4-6, three fixture
  archetypes, sort coverage. 8 waivers approved.

### Audit (Hobson)

- Ran the 2026-08-21a audit: 31 auditors, 7 batches, staggered across
  a rate-limit reset.
- Dan dispositioned all findings in one pass. Key rulings:
  - Overruled IDIOM-FMC-1 (empty-list create validation) with prejudice
    — infallible-create-with-orchestrator-validation is the established
    pattern.
  - Overruled IDIOM-TZ-1 (duplicate timezone config read) — previously
    ruled, not reopening.
  - Overruled CT-STG-2 (voided entries in dedup) — voids must not block
    re-import; that's the correction path.
  - Accepted NGUI-1.6-INGESTION with 4 sub-items for Dan — account name
    in return types, plus a design change to use account ID instead of
    code in ClassificationRule codeAtMatch.
  - Accepted AMB-STG-1 — Conflict should take priority over NoMatch when
    classification produces both on different lines of the same entry.
    Needs spec fix and test.

### Milestones

- Git tags created: `slice-account-crud` (32660f4, June 20) and
  `slice-journaling` (31da117, Aug 4).
- Codebase growth measured at each milestone.

### Other

- Wrote `HobsonsNotes/classification-handoff-for-bd.md` — business-level
  brief for BD's test onboarding.
- Wrote `HobsonsNotes/wakeup-skill-brief-for-bd.md` — describes the
  wakeup skill so BD can build his own.
- Discussed NGUI spec structure (#5 from prior audit). Agreed to defer —
  Reports CLI lives awkwardly in NGUI §4 but isn't worth the churn to
  move until more reports are built.

## What's next

1. **Hobson: 6 spec fixes from the audit.** AMB-STG-1 (Conflict
   priority), IE-STG-1 (classification_rule_id on 5.5), CON-STG-1
   (FK claim), AMB-JE-3.6.2 (should→must), AMB-NGUI-1 (typed error),
   AMB-RPT-1 (level vs generation). Plus STALE-DAL-EFF verbiage and
   STALE-README-JSON readme fixes.

2. **Dan: 5 code changes.** CUST-ACCTNAME-1 (account name in return
   types), NGUI-1.6-INGESTION (4 sub-items: ID reference, lookup cache,
   name in ClassificationRuleReturn, name in PrioritizedMatchReturn).

3. **BD: 6 test fixes.** EFF-CR-1 (magic strings ×2), EFF-CR-1b
   (fixture organization), BC-STG-1 (OneMatch path), FP-COV-1
   (property assertions), ASSERT-JE-1/3 (count-only and wrong-quantity
   assertions), TEST-MON-1/2 (boundary tests).

4. **Bespoke parsers** — outside SonOfLeo repo. Convert LeoBloomOps
   Python importers to produce the base staging JSONL format. Not started.

5. **Saturday cutover** — can Hobson run the full Saturday routine
   through SonOfLeo without touching LeoBloom's CLI for any ledger or
   obligation operation?

## Key resources

| What | Where |
|---|---|
| **Audit action items** | `Audit/2026-08-21a/action-items.md` |
| **Audit reports** | `Audit/2026-08-21a/10-*.md` |
| **CR spec** | `Specs/Behavioral/ClassificationRuleCrud.md` |
| **STG spec** | `Specs/Behavioral/DataIngestion.md` |
| **Resolved findings** | `Skills/SonOfLeoRequirementsAudit/resolved-findings.md` |
| **Classification code** | `Src/Model/DataIngestion/Classification/` |
| **Classification orchestrator** | `Src/ModelOrchestrator/ClassificationOrchestration.fs` |
| **Migration roadmap** | Memory: `project_sonofleo_roadmap.md` |
| **Prior wakeup** | `HobsonsNotes/wakeup-2026-08-21a.md` |

## Standing corrections

- **Hobson owns REQs only.** Everything else is Dan's to assign.
- **Never run `dotnet test`.** Dan's/BD's lane.
- **Never work in BD's clone.** Yours is `codeprojects`; his is `ai-sandbox`.
- **Never edit Src/ files.** Dan writes all F# code. Review only.
- **Dispositions outrank re-derivation.** If your fresh reading contradicts
  a disposition, the disposition wins — raise the conflict with Dan.
- **Dan is PO — make the case once, then defer.**
- **Account codes, not IDs in staging.** Code-to-ID resolution at post time.
- **fi_reference is always required.** Parsers derive a deterministic
  reference for sources without native IDs.
- **Classification authority hierarchy:** parser > classifier > operator.
- **Manual review = operator authority.** Status is caller-set, not
  auto-inferred.
- **Same-file duplicate fi_reference is a parser defect.** Caught at
  ingestion validation, not dedup.
- **JE source is provenance, not FI identity.** Fixed label like "Data
  ingestion import". The FI name goes on the external reference.
- **Pull before editing, pull before committing.** Dan and BD push
  concurrently. Use `--rebase`.
- **Check `git log` against BD's last commit before writing hand-off
  summaries.** Don't repeat things already in his clone.
- **Traceability hook is SLOW.** Skipped in pre-commit. Specs and tests
  land in separate commits from different actors. Run full
  `Checks/run-all.sh` manually when you need invariant 2.
- **Infallible create, orchestrator validates.** This is the established
  pattern. Don't propose moving validation into create functions —
  Dan overruled it with prejudice (IDIOM-FMC-1).
- **StringSearchPattern is not trimmed.** Whitespace is meaningful in
  regex patterns. Exception to REQ-SYS-1.1.

## Outstanding items

- **COA renumbering** — deferred to LeoBloom session.
- **NGUI §4 reorganization** — deferred until more reports are built.
- **`slice-ingestion` tag** — create after audit action items are closed.

---

**Do not take action.** Read this document, read what it points you to,
and report back to Dan: where we are, what's next, and any questions.
Then wait for instruction.
